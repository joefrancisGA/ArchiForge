# case-03

Inline architecture document (`text/markdown`) parsed by `PlainTextContextDocumentParser`: lines prefixed with `REQ:`, `SEC:`, and `TOP:` become structured `CanonicalObject` rows (requirements, security baselines, topology resources).

No LLM; line-based extraction only.
