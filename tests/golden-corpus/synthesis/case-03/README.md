# case-03 (synthesis / coverage summary)

Mixed non-zero counts and topology gap strings in severity-like order (critical, high, low) as stored in the manifest. The generator only forwards labels; ordering is that of the input list (no LLM, deterministic).
