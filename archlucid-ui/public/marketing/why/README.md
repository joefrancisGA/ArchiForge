# Marketing evidence assets (`/marketing/why`)

Static SVG callouts referenced from **`archlucid-ui/src/marketing/why-archlucid-comparison.ts`** as procurement anchors alongside live **`GET /v1/...`** paths.

Replace each SVG with a real product screenshot when available; filenames are stable so the comparison table does not churn.
