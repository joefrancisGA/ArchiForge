"use client";

/** Re-export: same boundary as mutation flows; use this name for heavy client-only sections (graph, compare, replay). */
export { MutationErrorBoundary as ClientErrorBoundary } from "@/components/MutationErrorBoundary";
