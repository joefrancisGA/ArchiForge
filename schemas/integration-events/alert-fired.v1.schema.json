{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://archlucid.dev/schemas/integration-events/alert-fired.v1.schema.json",
  "title": "com.archlucid.alert.fired",
  "description": "New alert row persisted and delivered (canonical com.archlucid.alert.fired; legacy com.archiforge.alert.fired).",
  "type": "object",
  "required": [
    "schemaVersion",
    "tenantId",
    "workspaceId",
    "projectId",
    "alertId",
    "ruleId",
    "category",
    "severity",
    "title",
    "deduplicationKey"
  ],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "tenantId": { "type": "string", "format": "uuid" },
    "workspaceId": { "type": "string", "format": "uuid" },
    "projectId": { "type": "string", "format": "uuid" },
    "alertId": { "type": "string", "format": "uuid" },
    "runId": { "type": "string", "format": "uuid" },
    "comparedToRunId": { "type": "string", "format": "uuid" },
    "ruleId": { "type": "string", "format": "uuid" },
    "category": { "type": "string" },
    "severity": { "type": "string" },
    "title": { "type": "string" },
    "deduplicationKey": { "type": "string" }
  },
  "additionalProperties": true
}
