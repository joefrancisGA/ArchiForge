# ArchiForge → ArchLucid Rename Checklist

Incremental rename plan. One batch is folded into each working session.
See `.cursor/rules/ArchLucid-Rename.mdc` for the standing instruction.

> **Rule**: keep the build green after every batch. Never break CI.

---

## Phase 1 — Display text and documentation (low risk, any session)

- [x] 1.1 Update product name in `archlucid-ui/src/app/layout.tsx` (title, heading)
- [x] 1.2 Update `archlucid-ui/README.md` — product name references
- [x] 1.3 Update root `README.md` — product name, badges, description (2026-04-04)
- [x] 1.4 Update `docs/GLOSSARY.md` (2026-04-05)
- [x] 1.5 Update `docs/ARCHITECTURE_CONTEXT.md`, `docs/ARCHITECTURE_COMPONENTS.md`, `docs/ARCHITECTURE_CONTAINERS.md` (2026-04-05)
- [x] 1.6 Update `docs/DEPLOYMENT.md`, `docs/DEPLOYMENT_TERRAFORM.md`, `docs/CONTAINERIZATION.md` (2026-04-06 — ArchLucid product naming pass where referenced)
- [x] 1.7 Update `docs/PILOT_GUIDE.md`, `docs/OPERATOR_QUICKSTART.md`, `docs/RELEASE_SMOKE.md`, `docs/RELEASE_LOCAL.md` (2026-04-06 — ArchLucid product naming in pilot/release docs; literal `ArchiForge.*` paths and config keys unchanged)
- [x] 1.8 Update onboarding docs: `docs/onboarding/day-one-developer.md`, `day-one-sre.md`, `day-one-security.md`, `docs/CONTRIBUTOR_ONBOARDING.md`, `docs/ONBOARDING_HAPPY_PATH.md` (2026-04-06 — ArchLucid product naming in prose; repo folder, `ArchiForge.*` assemblies, and config keys unchanged)
- [x] 1.9 Update `docs/BUILD.md`, `docs/FORMATTING.md`, `docs/TEST_STRUCTURE.md`, `docs/TEST_EXECUTION_MODEL.md` (2026-04-06 — ArchLucid product naming note / prose; `ArchiForge.*` paths unchanged)
- [x] 1.10 Update remaining docs: ADRs, runbooks, CLI docs, changelogs, and any other `.md` files with stale references (batch as needed) (2026-04-07 — product naming + API/DI/header alignment; Phase 7 literals retained)
  - [x] API SLO doc + synthetic probe runbook link (`docs/API_SLOS.md`, `SLO_PROMETHEUS_GRAFANA.md` §10, `.github/workflows/api-synthetic-probe.yml`) (2026-04-06)
  - [x] AgentRuntime multi-vendor LLM seam: `ILlmProvider`, `LlmProviderDescriptor`, `LlmProviderAuthScheme`; `IAgentCompletionClient` : `ILlmProvider`; DI `ILlmProvider` → `ILlmCompletionProvider` (2026-04-06)
  - [x] Agent prompt versioning: catalog templates + SHA-256, `AgentExecutionTrace` repro fields, `AgentPrompts` release labels, OTel tags (2026-04-06)
  - [x] `docs/CONTAINERIZATION.md` — Docker `restore` / `publish` stages must use `-r linux-musl-x64` with `--no-restore` publish (NETSDK1047) (2026-04-06)
- [x] 1.11 Update `archlucid-ui/docs/*.md` (ARCHITECTURE, COMPONENT_REFERENCE, TESTING_AND_TROUBLESHOOTING, OPERATOR_SHELL_TUTORIAL, etc.) (2026-04-07)
- [x] 1.12 Update `.cursor/rules/Navigation.mdc` and `CSharp-EmbeddedStatements-NoBraces.mdc` (2026-04-06 — Navigation: ArchLucid product naming + `ArchLucidHostingRole.cs` path; CSharp rule: no product literals, verified)

## Phase 2 — Configuration key bridges (medium risk, any session)

- [x] 2.1 Add fallback readers for `ArchiForgeAuth` → `ArchLucidAuth` in auth options binding (2026-04-05)
- [x] 2.2 Add fallback readers for `ArchiForge:StorageProvider` → `ArchLucid:StorageProvider` (2026-04-05)
- [x] 2.3 Add fallback readers for any other `ArchiForge*` config sections (grep `appsettings*.json`) (2026-04-07 — `ResolveArchLucidOptions` loads legacy `ArchiForge` section then overlays `ArchLucid`; `FeatureManagementAuthorityPipelineModeResolver` uses bridge for effective storage mode)
- [x] 2.4 Update `appsettings*.json` files to use new key names — **2026-04-07** (`ArchLucid`, `ArchLucidAuth`, `ConnectionStrings:ArchLucid`, Webhook CloudEvents paths, consulting template strings; bridge still reads legacy `ArchiForge` keys)
- [x] 2.5 Update `.env.example` — `ARCHLUCID_API_KEY` preferred + `ARCHIFORGE_API_KEY` fallback in proxy (2026-04-05)
- [x] 2.6 Add OIDC storage key bridge reads (`archiforge_oidc_*` → `archlucid_oidc_*`) in `session.ts` and `storage-keys.ts` — **2026-04-07**

## Phase 3 — UI directory and package rename (medium risk, any session)

- [x] 3.1 Rename `archiforge-ui/` directory → `archlucid-ui/` — **2026-04-07**
- [x] 3.2 Update `package.json` name field — **2026-04-07**
- [x] 3.3 Update Dockerfile path references (API Dockerfile, docker-compose, CI workflows) — **2026-04-07**
- [x] 3.4 Update `archlucid-ui` references in root `README.md`, docs, and CI — **2026-04-07**
- [x] 3.5 Update TypeScript imports, API route env var names, and correlation header names — **2026-04-07** (`ARCHIFORGE_API_BASE_URL` retained with proxy bridge)

## Phase 4 — Infrastructure rename (medium-high risk, one stack per session)

- [x] 4.1 `infra/terraform-monitoring/` — **2026-04-07** (descriptions + Grafana folder title **ArchLucid**; Terraform resource addresses e.g. `grafana_folder.archiforge` unchanged for state)
- [x] 4.2 `infra/terraform-storage/` — **2026-04-07** (no product literals requiring change)
- [x] 4.3 `infra/terraform-edge/` — **2026-04-07** (no product literals requiring change)
- [x] 4.4 `infra/terraform-container-apps/` — **2026-04-07**
  - [x] Worker `command` → `ArchLucid.Worker.dll` (2026-04-06). Defaults **`archlucid-api`**, **`archlucid-ui`**, **`archlucid-worker`**; template container names updated. **2026-04-08:** Terraform **variable defaults** for LAW/CAE/budget/VNet/Grafana prefixes → **`archlucid-*`** (greenfield); **resource addresses** (e.g. `azurerm_api_management.archiforge`) still **`archiforge`** until **7.5** `state mv`.
- [x] 4.5 `infra/terraform-sql-failover/` — **2026-04-07** (defaults unchanged for state)
- [x] 4.6 `infra/terraform-openai/` — **2026-04-07**
- [x] 4.7 `infra/terraform-entra/` — **2026-04-07** (consent strings + default display **ArchLucid API**)
- [x] 4.8 `infra/terraform/` — **2026-04-07** (APIM display **ArchLucid API**, publisher default **ArchLucid**)
- [x] 4.9 `infra/terraform-private/` — **2026-04-07** (PE resource names unchanged for state)
- [x] 4.10 Prometheus — **`archlucid-alerts.yml`**, **`archlucid-slo-rules.yml`** — **2026-04-07**; **2026-04-08** metric + recording series renames to `archlucid_*` / `archlucid:slo:*` (OTel parity)
- [x] 4.11 Grafana — **`dashboard-archlucid-*.json`** — **2026-04-07**
- [x] 4.12 CI/CD workflows — **`archlucid-api`**, **`archlucid-ui`** image tags — **2026-04-07**
- [x] 4.13 `docker-compose.yml` — **2026-04-07** (`ConnectionStrings__ArchLucid`, `ArchLucid__*`, `archlucid-api` container)
- [x] 4.14 `stryker-config.json`, `coverage.runsettings`, `.devcontainer/devcontainer.json` — **2026-04-07** (verified: no `archiforge` literals)

## Phase 5 — .NET project directory and file renames (high risk — ASK USER FIRST)

> **Do not start this phase without explicit user confirmation.**
> Strategy: rename directories and `.csproj` files but preserve `<RootNamespace>ArchiForge.*</RootNamespace>`
> so no C# source changes are needed yet. Update `.sln` and `<ProjectReference>` paths.

- [x] 5.1 Rename leaf test projects: `*.Tests` directories and `.csproj` files (2026-04-06 — folders + `ArchLucid.*.Tests.csproj`; `AssemblyName`/`RootNamespace` kept `ArchiForge.*.Tests` until Phase 6)
- [x] 5.2 Rename `ArchiForge.TestSupport` → `ArchLucid.TestSupport`, `ArchiForge.Benchmarks` → `ArchLucid.Benchmarks` (2026-04-06)
- [x] 5.3 Rename `ArchiForge.Cli`, `ArchLucid.Cli.Tests`, `ArchiForge.Backfill.Cli` → `ArchLucid.*` (2026-04-06)
- [x] 5.4 Rename `ArchiForge.Api.Client`, `ArchLucid.Api.Client.Tests` → `ArchLucid.Api.Client*` dirs (2026-04-06)
- [x] 5.5 Rename domain projects: `Core`, `Contracts`, `Persistence`, `Application`, `AgentRuntime`, `AgentSimulator` (2026-04-06)
- [x] 5.6 Rename domain projects: `Coordinator`, `Decisioning`, `KnowledgeGraph`, `Retrieval`, `ContextIngestion`, `Provenance`, `ArtifactSynthesis` (2026-04-06)
- [x] 5.7 Rename host projects: `Host.Core`, `Host.Composition` (2026-04-06)
- [x] 5.8 Rename `ArchiForge.Api`, `ArchiForge.Worker` → `ArchLucid.Api`, `ArchLucid.Worker` (2026-04-06)
- [x] 5.9 `ArchiForge.sln` → `ArchLucid.sln` (paths + CI/scripts/docs) (2026-04-06)
- [x] 5.10 Update `Directory.Build.props` — `ArchLucidApiClientPackageVersion` + `ArchLucidApiClientPackageVersion` bridge (2026-04-06)
- [x] 5.11 Update `templates/archlucid-finding-engine/` directory and contents — **2026-04-07** (`ArchLucidFindingEngine`, `dotnet new archlucid-finding-engine`)
- [x] 5.12 Rename C# files with `ArchiForge` prefix (instrumentation, configuration bridge, policies, UoW, CLI client, etc.) (2026-04-06)
- [x] 5.13 Update NSwag config (`nswag.json`) and regenerate client (`ArchLucidApiClient.g.cs`) (2026-04-06)

## Phase 6 — Bulk namespace rename (high risk — ASK USER FIRST)

> **Do not start this phase without explicit user confirmation.**
> This is a single large pass that updates all `namespace ArchiForge.*` and `using ArchiForge.*` directives.
> Remove `<RootNamespace>` overrides added in Phase 5.

- [x] 6.1 Bulk rename all `namespace ArchiForge.*` → `namespace ArchLucid.*` across product `.cs` files (2026-04-06)
- [x] 6.2 Bulk rename all `using ArchiForge.*` → `using ArchLucid.*` (2026-04-06)
- [x] 6.3 Update XML doc `<see cref="ArchiForge.*">` references where applicable (2026-04-06)
- [x] 6.4 Update string literals: OTel meter/activity names, `X-ArchLucid-*` headers, Swagger titles; legacy **`ArchiForge:*` / `ArchiForgeAuth` / `ArchiForge.sql` / DB name** intentionally retained where operational (2026-04-06); **2026-04-08** follow-up — user-facing/export/CLI/prompt strings → **ArchLucid**; CI guard `scripts/ci/archiforge-rename-allowlist.txt` + `.github/workflows/ci.yml` step after Release build
- [x] 6.5 Remove `<RootNamespace>` overrides — product projects use `ArchLucid.*` aligned with dirs (2026-04-06)
- [x] 6.6 Full `Release` build + fast-core test filter; relaxed flaky bulkhead timing assertion (2026-04-06)
- [x] 6.7 Update `docs/NEXT_REFACTORINGS.md` to reflect completion (2026-04-06)

## Phase 7 — Cleanup and external (operational — ASK USER FIRST)

- [x] 7.1 Remove config key fallbacks added in Phase 2 (old `ArchiForge*` keys) — **2026-04-08** (startup warning only when legacy keys present; no silent reads)
- [x] 7.2 Remove OIDC storage key bridge reads (old `archiforge_oidc_*` keys) — **2026-04-08**
- [x] 7.3 Remove environment variable fallbacks (old `ARCHIFORGE_*` vars) — **2026-04-08** (UI warns when legacy env vars set)
- [x] 7.4 Update SQL master DDL script (`ArchiForge.sql` → `ArchLucid.sql`); add a new migration for database rename if needed — **2026-04-08** (`044` comment reference, `045` graph property key; RLS object names unchanged in DB)
- [ ] 7.5 Terraform `state mv` operations for renamed resources (coordinate with deploy window) — **deferred — runbook ready:** [`docs/runbooks/TERRAFORM_STATE_MV_PHASE_7_5.md`](runbooks/TERRAFORM_STATE_MV_PHASE_7_5.md) (2026-04-12)
- [ ] 7.6 Rename GitHub repository — **deferred**
- [ ] 7.7 Update Entra ID app registrations (redirect URIs, display names) — **deferred**
- [ ] 7.8 Rename workspace root directory (`c:\ArchiForge\ArchiForge` → `c:\ArchLucid\ArchLucid`) — **deferred** — see [RENAME_DEFERRED_RATIONALE.md](RENAME_DEFERRED_RATIONALE.md) (2026-04-14)
- [x] 7.9 Final grep for any remaining `ArchiForge` / `archiforge` in **product-facing** docs and infra **identifiers** — **2026-04-10** sweep complete (see progress log). **Still expected by design:** this checklist title/history; **`BREAKING_CHANGES.md`** rows documenting **removed** legacy key spellings; **`MULTI_TENANT_RLS.md`** committed SQL **object** names (`rls.ArchiforgeTenantScope`, `rls.archiforge_scope_predicate`, migration filename **`036_RlsArchiforgeTenantScope.sql`**); CI allowlist paths **`scripts/ci/archiforge-rename-allowlist*.txt`**; Terraform **resource addresses** (`*.archiforge`); `.gitleaks.toml` allowlisted dev password literals. **Deferred:** 7.5–7.8.

---

## Progress log

| Date | Batch | Notes |
|------|-------|-------|
| 2026-04-14 | Quality prompts 1A–1D (agent forensics + eval hook + UI) | **`IAgentOutputTraceEvaluationHook`** / **`AgentOutputTraceEvaluationHook`**: post-execute **`EvaluateAndRecordMetricsAsync`** from **`ArchitectureRunExecuteOrchestrator`** (swallowed failures). **`GoldenAgentResultJsonEvaluationTests`** + fixtures. **`archlucid-ui`**: **`RunAgentForensicsSection`**, **`getRunTraces`** / **`getRunAgentEvaluation`**, run detail integration. Docs: **`AGENT_TRACE_FORENSICS.md`** (inline persistence, audit, histogram), **`OBSERVABILITY.md`**, **`AUDIT_COVERAGE_MATRIX.md`** (**`AgentTraceBlobPersistenceFailed`** row). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Live E2E — JWT parity (local PEM + CI mint) | **`ArchLucid.Api`**: **`JwtBearer`** local PEM loads **`RsaSecurityKey` from exported parameters** (avoid disposed **`RSA`**); **`MapInboundClaims = false`** for **`roles`** alignment; **`JwtLocalSigningWebAppFactory`** uses **`UseSetting`** so tests override **`appsettings.json`** **`DevelopmentBypass`**. **`ci.yml`** **`ui-e2e-live-jwt`** (subset, **`continue-on-error: true`**); **`live-e2e-nightly.yml`** **`live-e2e-nightly-jwt`**; **`docs/LIVE_E2E_JWT_SETUP.md`**; updates **`LIVE_E2E_HAPPY_PATH`**, **`LIVE_E2E_AUTH_PARITY`**, **`LIVE_E2E_AUTH_ASSUMPTIONS`**; **`playwright.live.config.ts`** forwards **`ARCHLUCID_PROXY_BEARER_TOKEN`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Live E2E — ApiKey parity (prompts 1–5) | **`docs/LIVE_E2E_AUTH_ASSUMPTIONS.md`**, **`LIVE_E2E_AUTH_PARITY.md`**; **`live-api-client.ts`** **`LIVE_API_KEY`** / **`LIVE_API_KEY_READONLY`**, **`liveJsonHeaders`**, governance raw helpers optional **`apiKey`**; **`live-api-apikey-auth.spec.ts`**; journey/negative specs use **`liveAuthActorName`** / **`livePeerReviewerActorName`**; **`ci.yml`** **`ui-e2e-live-apikey`**; **`live-e2e-nightly.yml`** DevelopmentBypass + ApiKey; **`LIVE_E2E_HAPPY_PATH`**, **`TEST_STRUCTURE`**, **`TEST_EXECUTION_MODEL`**, **`QUALITY_ASSESSMENT_2026_04_14`**, **`ARCHITECTURE_INDEX`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | CodeQL `cs/log-forging` — Application + API pipeline | **`LogSanitizer.Sanitize`** + **`IsEnabled`**/braces on user- or externally influenced structured log args in **`RunDetailQueryService`**, **`ApprovalSlaMonitor`**, **`ArchitectureRunExecuteOrchestrator`**, **`ArchitectureRunCreateOrchestrator`**, **`ArchitectureRunCommitOrchestrator`** (durable-audit warning), **`CoordinatorRunFailedDurableAudit`**, **`DefaultAgentEvaluationService`**, **`MermaidCliDiagramImageRenderer`**; **`ArchitectureApplicationService`** information **`if`** braces; **`PipelineExtensions`** (sanitize **`Method`**, **`IsEnabled`** on unhandled-exception log); **`WorkerHostPipelineExtensions`** **`IsEnabled`** on unhandled-exception log; **`HostLeaderElectionCoordinator`** lease-loss warning guard; **`Program.cs`** startup configuration error lines + pipeline start **`IsEnabled`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six prompts — residual doc + tests | **`LOAD_TEST_BASELINE.md`** / **`TEST_STRUCTURE.md`**: merge-blocking k6 summary assert; **`AGENT_TRACE_FORENSICS.md`**: sentinel table aligned to **`AgentExecutionTraceModelMetadata`**; **`RLS_RISK_ACCEPTANCE.md`**: concrete uncovered-table mirror of **`MULTI_TENANT_RLS.md`** §9; **`API_KEY_ROTATION.md`**: **`IOptionsMonitor`** reload (no restart when host refreshes config); **`TERRAFORM_STATE_MV_PHASE_7_5.md`**: inventory stamp **2026-04-14**; audit UI: **`role="status"`** + **`aria-live="polite"`** on result summary; **`GoldenAgentExecutionTraceTests`** + JSON fixtures; **`ExplainabilityTraceCompletenessAnalyzerPropertyTests`** (FsCheck); **`EXPLAINABILITY_TRACE_COVERAGE.md`** / **`AGENT_OUTPUT_EVALUATION.md`** cross-refs. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six quality prompts — verification | **`OpenApiContractSnapshotTests`** green after explainability DTO changes; **`docs/coverage-exclusions.md`** merged-line floor aligned to **71%**; filtered **`dotnet test ArchLucid.sln`** for **`RunExplanationSummaryServiceTests`**, **`CoordinatorRunFailedDurableAuditTests`**, **`TraceConfidenceLabelsPropertyTests`**, **`ExplainabilityTraceCompleteness`** (13 + 2 + 9 passed in respective assemblies). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six-prompt build follow-up | **`RunGoldenManifestConsistencyHealthCheck`**: `using ArchLucid.Persistence.Connections` for **`SqlTransientDetector`**. **`DataConsistencyOrphanProbeHostedService`**: **`Counter.Add`** with **`KeyValuePair<string, object?>`** tag (avoid **`TagList`** compile issue in Host.Core). **`ApiKeyAuthenticationHandlerTests`**: **`using Microsoft.Extensions.Logging`** for **`ILoggerFactory`**. **Release** **`dotnet build ArchLucid.sln`**; **139** **`ArchLucid.AgentRuntime.Tests`**; **677** **`ArchLucid.Api.Tests`**; **74** Vitest files / **340** tests. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six quality prompts (execution) | **1** Live E2E: `live-api-search-ask-graph.spec.ts`, `live-api-alert-rules.spec.ts` + `toRunGuidPathSegment` helper. **2** `IAgentOutputEvaluationHarness` / `AgentOutputEvaluationHarness` + Core tests. **3** `ApiKeyAuthenticationOptions` + `IOptionsMonitor` in `ApiKeyAuthenticationHandler` + rotation-style unit test. **4** Sonner `AppToaster`, `@/lib/toast`, governance dashboard toasts, `CollapsibleSection` on run detail (explanation + artifacts). **5** `RunGoldenManifestConsistencyHealthCheck`, `DataConsistencyOrphanProbeHostedService`, `archlucid_data_consistency_orphans_detected_total`, Prometheus alert, matrix + observability docs. **6** `NEXT_REFACTORINGS` triage note + §8 archive pointer. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (Six prompts — completion) | **Prompt 3:** `AgentOutputQualityGate` + `archlucid_agent_output_quality_gate_total`; `RunExplanationAggregateOptions` + deterministic aggregate fallback + `archlucid_explanation_aggregate_faithfulness_fallback_total`. **Prompt 4:** `GET /v1/authority/runs/{runId}/pipeline-timeline`, `IRunPipelineAuditTimelineService`, UI `AuthorityPipelineTimeline`. **Prompt 5:** `docs/runbooks/API_KEY_ROTATION.md`, `docs/security/RLS_RISK_ACCEPTANCE.md` + cross-links. **Prompt 6:** `CombinedFailureChaosTests`, `tests/load/soak.js`, `.github/workflows/k6-soak-scheduled.yml` + perf/chaos/test docs. OpenAPI snapshot updated. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Six quality improvements — wrap-up) | Cross-links: **`SECURITY.md`** → **`SYSTEM_THREAT_MODEL.md`**; **`ARCHITECTURE_CONSTRAINTS.md`** DDL smoke prose aligned with **`dbo.Runs`**; **`EXPLAINABILITY_TRACE_COVERAGE.md`** faithfulness section; **`OBSERVABILITY.md`** **`archlucid_explanation_faithfulness_ratio`**; **`LOAD_TEST_BASELINE.md`** keyset note; **`CAPACITY_AND_COST_PLAYBOOK.md`** per-tenant LLM tags; **`ARCHITECTURE_INDEX.md`** (coverage, Stryker ratchet, threat model, geo drill, explainability). **`TenantScopedTableDdlTests`**: match **`CREATE TABLE dbo.Runs`** + unbracketed column ids; **`ExplanationFaithfulnessCheckerTests`**: isolate rule-id token. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (Quality prompts + Phase 7.5 runbook) | Live E2E / k6 docs + CI k6 job; audit UI CSV + summary; **`ComparisonSummaryPersisted`** matrix; agent trace blob keys (**`053`**), **`AgentExecution:TraceStorage:PersistFullPrompts`**; pre-commit gate (**`054`**, **`ArchLucid:Governance:PreCommitGateEnabled`**, **`#governance-pre-commit-blocked`**); **`docs/runbooks/TERRAFORM_STATE_MV_PHASE_7_5.md`** for **`state mv`**. Item **7.5** → deferred with runbook link. **7.6–7.8** still deferred. |
| 2026-04-12 | (Session A — explainability + governance + traces) | **`IDeterministicExplanationService`** / **`DeterministicExplanationService`**; **`AgentExecutionTrace`** token counts + optional **`EstimatedCostUsd`** (**`LlmCostEstimationOptions`**, **`ILlmCostEstimator`**); **`GET …/approval-requests/{id}/rationale`** + **`IGovernanceRationaleService`**; lineage finding **`sourceAgentExecutionTraceId`**; Simulator trace recorder passes explicit null token counts; UI governance dashboard **Approve/Reject** with **`ConfirmationDialog`**; **`getting-started`** page + nav; **`appsettings.Development.json`** sample **`AgentExecution:LlmCostEstimation`**; OpenAPI snapshot + NSwag client regen. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Docs + governance + pipeline + quality gates) | **`GET /v1/governance/compliance-drift-trend`** + **`ComplianceDriftTrendService`**; promotion/approval **dev → test → prod** only (**`GovernanceEnvironmentOrder`**). **`AuthorityPipelineOptions`** + orchestrator **linked CTS** timeout + **`archlucid_authority_pipeline_timeouts_total`**. UI **`ComplianceDriftChart`** on governance dashboard. **`RunId`** value type (**`ArchLucid.Core.Identity`**) + tests. Docs: **`DEGRADED_MODE.md`**, **`START_HERE.md`** (reading order + tiers + degraded link), **`DATA_CONSISTENCY_MATRIX.md`** (replica lag + **`RunId`** note), **`archive/README.md`**, **`ARCHITECTURE_INDEX.md`** (archive section), **`API_FUZZ_TESTING.md`**, **`UI_COMPONENTS.md`**. CI: **`api-schemathesis-light`** in **`ci.yml`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (CI — Docker actions Node 24) | **`docker/setup-buildx-action@v4`**, **`docker/build-push-action@v7`** in **`ci.yml`** (docker-build-smoke, ZAP), **`cd.yml`**, **`cd-staging-on-merge.yml`** — official Docker actions ship Node **24** runtimes (runner **≥ v2.327.1**), clearing Node **20** deprecation noise under **`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Greenfield SQL boot — quality improvement #1) | **`GreenfieldSqlApiFactory`**, **`GreenfieldSqlBootIntegrationTests`** (`ArchLucid.Api.Tests`, **`Xunit.SkippableFact`**); CI **`api-greenfield-boot`** after **`dotnet-fast-core`**; **`dotnet-full-regression`** **`needs`** **`api-greenfield-boot`**. Docs: **`TEST_EXECUTION_MODEL.md`**, **`TEST_STRUCTURE.md`**, **`QUALITY_ASSESSMENT_2026_04.md`** supplement. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Execute → `ReadyForCommit` on `dbo.Runs`) | **`ArchitectureRunExecuteOrchestrator`**: after persist (and on idempotent execute when results already exist), **`LegacyRunStatus` → `ReadyForCommit`** when Topology/Cost/Compliance/Critic each have exactly one result — fixes live Playwright **`waitForReadyForCommit`** timeouts (ADR-0012 had stopped updating the header). Unit test **`ExecuteRunAsync_after_persist_sets_LegacyRunStatus_ReadyForCommit_when_four_required_agents_complete`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Storage provider composition tests) | **`InMemoryStorageProviderRegistrar`** calls **`RegisterHotPathReadCaching`** (parity with Sql + **`StorageProviderRegistrationParityTests`**). Parity allowlist: **`IOptionsChangeTokenSource`/`IConfigureOptions` for `SqlServerOptions`**. **`StorageProviderDiGraphValidationTests`**: **`AddHttpContextAccessor`**, no-op **`IAuditService`** (registered in **`Program.cs`**, not composition). Filtered **`StorageProvider`** tests: **4 passed** in ~0.2s after build. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (CodeQL `cs/log-forging` — Host.Core) | **`LogSanitizer.Sanitize`** on structured log args derived from requests, queue payloads, Service Bus messages, and **`ex.Message`** in **`ComparisonReplayApiService`**, **`ArchitectureApplicationService`**, **`WorkerHostPipelineExtensions`**, **`BackgroundJobQueueProcessorHostedService`**, **`InMemoryBackgroundJobQueue`**, **`AzureServiceBusIntegrationEventPublisher`/`Consumer`**, **`CircuitBreakerAuditBridge`**, **`AuthorityPipelineWorkProcessor`**, **`HostLeaderElectionCoordinator`**, **`LoggingIntegrationEventHandler`**, **`AskService`**. Braces on **`ComparisonReplayApiService`** information log **`if`**. **`dotnet build ArchLucid.Host.Core` (Release)** OK. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (Host.Composition coverage + CPM) | **`ServiceCollectionExtensionsCompositionResolveTests`**: build **`ServiceProvider`** after **`AddArchLucidApplicationServices`** (Real Azure + optional fallback LLM) to cover composition lambdas; **`IConfiguration`** via **`AddSingleton(typeof(IConfiguration), configuration)`**; **`ArchLucid.Core.Tests`**: drop bogus **`Microsoft.Extensions.Configuration.Memory`** reference (**`AddInMemoryCollection`** lives on **`Microsoft.Extensions.Configuration`**). **`docs/BUILD.md`** note corrected. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (CLI `trace` command) | **`archlucid trace <runId>`**: **`TraceCommand`** uses **`GetRunAsync`** (same as **`status`**, **`GET v1/architecture/run/{runId}`**), reads **`run.otelTraceId`**, **`ARCHLUCID_TRACE_VIEWER_URL_TEMPLATE`** (+ optional **`ARCHLUCID_TRACE_OPEN_BROWSER`**). **`RunInfo.OtelTraceId`** on CLI client DTO. Tests: **`TraceCommandTests`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (run detail `OtelTraceId`) | **`ArchitectureRun.OtelTraceId`** + **`RunRecordToArchitectureRunMapper`**; run detail JSON already includes **`run.otelTraceId`** via **`RunRecord`**. UI: **`Creation trace`** + **`RunTraceViewerLink`** on **`runs/[runId]/page.tsx`**; **`RunDetail.run.otelTraceId`** in **`authority.ts`**. OpenAPI snapshot: **`otelTraceId`** on **`RunRecord`** + **`ArchitectureRun`**; **`RunDetailDto`** remarks → regenerate with **`ARCHLUCID_UPDATE_OPENAPI_SNAPSHOT=1`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (audit retention docs) | **`docs/AUDIT_RETENTION_POLICY.md`** — hot/warm/cold tiers, Migration **051**, **`GET /v1/audit/export`** workflow (`curl`), regulatory notes, future archival/Terraform. **`CHANGELOG.md`**, **`AUDIT_COVERAGE_MATRIX.md`** (read-path table), **`ARCHITECTURE_INDEX.md`** cross-links. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (audit export API) | **`GET /v1/audit/export`**: JSON + **`text/csv`** (**`AuditEventCsvFormatter`**, CSV **`Content-Disposition`** via formatter + **`HttpContext.Items`**); **`IAuditRepository.GetExportAsync`** (Dapper + in-memory, **`TOP`/`ORDER BY OccurredUtc ASC`**); controller + repo clamp **`maxRows`** 1–10,000; **`[EnableRateLimiting("expensive")]`**; validate **`fromUtc` < `toUtc`** and range ≤ 90 days. Tests: **`AuditExportControllerTests`**, **`AuditEventCsvFormatterTests`**, audit repo contract additions; **`openapi-v1.contract.snapshot.json`** regenerated. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (policy packs E2E) | **`policy-packs/page.tsx`**: after create + `load()`, **`setSelectedPackId(created.policyPackId)`** so Publish is not gated on `useEffect` races. **`e2e/policy-packs-mock-state.ts`**: **`listMockVersions`** returns full version row (**`policyPackVersionId`**, **`createdUtc`**, **`policyPackId`**) so the UI does not throw on **`.slice`**. **`e2e/policy-packs-journey.spec.ts`**: **`test.setTimeout(120_000)`** so expects are not capped by the default 30s test timeout. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (doc/DDL header alignment) | **`ArchLucid.sql`** header — DOCUMENTATION line: "run header model" (not "two Run tables"); CONTENT OVERVIEW — note authority is **`dbo.Runs`** after **049**. **`docs/GLOSSARY.md`** — remove legacy **`dbo.ArchitectureRuns`** sentence. |
| 2026-04-12 | (drop **`ArchitectureRuns`**) | Migration **`049_DropArchitectureRunsTable.sql`**; **`ArchLucid.sql`** — remove **`dbo.ArchitectureRuns`**; delete **`IArchitectureRunRepository`** + Dapper/in-memory impls + contract tests; remove dual-persistence health checks + options + API tests; remove CI **`assert_architecture_run_write_pragma`** step/scripts; **`CoordinatorService`** / **`DemoSeedService`** no legacy insert; **`ArchitectureCommitTestSeed`** → **`dbo.Runs`**; planning link repo validates **`dbo.Runs`**. Docs: **ADR 0012** completed, **ADR 0002** superseded, **`DATA_CONSISTENCY_MATRIX`**, **`ARCHITECTURE_COMPONENTS`**, **`DATA_MODEL`**, **`TRUSTED_BASELINE`**, **`API_CONTRACTS`**, **`SQL_DDL_DISCIPLINE`**, runbooks. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (analysis + commit integration fixes) | **`ArchitectureAnalysisService`**: load manifest when **`CurrentManifestVersion`** is empty using **`v1-{runId}`** (matches **`RunDetailQueryService`** / first-commit convention). **`ArchitectureRunCommitOrchestrator`**: **`ConflictException`** (409) when no agent results; run that check **before** evidence-package prerequisite so commit-before-execute is not masked by 400. Tests: **`ArchitectureAnalysisServiceCanonicalPreloadTests`** (null version + preloaded manifest). **`DATA_CONSISTENCY_MATRIX`**: row-reconciliation note for **`LegacyRunStatus`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (runs read path / ADR-0012) | Migration **`048_RunsLegacyReadAlignment.sql`** + **`ArchLucid.sql`** — **`dbo.Runs`** **`ArchitectureRequestId`**, **`LegacyRunStatus`**, **`CompletedUtc`**, **`CurrentManifestVersion`**; **`IRunRepository.ListRecentInScopeAsync`**; **`RunDetailQueryService`** / **`RunsController`** / orchestrators / **`AuthorityPipelineWorkProcessor`** read **`dbo.Runs`**; **`CoordinatorService`** patches authority header after coordination; **`ContextIngestionRequest.ArchitectureRequestId`**. Docs: **ADR 0012** read section, **`DATA_CONSISTENCY_MATRIX`**. Tests: run repo contract, **`RunRecordArchitectureRunMappingParityTests`**, updated service tests. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (dual persistence row reconciliation) | **`DualPersistenceRowReconciliationHealthCheck`** + **`DualPersistenceRowReconciliationOptions`** (`HealthChecks:DualPersistenceRowReconciliation`); **`DualPersistenceRowReconciliationRow`** probe DTO; Composition registers **`dual_persistence_row_reconciliation`** alongside count-based check. Docs: **`DATA_CONSISTENCY_MATRIX.md`**. Tests: **`DualPersistenceRowReconciliationHealthCheckTests`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-10 | Operator UI a11y baseline | **`archlucid-ui`:** `@axe-core/playwright` + `e2e/helpers/axe-helper.ts` + `e2e/accessibility.spec.ts` (Home, Runs, Audit, Policy packs, Alerts) — CI gates **critical/serious** axe violations via existing `ui-e2e-smoke`. Root **`ACCESSIBILITY.md`** (WCAG 2.1 AA target). Fixes: **`.workflow-inline-link`** underline + first-run footer text contrast; **Runs** pagination disabled labels **#64748b** + **`aria-disabled`**. **`policy-packs-mock-state`** `createMockPack` body types align with POST JSON. **Phase 7.5–7.8** unchanged (deferred). |
| 2026-04-10 | Phase 7 (docs + infra naming) | **Doc/infra sweep:** `docs/**` + project READMEs → ArchLucid naming; **`ArchLucid.sql`** references; Terraform **`archlucid_api_backend_url`** (replaces `archiforge_api_backend_url`); private endpoints **`pe-archlucid-*`**; sample **`terraform.tfvars.example`** defaults (APIM, Entra, edge, container-apps, SQL failover); **CI** rename-guard **echo** copy; **`codeql.yml`** UI path comment; **`ArchLucid.Worker`** **`launchSettings.json`** profile; **`.gitignore`** + **`git rm --cached`** **`_docker_publish_*`**, **`_docker_r2r_noflag`**, **`artifacts-bench/results/`**. **Unchanged:** migrations **001–043** file bodies, RLS **DB** identifiers, Terraform **resource addresses** (`azurerm_api_management.archiforge`, `grafana_folder.archiforge`, …), allowlist **filenames**. **Deferred:** 7.5–7.8. |
| 2026-04-10 | (authority pipeline OTel) | **`AuthorityPipelineStagesExecutor`**: stage activities parented to **`ctx.RunActivity`**; tags **`archlucid.run_id`**, **`archlucid.stage.name`**; histogram **`archlucid_authority_pipeline_stage_duration_ms`** (`stage`, `outcome`); error status + **`error.type`** on failure. **`ArchLucidInstrumentation`**. Tests: **`AuthorityPipelineStagesExecutorTests`** (`Suite=Core`). Docs: **`BACKGROUND_JOB_CORRELATION.md`** §10, **`ARCHITECTURE_FLOWS.md`**, **`OBSERVABILITY.md`**, **`TEST_STRUCTURE.md`**. **Verified:** `dotnet test` filter **`AuthorityPipelineStagesExecutorTests`** (4 passed). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-08 | Repo / release surface | **`docs/REPO_HYGIENE.md`** (committed vs generated, `artifacts/`, NSwag `Generated/` policy); root **README** + **START_HERE** + **PILOT_GUIDE** + **RELEASE_LOCAL** cross-links; **CHANGELOG** 56R/57R bullets aligned with **`archlucid`** CLI and **`mock-archlucid-api-server.ts`**; `.gitignore` root transcript patterns + NSwag path comment; **`git rm --cached`** root `*.txt` logs (files may remain locally). Phase **7.5–7.8** unchanged. |
| 2026-04-08 | Phase 7 (finish) | **Docs + tooling:** runbooks (SLO/Redis/provenance/migration/secret/load), **`docs/terraform-azure-variables.md`**, **`archlucid-ui`** README + operator docs, **`.devcontainer`**, **`api-synthetic-probe.yml`**. **`package-release.ps1`** + **`Write-ReleasePackageArtifacts.ps1`** → **ArchLucid.Api** / **`ArchLucid.*`** package kinds. Sample **`appsettings.*`** → **`api://archlucid-api`**, Key Vault secret name examples **`archlucid-*`**, `_docker_*` parity. **Terraform:** Phase **7.5** comment headers on module **`main.tf`** + **`apim.tf`**; human-facing **defaults** (`law-archlucid-ca`, `cae-archlucid`, budgets, VNet, Grafana, Entra **`api://archlucid-api`**). **`scripts/assert_context_ingestion_di_guards.py`** → **ArchLucid.Host.Composition**. C#/TS **`archiforge`** grep guards: **zero** product hits; allowlists comment-only. **Deferred:** checklist **7.5–7.8**. |
| 2026-04-09 | (hot-path cache / archival) | Reviewed **`IHotPathReadCache`** usage (**runs** `GetById`, **golden manifest** `GetById`, **policy pack** `GetById` only; lists/artifacts/alerts not cached). **`ArchiveRunsCreatedBeforeAsync`** now returns **`RunArchiveBatchResult`** with **`OUTPUT`** keys; **`CachingRunRepository`** evicts each archived run. Docs: **`DATA_CONSISTENCY_MATRIX.md`**, **`REDIS_AND_MULTI_REGION.md`**. Tests: **`CachingRunRepositoryArchiveInvalidationTests`**. Phase 7 unchanged. |
| 2026-04-08 | Phase 7 (code) | Removed config/auth/env/OIDC/CLI bridges; **`ArchLucid.sql`** + migrations **`044`**/**`045`**; Docker/compose **`archlucid-*`**; CLI **`archlucid`/`archlucid.json`**; integration events canonical-only; **`BREAKING_CHANGES.md`**; CI **`ARCHLUCID_SQL_TEST`**; C#/TS rename guards clean without broad allowlists. **Deferred:** Terraform **`state mv`** (7.5), repo/Entra/workspace renames (7.6–7.8). |
| 2026-04-08 | (list pagination) | **`GET …/authority/projects/{projectId}/runs`**: optional **`page`**/**`pageSize`** → **`PagedResponse&lt;RunSummaryResponse&gt;`**; legacy **`take`** + array unchanged. **`IRunRepository.ListByProjectPagedAsync`**, SQL **`OFFSET`/`FETCH`** + count. Operator UI **Runs** (paged + nav) and **Alerts** (**`listAlertsPaged`**, 25/page). Vitest **`coerceRunSummaryPaged`**. **`docs/API_CONTRACTS.md`**. Regenerate OpenAPI contract snapshot when CI requires. Phase 7 unchanged. |
| 2026-04-09 | (support bundle) | **`README.txt`** + **`manifest.json`** **1.1** (`triageReadOrder`, **`archlucidJsonPath`** / present); **`api-contract.json`** bounded **`GET /openapi/v1.json`**; **`GetBoundedUtf8BodyAsync`** on **`ArchLucidApiClient`**. **`SupportBundleTriageCatalog`**, **`SupportBundleReadme`**. Docs: **`TROUBLESHOOTING.md`**, **`CLI_USAGE.md`**, **`PILOT_GUIDE.md`**, **`CHANGELOG.md`**. Tests: **`SupportBundleTests`**, **`SupportBundleHealthProbePathsTests`**. Phase 7 unchanged. |
| 2026-04-09 | (correlation / error surfacing) | API **`application/problem+json`** includes **`correlationId`** (and **`InvalidModelStateResponseFactory`** validation errors); **`ApplicationProblemMapper`** + global exception handler use **`ProblemCorrelation.Attach`**. UI proxy **502/413/503/429** JSON + **`X-Correlation-ID`**; **`buildApiRequestErrorFromParts`** falls back to body **`correlationId`**. Docs: **`API_CONTRACTS.md`**, **`TROUBLESHOOTING.md`**, **`operator-shell.md`**; CLI support-bundle reference + **`CliOperatorHints`**. Tests: mapper/filter assertions; Vitest updates. Phase 7 unchanged. |
| 2026-04-08 | (replay / compare / export data) | **`GetRunDetailAsync`** loads **artifact bundles by golden manifest id** even when **`ArtifactBundleId`** on the run is null (Dapper + in-memory query services). **Run compare** adds a **GoldenManifestId** run-level diff when commits differ; **`GET …/authority/compare/manifests`** returns **409 Conflict** (problem details) when both manifests exist but stored scopes disagree. Tests: query service bundle hydration, compare runs asymmetry, **`AuthorityCompareController`** unit test. **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-09 | (diagram export quality) | **`IDiagramImageRenderer`** moved to **`ArchLucid.Core`** (`Core/Diagrams/`) to avoid **Persistence → ArtifactSynthesis → Application** cycle. **`DocxExportService`** injects it: Mermaid artifacts rasterize to PNG when renderer returns bytes; **`MermaidCliDiagramImageRenderer`** logs and returns **null** on CLI failure. **`ArchLucid:MermaidCli:Enabled`** selects **Mermaid CLI** vs **`NullDiagramImageRenderer`**. Analysis DOCX usings updated to **Core.Diagrams**. Tests: **`DocxExportServiceGoldenTests`** (+ mock PNG). **`appsettings.json`**, **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-09 | (exports / packages) | **`DocxExportService`**: architecture diagram section uses **PNG artifact** (base64), else **Mermaid** from synthesized diagram artifact, else explicit empty + manifest counts; removed **DiagramPlaceholderBytes**. **`WordDocumentBuilder.AddMonospaceSourceLines`**. **`ArtifactPackagingService.BuildRunExportPackage`** + **`RunExportReadmeContext`**; **`ArtifactExportController`** passes manifest name/hash/rule set into **README.txt**. Tests: **`DocxExportServiceGoldenTests`**, **`ArtifactPackagingServiceTests`**. **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-08 | (UI nav / V1 discoverability) | **`ShellNav`**: group label **Start here · runs & review**, **`New run`**, **Compare two runs** / **Replay a run**, nav **`aria-label`** **Primary operator workflows**. **Home**: **Typical V1 path** callout + quick links map to header groups. **`OperatorFirstRunWorkflowPanel`**: clearer primary labels; **Hide checklist** / **Show V1 workflow checklist**. **Onboarding** link text aligned. Tests: **`ShellNav`**, **`HomePage`**, **`OperatorFirstRunWorkflowPanel`**. **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-08 | (UI operator states) | **`archlucid-ui`**: **`OperatorTryNext`** + consistent **Try next:** copy on **runs** (malformed outside callout), **graph** (load/malformed/viewer/dynamic loading), **compare** (legacy/golden/AI fail + malformed), **replay**, **manifest** summary/artifacts; **alerts** loading vs empty vs failure; **planning** refresh uses **`OperatorLoadingNotice`** + failure/empty next steps. **`docs/operator-shell.md`** — empty/loading/error pattern. Vitest **`OperatorShellMessage.test.tsx`**. Phase 7 unchanged. |
| 2026-04-08 | (UI first-run) | **`archlucid-ui`**: **`OperatorFirstRunWorkflowPanel`** on Home; improved empty states (**runs** list, **run detail** pre-manifest, **replay**); **new run** callout; **onboarding** links Home; Vitest **`OperatorFirstRunWorkflowPanel.test.tsx`**; **`README.md`**. Phase 7 unchanged. |
| 2026-04-08 | (docs) | **`docs/DEPLOYMENT_RUNBOOK.md`** — practical failed-deploy / rollback for Container Apps + GitHub CD; link from **`DEPLOYMENT.md`**. Phase 7 unchanged. |
| 2026-04-08 | (CD post-deploy) | **`scripts/ci/cd-post-deploy-verify.sh`**: structured checks + logs + `::error::`; **`/health/ready`** must report **`Healthy`**; **`/version`** + OpenAPI; optional **`CD_POST_DEPLOY_*`** retries. **`cd.yml`** / **`cd-staging-on-merge.yml`**; test contract in **`HealthEndpointSecurityIntegrationTests`**. Docs: **`DEPLOYMENT_CD_PIPELINE.md`**, **`RELEASE_SMOKE.md`**, **`ARCHITECTURE_ON_A_PAGE.md`**. Phase 7 unchanged. |
| 2026-04-08 | (CD / docs) | **`.github/workflows/cd.yml`** + **`cd-staging-on-merge.yml`**: deploy **worker** Container App with same **`archlucid-api:<tag>`** image as API; optional rollback deactivates worker revision too; staging smoke aligned with manual CD (**`/health/ready`**, **`/openapi/v1.json`**). Docs: **`DEPLOYMENT_CD_PIPELINE.md`**, **`DEPLOYMENT.md`**, **`CONTAINERIZATION.md`**, **`DEPLOYMENT_TERRAFORM.md`**, **`RELEASE_SMOKE.md`**, **`ARCHITECTURE_ON_A_PAGE.md`**, **`infra/terraform-container-apps/README.md`**. Phase 7 unchanged. |
| 2026-04-08 | (docs / V1 scope) | **`docs/V1_DEFERRED.md`** — doc-sourced inventory of **V1.1+ / deferred / exploratory** items (59R planning bridge, **AUDIT_COVERAGE_MATRIX** known gaps, Phase 7 rename reference, **CONTAINERIZATION** org gaps, **NEXT_REFACTORINGS** as maintainer backlog). Cross-links from **`V1_SCOPE.md`**, **`ARCHITECTURE_INDEX.md`**. Phase 7 unchanged. |
| 2026-04-08 | (CI guard) | C# rename guard: allowlist **`RlsArchLucidScopeIntegrationTests.cs`** for Phase 7 **`rls.ArchiforgeTenantScope`** policy name in SQL/doc/class id; **`SqlConnectionHealthCheck`** primary ctor param **`archiForgeOptions` → `archLucidOptions`** (avoids case-insensitive `archiforge` hit). |
| 2026-04-08 | (OTel + metrics + CLI + UI env + CI guards) | **String literal cleanup:** OTel activity tags `archlucid.*`, metrics `archlucid_*` (`ArchLucidInstrumentation`, Prometheus/Grafana/Azure monitor TF, docs). CLI usage `archlucid`; exports/ProblemDetails/CloudEvents/queue defaults/webhook defaults/temp paths → ArchLucid. **Support bundle** JSON `hasArchlucidJson` / `archlucidAndDotnetEnvironment` + legacy read-only props; **CLI/API env** `ARCHLUCID_*` first with legacy fallbacks; **Backfill** `ARCHLUCID_SQL` bridge. **UI:** `legacy-arch-env.ts`, `fetchArchLucidJson` / `askArchLucid`, `ArchLucidRole`, mock server rename, proxy/config/auth env bridges, `SupportBundleRedactor` includes `ARCHLUCID_*` keys. **CI:** case-insensitive C# grep + TS/TSX guard + `archiforge-rename-allowlist-ts.txt`. **Contracts:** `archlucid-asyncapi-2.6.yaml`, schema descriptions. Phase 7 config/OIDC/SQL filenames unchanged. |
| 2026-04-08 | (schema) | **`043_ArtifactBundles_LegacyJsonNullable.sql`** + **`ArchiForge.sql`**: `ArtifactsJson` / `TraceJson` on **`dbo.ArtifactBundles`** nullable (dual-write / relational-primary rows). **`PersistenceContractSupplement.sql`**: align **`TraceJson`** with existing **`ArtifactsJson`** nullability for integration tests. Phase 7 unchanged. |
| 2026-04-08 | (LLM resilience) | **Per-call Polly retry inside CB decorators** (`LlmCallResilienceDefaults`, `AgentExecution:Resilience` LLM keys); OTel `archlucid_llm_call_retries_total`; Simmy tests + `LlmCallChaosEndToEndTests`; scheduled **`.github/workflows/simmy-chaos-scheduled.yml`**; **`docs/LLM_RETRY_AND_CIRCUIT_BREAKER.md`**. Docker sample `appsettings` Resilience keys aligned. Phase 7 unchanged. |
| 2026-04-08 | (auditability) | **`docs/AUDIT_COVERAGE_MATRIX.md`** + CI guard; **`IAuditRepository.GetFilteredAsync`** + `GET /v1/audit/search` + `GET /v1/audit/event-types`; **`CircuitBreakerAuditEntry`** / optional **`CircuitBreakerGate`** callback + **`CircuitBreakerAuditBridge`** DI; operator UI **`/audit`** + **`ShellNav`**. OpenAPI snapshot regen. Phase 7 unchanged. |
| 2026-04-08 | (explainability) | **`ExplainabilityTraceCompletenessAnalyzer`** + advisory `ResultJson.traceCompleteness` + OTel histogram `archlucid_explainability_trace_completeness_ratio`; engine trace fills (policy/requirement/topology coverage, rules/notes on baseline/cost/requirement/compliance). UI: `ProvenanceGraphDiagram` on coordinator provenance page. **`docs/EXPLAINABILITY_TRACE_COVERAGE.md`**. Phase 7 unchanged. |
| 2026-04-08 | (integration events) | **Single publish entry point:** `AuthorityRunOrchestrator` uses `OutboxAwareIntegrationEventPublishing.TryPublishOrEnqueueAsync` for `com.archlucid.authority.run.completed` (transactional enqueue when enabled + `SupportsExternalTransaction`). CI grep + `DependencyConstraintTests` guard direct `IIntegrationEventPublisher.PublishAsync` except `IntegrationEventPublishing` / `IntegrationEventOutboxProcessor`. **JSON Schema** catalog under `schemas/integration-events/`; `IntegrationEventPayloadContractTests` assert payloads against committed schemas. Docs: `INTEGRATION_EVENTS_AND_WEBHOOKS.md`. Phase 7 unchanged. |
| 2026-04-08 | Phase 6.4 cleanup | **String literal rename sweep (~60 replacements):** API/Swagger/Worker startup messages, agent prompts, export/report headings, CLI operator output, decisioning manifest labels, artifact packaging, XML doc comments, validation + Swagger auth description text (new key first, legacy `ArchiForge*` named where still valid). Tests + temp prefixes aligned. **CI:** `Guard — unexpected ArchiForge string literals in C#` after `dotnet build` in `dotnet-fast-core`; allowlist = Phase 7 bridges, `ArchiForge.sql`, connection string / config key literals, test factories. Remaining `ArchiForge` in `.cs` = Phase 7 / compatibility only. |
| 2026-04-07 | (resilience) | **External resilience + CB metrics:** named `CircuitBreakerOptions` (`AzureOpenAI:CircuitBreaker:Completion` / `:Embedding` + shared fallback); `IOptionsFactory` snapshot at keyed `CircuitBreakerGate` construction; OTel counters `archiforge_circuit_breaker_*` on the gate; `Persistence:SqlOpenResilience` for SQL open retries; CLI `httpResilience` in `archiforge.json`. **`docs/RESILIENCE_CONFIGURATION.md`**. Tests: `CircuitBreakerGateMetricsTests`, `SqlOpenResilienceOptionsTests`, CLI handler/options. **`IntegrationEventOutboxProcessorCorrelationTests`** filter activities by `archiforge.outbox_id` so parallel `Suite=Core` runs do not flake. Phase 7 deferred. |
| 2026-04-08 | (CI) | **CodeQL JavaScript job:** `source-root` and npm paths use `ARCHLUCID_UI_DIR=archlucid-ui` (post–Phase 3 rename); verify step fails fast if the folder is missing. |
| 2026-04-08 | (integration events) | **Transactional outbox for all 6 event types** via `OutboxAwareIntegrationEventPublishing`; `IIntegrationEventOutboxRepository.EnqueueAsync` takes `Guid? runId`. **Canonical** `com.archlucid.*` type strings + legacy map (`IntegrationEventTypes`). **Worker** `AzureServiceBusIntegrationEventConsumer` + `LoggingIntegrationEventHandler`; `IntegrationEventsOptions` consumer settings. **Terraform** `infra/terraform-servicebus/`. Docs: `INTEGRATION_EVENTS_AND_WEBHOOKS.md`, AsyncAPI. Tests: outbox-aware, contract payloads, governance outbox path, `LoggingIntegrationEventHandler`. |
| 2026-04-07 | (observability) | **Background job correlation:** new `ActivitySource`s `RetrievalIndexingOutbox`, `IntegrationEventOutbox`, `DataArchival` + OTel registration; `RetrievalIndexingOutboxProcessor`, `IntegrationEventOutboxProcessor`, `DataArchivalCoordinator` set `correlation.id` + Serilog `CorrelationId`; `AdvisoryScanRunner` / `AuthorityRunOrchestrator` push `LogContext`. **`docs/BACKGROUND_JOB_CORRELATION.md`**. Persistence tests: `*CorrelationTests` (`Suite=Core`). Serilog 3.1.1 package for `LogContext` on coordination/integration/runtime/advisory persistence projects. |
| 2026-04-07 | (architecture tests) | **`ArchLucid.Architecture.Tests`**: NetArchTest.Rules 1.3.2, 15× `[Fact]` + `Suite=Core` in `DependencyConstraintTests`; Tier 2/CLI API boundary via `GetReferencedAssemblies()` where namespace prefix would false-positive (`ArchLucid.Api.Client`). **`docs/ARCHITECTURE_CONSTRAINTS.md`** + index link. Project added to **`ArchLucid.sln`**. Phase 7 unchanged (await explicit go-ahead). |
| 2026-04-07 | (bugfix) | **`FindingsJson` read path:** `SqlFindingsSnapshotRepository` calls `FindingPayloadJsonCodec.HydrateJsonElementPayloads` after `FindingsSnapshotMigrator.Apply` so JSON-deserialized findings get typed `Payload` (matches relational read). Tests: `FindingPayloadJsonCodecTests` + `SqlFindingsSnapshotRepositorySqlIntegrationTests.GetById_when_no_FindingRecords_falls_back_to_FindingsJson`. |
| 2026-04-07 | 2.3 | ArchLucid product config section merge + authority resolver uses `ResolveArchLucidOptions` |
| 2026-04-07 | 2.4–2.6, 3.x, 4.x, 5.11 | Appsettings ArchLucid keys + `IOptions`/`ResolveArchLucidOptions` legacy-base fix; OIDC storage bridge; **`archlucid-ui`**; CI images **`archlucid-api`**/**`archlucid-ui`**; Prometheus/Grafana **`archlucid-*`** files; template **`archlucid-finding-engine`**; Terraform display strings (Entra/APIM/monitoring). |
| 2026-04-04 | 1.1 | layout.tsx title + heading → ArchLucid |
| 2026-04-05 | 1.2 | archlucid-ui/README.md product-facing name → ArchLucid (env keys unchanged) |
| 2026-04-05 | 1.4 | GLOSSARY.md title + ArchLucid note; outbox / integration dead-letter terms |
| 2026-04-05 | 2.1–2.2, 2.5 | Config bridges + `ResolveArchiForgeOptions` at early DI sites; UI proxy API key fallback; ops docs (architecture on a page, code map, config sunset, capacity/cost) |
| 2026-04-05 | 1.5 | Architecture context/components/containers titles + ArchLucid product framing; production cost guardrails (`production.tfvars.example`, LA quota note, WAF version var, FinOps playbook §9) |
| 2026-04-05 | (Improvement 5) | Merged `ArchiForge.DecisionEngine` into `ArchLucid.Decisioning` (later flattened to `ArchLucid.Decisioning.Merge` / `ArchLucid.Decisioning.Validation` — see 2026-04-06); added `ArchLucid.Persistence.Runtime` (orchestration, UoW, hot-path cache impls, blob store impls, archival); coordinator repos renamed to `ICoordinatorGoldenManifestRepository` / `ICoordinatorDecisionTraceRepository` in `Persistence.Data.Repositories`. |
| 2026-04-06 | Namespace flatten | Removed nested `Decisioning.DecisionEngine.*` namespaces: code under `ArchLucid.Decisioning/Merge/` and `ArchLucid.Decisioning/Validation/`; tests under `ArchLucid.Decisioning.Tests/Merge/` and `Validation/`; OTel meter `ArchLucid.Decisioning.SchemaValidation` (replaces `ArchiForge.DecisionEngine.SchemaValidation`). |
| 2026-04-06 | Quality batch (six improvements) | Stryker `break:60` (all `stryker-config*.json`); merged line-coverage gate documented in `docs/coverage-exclusions.md` (not Coverlet `<Threshold>` per assembly — see comment in `coverage.runsettings`); CLI `ResolveApiErrorMessage` for typed ProblemDetails; rename `DecisionTrace` → `RunEventTrace` (coordinator) / `RuleAuditTrace` (authority); `GET /v1/authority/runs/{id}/provenance`; UI `/runs/[id]/provenance`; `docs/DUAL_PIPELINE_NAVIGATOR.md`, `docs/CHAOS_TESTING.md`, `CliRetryDelegatingHandlerTests`. Prometheus SLO/outbox alerts already in `infra/prometheus/`. Phase 5–6 bulk project/namespace rename **not** started (explicit scope). |
| 2026-04-06 | 1.7 + CI coverage gate | Phase **1.7** pilot/release docs → ArchLucid product naming (`PILOT_GUIDE`, `OPERATOR_QUICKSTART`, `RELEASE_SMOKE`, `RELEASE_LOCAL`); CI **70%** merged line gate via `scripts/ci/assert_merged_line_coverage_min.py`; `RetrievalIndexingOutboxProcessor` uses `RunDetailDto.DecisionTrace` (fixes stale `RuleAuditTrace` property). |
| 2026-04-06 | **5.1** | All main-solution leaf test projects renamed to **`ArchLucid.*.Tests`** (dirs + csproj); solution + Stryker + Dockerfile + `nswag.json` paths updated; `OpenApiContractSnapshotTests` resolves `ArchLucid.Api.Tests.csproj`; docs: `CI_MIGRATION_CHECKLIST`, `ArchLucid.Api.Client/README`, `Migrations/README`. |
| 2026-04-06 | 1.10 (partial) + cognitive-load | Expanded `docs/DUAL_PIPELINE_NAVIGATOR.md` (side-by-side flow, shared-artifact matrix, coordinator HTTP→commit walkthrough); introduced sealed `RunEventTrace` / `RuleAuditTrace` with abstract `DecisionTrace` + `DecisionTraceJsonConverter` (stable OpenAPI shape); `docs/ONBOARDING_HAPPY_PATH.md` link + corrected `POST /v1/architecture/request`; `docs/ARCHITECTURE_INDEX.md` navigator blurb; `ArchLucid.Contracts.Tests/DecisionTraceJsonRoundTripTests.cs`. |
| 2026-04-06 | **5.2–5.10, 5.12–5.13 + 6.x** | **Phases 5–6:** `git mv` all product projects `ArchiForge.*` → `ArchLucid.*`; **`ArchLucid.sln`**; identifier pass (`AddArchLucid*`, `IArchLucidUnitOfWork`, `ArchLucidConfigurationBridge`, `ResolveSqlConnectionString` ArchLucid→ArchiForge fallback); NSwag **`ArchLucidApiClient`**; **`Directory.Build.props`** `ArchLucidApiClientPackageVersion` + MSBuild bridge; renamed `ArchiForge*.cs` source files; **`X-ArchLucid-*`** headers; CORS policy **`ArchLucid`**; scripts `phase6-rename-identifiers.ps1` / `bulk-replace-archlucid-phase6.ps1`. **Deferred:** template `archlucid-finding-engine` (5.11). |
| 2026-04-06 | (decomposition) | **`ArchitectureRunService`** → thin facade over **`IArchitectureRunCreateOrchestrator`** / **`Execute`** / **`Commit`** (`ArchLucid.Application/Runs/Orchestration/`). **`DecisionEngineService`** delegates to **`DecisionMergeInputGate`**, **`AgentProposalManifestMerger`**, **`DecisionNodeManifestMerger`**, **`ManifestGovernanceMerger`**, **`DecisionMergeTraceRecorder`**, **`GoldenManifestFactory`**, **`DecisionTraceManifestAttachment`**; DI registers merge strategies in **`ServiceCollectionExtensions.CoordinatorAndArtifacts`**. Single-arg **`DecisionEngineService(ISchemaValidationService)`** retained for tests. |
| 2026-04-06 | 1.10 (slice) | **`docs/API_SLOS.md`** — SLO table (99.5% / 5xx budget, p95 guardrail, synthetic canary); **`.github/workflows/api-synthetic-probe.yml`** — scheduled external `GET /health/live` + `GET /version`; **`docs/runbooks/SLO_PROMETHEUS_GRAFANA.md`** §10 + component table row. |
| 2026-04-06 | 1.10 (slice) | **`ILlmProvider`** + **`LlmProviderDescriptor`** / **`LlmProviderAuthScheme`** in `ArchLucid.AgentRuntime`; **`IAgentCompletionClient`** inherits `ILlmProvider`; Azure/echo/fake/wrappers expose `Descriptor`; **`DelegatingLlmCompletionProvider`** overlays telemetry labels; **`ILlmProvider`** registered in host composition. |
| 2026-04-06 | 1.10 (slice) | **Prompt reproducibility:** `Prompts/*SystemPromptTemplate`, **`IAgentSystemPromptCatalog`** / **`CachedAgentSystemPromptCatalog`**, **`AgentPromptReproMetadata`**, trace fields + **`IAgentExecutionTraceRecorder`**; **`AgentPromptActivityTags`**; Simulator traces; NSwag client regen. |
| 2026-04-06 | 1.10 (slice) | **`docs/CONTAINERIZATION.md`** — table + notes: RID-scoped `dotnet restore` matches Alpine `publish -r linux-musl-x64 --no-restore`; aligns with `ArchLucid.Api/Dockerfile` (avoids NETSDK1047 in CI/buildx). |
| 2026-04-06 | Phase 4 slice (4.4 partial) | **`infra/terraform-container-apps/main.tf`** worker `command` → **`ArchLucid.Worker.dll`**; locals comment; **`variables.tf`**, **`terraform.tfvars.example`**, **`README.md`**; **`infra/terraform-entra/variables.tf`** + **`infra/terraform/variables.tf`** descriptions (ArchLucid.Api / API backend). **`terraform fmt`** on `terraform-container-apps`. |
| 2026-04-07 | **1.10 + 1.11** | **Docs + UI docs rename sweep:** `docs/**/*.md` product/DI/telemetry names aligned to **ArchLucid** (`DI_REGISTRATION_MAP` → `Host.Composition`, `AddArchLucid*`, `X-ArchLucid-*`, `SYSTEM_MAP`, ADRs 0010–0011, SQL/runbook prose); **kept** `ArchiForge.sql`, `ArchiForge:*` / `ArchiForgeAuth` / legacy connection string keys / Prometheus alert names / `archiforge_*` metric prefixes where operational. **`archlucid-ui/docs/*.md`** titles + diagrams + examples; **`archlucid-ui/src/app/global-error.tsx`** heading → **ArchLucid**. |
| 2026-04-07 | **2.4–2.6, 3.x, 4.x, 5.11** | **Appsettings** → `ArchLucid` / `ArchLucidAuth` / `ConnectionStrings:ArchLucid` (+ `ResolveArchLucidOptions` legacy-`ArchiForge` base + `IOptions` bridge). **OIDC** sessionStorage `archlucid_oidc_*` + legacy read/migrate. **`archlucid-ui`** dir + CI/CD image names **`archlucid-api`** / **`archlucid-ui`**. **Prometheus/Grafana** files `archlucid-*.yml` / `dashboard-archlucid-*.json`. **Template** `templates/archlucid-finding-engine`. Terraform **human-facing** strings (Entra consent, APIM display, monitoring descriptions); many **resource addresses** / defaults still `archiforge*` until Phase 7 `state mv`. |
| 2026-04-13 | (quality improvement 1) | **Pre-commit gate tests:** `ArchitectureRunServiceExecuteCommitTests` (block/allow/skip gate), `ArchitectureRunCommitPipelineIntegrationTests` (real gate + audit + no manifest on block), **`PreCommitGovernanceGateTests`** edge cases. **Stryker:** baselines **62→65**, workflow tolerance **0.15→0.10** pp; **`MUTATION_TESTING_STRYKER.md`** ratchet section; **`TEST_STRUCTURE`** / **`TEST_EXECUTION_MODEL`** Tier **4c**; **`CHANGELOG`**. |
| 2026-04-13 | (structured output evaluation harness) | **`AgentResultParserTests`** + **`AgentCompletionModelMetadataTests`**; handler + **`RealRuntimeMixedModeTests`** wired to **`IAuditService`** / **`IScopeContextProvider`**; **`ExplanationServiceRunTests`** / **`ExplanationServiceComparisonTests`** pass **`ISchemaValidationService`**; run explanation test for invalid **`schemaVersion:1`** + real **`SchemaValidationService`**. **`ExplanationService`**: `StartsWith("{", …)` (compile on current `string` API). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (live E2E operator happy path) | **`e2e/helpers/live-api-client.ts`**; **`live-api-journey.spec.ts`**: create → execute → poll **ReadyForCommit** → commit → manifest UI → **`GET /v1/artifacts/runs/{runId}/export`** → governance submit/approve (**peer reviewer** ≠ dev submitter) → audit type assertions (**`RunExported`** included) → **`/governance`** **Load** → **`/audit`**. Docs: **`LIVE_E2E_HAPPY_PATH.md`**, **`TEST_STRUCTURE`**, **`TEST_EXECUTION_MODEL`**, **`V1_READINESS_SUMMARY`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (k6 smoke vs rate limit) | **`tests/load/smoke.js`**: runs → **`GET /v1/architecture/runs`**; audit → **`take=50`**. **`.github/workflows/ci.yml`** **`k6-smoke-api`**: **`RateLimiting__FixedWindow__PermitLimit=200000`** for API process (default **100/min** caused mass **429** on **`[EnableRateLimiting("fixed")]`** endpoints). **`PERFORMANCE_TESTING.md`** — local rate-limit note. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (quality improvement 1 — gates + coverage advisory) | **`ui-e2e-live`** + **`k6-smoke-api`**: **`continue-on-error` removed** (merge-blocking). **`assert_merged_line_coverage_min.py`**: **`--warn-below-package-line-pct 70`**, **`--annotations-file`**; **`coverage-metrics`** artifact is a folder (**`coverage-metrics.txt`** + **`coverage-annotations-assert.txt`**); **`coverage-pr-comment`** appends assert warnings. **`Suite=Core`** on learning/product-learning/evolution/ask integration tests + **`EvolutionSimulationReportBuilderTests`**; extra **`Learning`/`ProductLearning`/`Evolution`** API tests. Live E2E: **5xx retry** on run poll, **`expect.soft`** governance negatives, **`GET /v1/architecture/runs`**. Docs: **`TEST_EXECUTION_MODEL`**, **`V1_READINESS_SUMMARY`**, **`LIVE_E2E_HAPPY_PATH`**, **`TEST_STRUCTURE`**, **`PERFORMANCE_TESTING`**, **`coverage-exclusions.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (live E2E — error/edge paths 1A–1C) | **`e2e/live-api-conflict-journey.spec.ts`** (409 double-commit + audit **`ManifestGenerated`** stability, 404 commit); **`e2e/live-api-governance-rejection.spec.ts`** (reject + audit + UI); **`e2e/live-api-error-states.spec.ts`** (fake run detail, runs list, audit no-results, governance dashboard). **`live-api-client.ts`**: **`commitRunRaw`**, **`waitForReadyForCommit`**, **`rejectGovernanceRequest`**, **`postGovernanceRejectRaw`**. **`playwright.live.config.ts`** **`testMatch`** array; **`playwright.config.ts`** **`testIgnore: "**/live-api-*.spec.ts"`**. Docs: **`LIVE_E2E_HAPPY_PATH.md`**, **`TEST_STRUCTURE.md`**, **`TEST_EXECUTION_MODEL.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (traceability improvement 1 — audit indexes + runbook) | Migration **`055_AuditEvents_CorrelationId_RunId_Indexes.sql`** + **`ArchLucid.sql`** filtered indexes; **`AUDIT_COVERAGE_MATRIX`** §Indexes; **`AuditServiceCorrelationEnrichmentTests`**; **`AuditControllerSearchTests`** (`correlationId` / `runId` query); **`docs/runbooks/TRACE_A_RUN.md`** + runbooks index; **`infra/grafana/dashboard-archlucid-run-lifecycle.json`**; **`OBSERVABILITY`**, **`SLO_PROMETHEUS_GRAFANA`**, **`LIVE_E2E_HAPPY_PATH`**; live E2E **`correlationId`** assertions + **`searchAudit`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (quality improvement 2 — trace blobs + structural eval) | **`PersistFullPrompts`** default **true**; **`appsettings`** **`AgentExecution:TraceStorage`**; **`IAgentOutputEvaluator`** / **`AgentOutputEvaluator`** / **`AgentOutputEvaluationRecorder`**; OTEL **`archlucid_agent_output_structural_completeness_ratio`** + **`archlucid_agent_output_parse_failures_total`**; **`GET /v1/architecture/run/{runId}/agent-evaluation`**; contracts **`AgentOutputEvaluationScore`** / **`AgentOutputEvaluationSummary`**; tests **`AgentOutputEvaluatorTests`**; docs **`AGENT_TRACE_FORENSICS`**, **`OBSERVABILITY`**, **`AGENT_OUTPUT_EVALUATION`**, **`ARCHITECTURE_INDEX`**. OpenAPI snapshot regen when CI requires. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (Compose full-stack + k6 baseline) | **`docker-compose.yml`**: **`sqlserver-init`** one-shot creates **`ArchLucid`** DB before **api**; **api** env **`RateLimiting__*`** high permit limits for k6 (parity with **`ci.yml`** k6 smoke). **`record_baseline.ps1`**: **`try`/`finally`** teardown; **`K6_SUMMARY_TREND_STATS`** quoted for PowerShell; bind-mount path uses forward slashes. **`record_baseline.sh`**: **`trap`** teardown; quoted **`K6_SUMMARY_TREND_STATS`**. **`print_k6_summary_metrics.py`**: read k6 summary metrics when trend stats are on the metric object (not only **`values`**). **`LOAD_TEST_BASELINE.md`** **Initial** row + **`hotpaths.js`** **`p(95)<2000`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (live E2E — full `live-api-*.spec.ts` in CI) | **`playwright.live.config.ts`**: **`testMatch: ["live-api-*.spec.ts"]`** (all 15 live files). **`live-api-client.ts`**: advisory/replay/analysis/DOCX/alert-rules/graph/ask + digest subscription helpers. New specs: **`live-api-digest-webhook.spec.ts`**, **`live-api-concurrency.spec.ts`**, **`live-api-archival.spec.ts`**. **`.github/workflows/ci.yml`** **`ui-e2e-live`**: **`timeout-minutes: 35`**. Docs: **`LIVE_E2E_HAPPY_PATH.md`**, **`V1_READINESS_SUMMARY.md`**, **`TEST_EXECUTION_MODEL.md`**, **`QUALITY_ASSESSMENT_2026_04_14.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (quality improvement 1 — forensic prompts + reference eval) | Removed **`PersistFullPrompts`**; **`PatchInlinePromptFallbackAsync`** + SQL **`062`** inline columns; **`AgentOutputReferenceCaseRunEvaluator`** + **`AgentExecution:ReferenceEvaluation`** + OTEL **`archlucid_agent_output_reference_case_*`**; **`IAgentOutputEvaluationResultRepository`** / **`063`**; docs **`AGENT_TRACE_FORENSICS`**, **`OBSERVABILITY`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (quality improvement 2 — API semantic + contract alignment) | **`AgentOutputSemanticScore`** moved to **`Contracts`**; **`GET …/agent-evaluation`** returns nested **`Semantic`**, **`blobUploadFailed`**, **`averageSemanticScore`**; **`AgentOutputEvaluationRecorder`** semantic log threshold **0.3**; **`AgentOutputEvaluationRecorderTests`**; OpenAPI snapshot + **`ArchLucid.Api.Client`** regen. Blob retries / **`BlobUploadFailed`** / semantic evaluator already landed earlier. Phase **7.5–7.8** unchanged (deferred). |
