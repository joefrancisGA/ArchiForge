# ArchiForge → ArchLucid Rename Checklist

Incremental rename plan. One batch is folded into each working session.
See `.cursor/rules/ArchLucid-Rename.mdc` for the standing instruction.

> **Rule**: keep the build green after every batch. Never break CI.

---

## Phase 1 — Display text and documentation (low risk, any session)

- [x] 1.1 Update product name in `archlucid-ui/src/app/layout.tsx` (title, heading)
- [x] 1.2 Update `archlucid-ui/README.md` — product name references
- [x] 1.3 Update root `README.md` — product name, badges, description (2026-04-04)
- [x] 1.4 Update `docs/GLOSSARY.md` (2026-04-05)
- [x] 1.5 Update `docs/ARCHITECTURE_CONTEXT.md`, `docs/ARCHITECTURE_COMPONENTS.md`, `docs/ARCHITECTURE_CONTAINERS.md` (2026-04-05)
- [x] 1.6 Update `docs/DEPLOYMENT.md`, `docs/DEPLOYMENT_TERRAFORM.md`, `docs/CONTAINERIZATION.md` (2026-04-06 — ArchLucid product naming pass where referenced)
- [x] 1.7 Update `docs/PILOT_GUIDE.md`, `docs/OPERATOR_QUICKSTART.md`, `docs/RELEASE_SMOKE.md`, `docs/RELEASE_LOCAL.md` (2026-04-06 — ArchLucid product naming in pilot/release docs; literal `ArchiForge.*` paths and config keys unchanged)
- [x] 1.8 Update onboarding docs: `docs/onboarding/day-one-developer.md`, `day-one-sre.md`, `day-one-security.md`, `docs/CONTRIBUTOR_ONBOARDING.md`, `docs/ONBOARDING_HAPPY_PATH.md` (2026-04-06 — ArchLucid product naming in prose; repo folder, `ArchiForge.*` assemblies, and config keys unchanged)
- [x] 1.9 Update `docs/BUILD.md`, `docs/FORMATTING.md`, `docs/TEST_STRUCTURE.md`, `docs/TEST_EXECUTION_MODEL.md` (2026-04-06 — ArchLucid product naming note / prose; `ArchiForge.*` paths unchanged)
- [x] 1.10 Update remaining docs: ADRs, runbooks, CLI docs, changelogs, and any other `.md` files with stale references (batch as needed) (2026-04-07 — product naming + API/DI/header alignment; Phase 7 literals retained)
  - [x] API SLO doc + synthetic probe runbook link (`docs/API_SLOS.md`, `SLO_PROMETHEUS_GRAFANA.md` §10, `.github/workflows/api-synthetic-probe.yml`) (2026-04-06)
  - [x] AgentRuntime multi-vendor LLM seam: `ILlmProvider`, `LlmProviderDescriptor`, `LlmProviderAuthScheme`; `IAgentCompletionClient` : `ILlmProvider`; DI `ILlmProvider` → `ILlmCompletionProvider` (2026-04-06)
  - [x] Agent prompt versioning: catalog templates + SHA-256, `AgentExecutionTrace` repro fields, `AgentPrompts` release labels, OTel tags (2026-04-06)
  - [x] `docs/CONTAINERIZATION.md` — Docker `restore` / `publish` stages must use `-r linux-musl-x64` with `--no-restore` publish (NETSDK1047) (2026-04-06)
- [x] 1.11 Update `archlucid-ui/docs/*.md` (ARCHITECTURE, COMPONENT_REFERENCE, TESTING_AND_TROUBLESHOOTING, OPERATOR_SHELL_TUTORIAL, etc.) (2026-04-07)
- [x] 1.12 Update `.cursor/rules/Navigation.mdc` and `CSharp-EmbeddedStatements-NoBraces.mdc` (2026-04-06 — Navigation: ArchLucid product naming + `ArchLucidHostingRole.cs` path; CSharp rule: no product literals, verified)
- [x] 1.13 Contributor navigation + API bulk docs — **`docs/FIRST_5_DOCS.md`**, **`docs/CONTROLLER_AREA_MAP.md`**, **`START_HERE.md`** / **`CURSOR_PROMPTS_CANONICAL.md`** cross-links, **`API_CONTRACTS.md`** bulk endpoints, **`adr/0006`** v2/deprecation policy (2026-04-15)

## Phase 2 — Configuration key bridges (medium risk, any session)

- [x] 2.1 Add fallback readers for `ArchiForgeAuth` → `ArchLucidAuth` in auth options binding (2026-04-05)
- [x] 2.2 Add fallback readers for `ArchiForge:StorageProvider` → `ArchLucid:StorageProvider` (2026-04-05)
- [x] 2.3 Add fallback readers for any other `ArchiForge*` config sections (grep `appsettings*.json`) (2026-04-07 — `ResolveArchLucidOptions` loads legacy `ArchiForge` section then overlays `ArchLucid`; `FeatureManagementAuthorityPipelineModeResolver` uses bridge for effective storage mode)
- [x] 2.4 Update `appsettings*.json` files to use new key names — **2026-04-07** (`ArchLucid`, `ArchLucidAuth`, `ConnectionStrings:ArchLucid`, Webhook CloudEvents paths, consulting template strings; bridge still reads legacy `ArchiForge` keys)
- [x] 2.5 Update `.env.example` — `ARCHLUCID_API_KEY` preferred + `ARCHIFORGE_API_KEY` fallback in proxy (2026-04-05)
- [x] 2.6 Add OIDC storage key bridge reads (`archiforge_oidc_*` → `archlucid_oidc_*`) in `session.ts` and `storage-keys.ts` — **2026-04-07**

## Phase 3 — UI directory and package rename (medium risk, any session)

- [x] 3.1 Rename `archiforge-ui/` directory → `archlucid-ui/` — **2026-04-07**
- [x] 3.2 Update `package.json` name field — **2026-04-07**
- [x] 3.3 Update Dockerfile path references (API Dockerfile, docker-compose, CI workflows) — **2026-04-07**
- [x] 3.4 Update `archlucid-ui` references in root `README.md`, docs, and CI — **2026-04-07**
- [x] 3.5 Update TypeScript imports, API route env var names, and correlation header names — **2026-04-07** (`ARCHIFORGE_API_BASE_URL` retained with proxy bridge)

## Phase 4 — Infrastructure rename (medium-high risk, one stack per session)

- [x] 4.1 `infra/terraform-monitoring/` — **2026-04-07** (descriptions + Grafana folder title **ArchLucid**; Terraform resource addresses e.g. `grafana_folder.archiforge` unchanged for state)
- [x] 4.2 `infra/terraform-storage/` — **2026-04-07** (no product literals requiring change)
- [x] 4.3 `infra/terraform-edge/` — **2026-04-07** (no product literals requiring change)
- [x] 4.4 `infra/terraform-container-apps/` — **2026-04-07**
  - [x] Worker `command` → `ArchLucid.Worker.dll` (2026-04-06). Defaults **`archlucid-api`**, **`archlucid-ui`**, **`archlucid-worker`**; template container names updated. **2026-04-08:** Terraform **variable defaults** for LAW/CAE/budget/VNet/Grafana prefixes → **`archlucid-*`** (greenfield); **resource addresses** (e.g. `azurerm_api_management.archiforge`) still **`archiforge`** until **7.5** `state mv`.
- [x] 4.5 `infra/terraform-sql-failover/` — **2026-04-07** (defaults unchanged for state)
- [x] 4.6 `infra/terraform-openai/` — **2026-04-07**
- [x] 4.7 `infra/terraform-entra/` — **2026-04-07** (consent strings + default display **ArchLucid API**)
- [x] 4.8 `infra/terraform/` — **2026-04-07** (APIM display **ArchLucid API**, publisher default **ArchLucid**)
- [x] 4.9 `infra/terraform-private/` — **2026-04-07** (PE resource names unchanged for state)
- [x] 4.10 Prometheus — **`archlucid-alerts.yml`**, **`archlucid-slo-rules.yml`** — **2026-04-07**; **2026-04-08** metric + recording series renames to `archlucid_*` / `archlucid:slo:*` (OTel parity)
- [x] 4.11 Grafana — **`dashboard-archlucid-*.json`** — **2026-04-07**
- [x] 4.12 CI/CD workflows — **`archlucid-api`**, **`archlucid-ui`** image tags — **2026-04-07**
- [x] 4.13 `docker-compose.yml` — **2026-04-07** (`ConnectionStrings__ArchLucid`, `ArchLucid__*`, `archlucid-api` container)
- [x] 4.14 `stryker-config.json`, `coverage.runsettings`, `.devcontainer/devcontainer.json` — **2026-04-07** (verified: no `archiforge` literals)

## Phase 5 — .NET project directory and file renames (high risk — ASK USER FIRST)

> **Do not start this phase without explicit user confirmation.**
> Strategy: rename directories and `.csproj` files but preserve `<RootNamespace>ArchiForge.*</RootNamespace>`
> so no C# source changes are needed yet. Update `.sln` and `<ProjectReference>` paths.

- [x] 5.1 Rename leaf test projects: `*.Tests` directories and `.csproj` files (2026-04-06 — folders + `ArchLucid.*.Tests.csproj`; `AssemblyName`/`RootNamespace` kept `ArchiForge.*.Tests` until Phase 6)
- [x] 5.2 Rename `ArchiForge.TestSupport` → `ArchLucid.TestSupport`, `ArchiForge.Benchmarks` → `ArchLucid.Benchmarks` (2026-04-06)
- [x] 5.3 Rename `ArchiForge.Cli`, `ArchLucid.Cli.Tests`, `ArchiForge.Backfill.Cli` → `ArchLucid.*` (2026-04-06)
- [x] 5.4 Rename `ArchiForge.Api.Client`, `ArchLucid.Api.Client.Tests` → `ArchLucid.Api.Client*` dirs (2026-04-06)
- [x] 5.5 Rename domain projects: `Core`, `Contracts`, `Persistence`, `Application`, `AgentRuntime`, `AgentSimulator` (2026-04-06)
- [x] 5.6 Rename domain projects: `Coordinator`, `Decisioning`, `KnowledgeGraph`, `Retrieval`, `ContextIngestion`, `Provenance`, `ArtifactSynthesis` (2026-04-06)
- [x] 5.7 Rename host projects: `Host.Core`, `Host.Composition` (2026-04-06)
- [x] 5.8 Rename `ArchiForge.Api`, `ArchiForge.Worker` → `ArchLucid.Api`, `ArchLucid.Worker` (2026-04-06)
- [x] 5.9 `ArchiForge.sln` → `ArchLucid.sln` (paths + CI/scripts/docs) (2026-04-06)
- [x] 5.10 Update `Directory.Build.props` — `ArchLucidApiClientPackageVersion` + `ArchLucidApiClientPackageVersion` bridge (2026-04-06)
- [x] 5.11 Update `templates/archlucid-finding-engine/` directory and contents — **2026-04-07** (`ArchLucidFindingEngine`, `dotnet new archlucid-finding-engine`)
- [x] 5.12 Rename C# files with `ArchiForge` prefix (instrumentation, configuration bridge, policies, UoW, CLI client, etc.) (2026-04-06)
- [x] 5.13 Update NSwag config (`nswag.json`) and regenerate client (`ArchLucidApiClient.g.cs`) (2026-04-06)

## Phase 6 — Bulk namespace rename (high risk — ASK USER FIRST)

> **Do not start this phase without explicit user confirmation.**
> This is a single large pass that updates all `namespace ArchiForge.*` and `using ArchiForge.*` directives.
> Remove `<RootNamespace>` overrides added in Phase 5.

- [x] 6.1 Bulk rename all `namespace ArchiForge.*` → `namespace ArchLucid.*` across product `.cs` files (2026-04-06)
- [x] 6.2 Bulk rename all `using ArchiForge.*` → `using ArchLucid.*` (2026-04-06)
- [x] 6.3 Update XML doc `<see cref="ArchiForge.*">` references where applicable (2026-04-06)
- [x] 6.4 Update string literals: OTel meter/activity names, `X-ArchLucid-*` headers, Swagger titles; legacy **`ArchiForge:*` / `ArchiForgeAuth` / `ArchiForge.sql` / DB name** intentionally retained where operational (2026-04-06); **2026-04-08** follow-up — user-facing/export/CLI/prompt strings → **ArchLucid**; CI guard in `.github/workflows/ci.yml` (substring **`archiforge`** forbidden in product **`.cs`** / **`.ts`** / **`.tsx`**; allowlists removed **2026-04-17**)
- [x] 6.5 Remove `<RootNamespace>` overrides — product projects use `ArchLucid.*` aligned with dirs (2026-04-06)
- [x] 6.6 Full `Release` build + fast-core test filter; relaxed flaky bulkhead timing assertion (2026-04-06)
- [x] 6.7 Update `docs/NEXT_REFACTORINGS.md` to reflect completion (2026-04-06)

## Phase 7 — Cleanup and external (operational — ASK USER FIRST)

- [x] 7.1 Remove config key fallbacks added in Phase 2 (old `ArchiForge*` keys) — **2026-04-08** (startup warning only when legacy keys present; no silent reads)
- [x] 7.2 Remove OIDC storage key bridge reads (old `archiforge_oidc_*` keys) — **2026-04-08**
- [x] 7.3 Remove environment variable fallbacks (old `ARCHIFORGE_*` vars) — **2026-04-08** (UI warns when legacy env vars set)
- [x] 7.4 Update SQL master DDL script (`ArchiForge.sql` → `ArchLucid.sql`); add a new migration for database rename if needed — **2026-04-08** (`044` comment reference, `045` graph property key; RLS object names unchanged in DB)
- [ ] 7.5 Terraform `state mv` operations for renamed resources (coordinate with deploy window) — **deferred — runbook ready:** [`docs/runbooks/TERRAFORM_STATE_MV_PHASE_7_5.md`](runbooks/TERRAFORM_STATE_MV_PHASE_7_5.md) (2026-04-12)
- [ ] 7.6 Rename GitHub repository — **deferred**
- [ ] 7.7 Update Entra ID app registrations (redirect URIs, display names) — **deferred**
- [ ] 7.8 Rename workspace root directory (`c:\ArchiForge\ArchiForge` → `c:\ArchLucid\ArchLucid`) — **deferred** — see [RENAME_DEFERRED_RATIONALE.md](RENAME_DEFERRED_RATIONALE.md) (2026-04-14)
- [x] 7.9 Final grep for any remaining `ArchiForge` / `archiforge` in **product-facing** docs and infra **identifiers** — **2026-04-10** sweep complete (see progress log). **Still expected by design:** this checklist title/history; **`BREAKING_CHANGES.md`** rows documenting **removed** legacy key spellings; **`MULTI_TENANT_RLS.md`** committed SQL **object** names (`rls.ArchiforgeTenantScope`, `rls.archiforge_scope_predicate`, migration filename **`036_RlsArchiforgeTenantScope.sql`**); **CI** rejects any **`archiforge`** substring in product **`.cs`** / **`.ts`** / **`.tsx`** (allowlists removed **2026-04-17**); Terraform **`moved`** blocks still reference historical **`archiforge`** addresses in **`from`** clauses; APIM Azure API **name** **`archiforge-api`** until operator rename; `.gitleaks.toml` allowlisted dev password literals. **Deferred:** 7.5–7.8.

---

## Progress log

| Date | Batch | Notes |
|------|-------|-------|
| 2026-04-18 | **Docs + UI seam comments** — packaging ↔ code alignment | **`PRODUCT_PACKAGING.md`**: *Contributor drift guard* + Vitest list; **`COMMERCIAL_BOUNDARY_HARDENING_SEQUENCE.md`** Stage 1 (**`LayerHeader`**, rank parity nav vs mutations); **`README.md`**, **`archlucid-ui/README.md`** (*Seam maintenance*); module comments: **`nav-config`**, **`nav-authority`**, **`current-principal`**, **`nav-shell-visibility`**, **`LayerHeader`**, **`layer-guidance`**, **`enterprise-mutation-capability`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — Authority-shaped UI regression tests | **`current-principal.test.ts`** (**`normalizeAuthMeResponse`** rank / **`hasEnterpriseOperatorSurfaces`**); **`nav-shell-visibility.test.ts`** (omit group when authority strips all links); **`LayerHeader`**: **`data-testid="layer-header-enterprise-rank-cue"`** + rank tests via mocked **`useNavCallerAuthorityRank`**; **`use-enterprise-mutation-capability.test.tsx`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — Enterprise Controls cognitive load (copy + hierarchy) | **`layer-guidance.ts`**: shorter Enterprise **`useWhen`** / **`firstPilotNote`**; **`LayerHeader`**: muted **`text-xs`** for Enterprise **`useWhen`**; page intros / inspect-first: **`audit`**, **`alerts`**, **`governance/dashboard`**, **`governance-resolution`**, **`governance`** workflow, **`policy-packs`**, alert tooling pages, **`alert-routing`** (**`flex-col-reverse`** when read-only). **`LayerHeader.test.tsx`** green. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — Enterprise action-level authority shaping | **`useEnterpriseMutationCapability`**: governance dashboard pending empty reader supplement; governance workflow **`flex-col-reverse`** (inspect before submit) + query card / empty copy; policy packs empty-scope copy; alert rules **`flex-col-reverse`** + defined-list empty copy + intro; strings in **`enterprise-controls-context-copy.ts`**; **`README`**, **`enterprise-mutation-capability`** comment. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **CI / k6** — RLS break-glass for **`dotnet run` ArchLucid.Api** | **`scripts/ci/start_api_for_k6.sh`** + **`.github/workflows/live-e2e-nightly.yml`** set **`ARCHLUCID_ALLOW_RLS_BYPASS`** + **`ArchLucid__Persistence__AllowRlsBypass`** (matches **`appsettings.Advanced.json`** RLS session context + schema bootstrap). **`docs/TEST_EXECUTION_MODEL.md`**, **`tests/load/README.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — Enterprise authority-aware copy (nav + **`LayerHeader`**) | **`enterprise-controls-context-copy`**: **`layerHeaderEnterpriseReaderRankLine`** / **`layerHeaderEnterpriseOperatorRankLine`**; **`LayerHeader`** imports them; Enterprise nav **caption** + **`enterpriseNavHint*`** + Home Enterprise blurb + onboarding clause; **`ShellNav`**, **`LayerHeader`** tests; **`README`**, **`archlucid-ui/docs/ARCHITECTURE.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — Operator shell nav: **`listNavGroupsVisibleInOperatorShell`** | Sidebar, **`MobileNavDrawer`**, **`CommandPalette`** share **`listNavGroupsVisibleInOperatorShell`** (tier → authority, drop empty groups). Vitest: **`nav-shell-visibility.test.ts`** for **`listNavGroupsVisibleInOperatorShell`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — Enterprise UI actions vs **`useEnterpriseMutationCapability`** | Alerts inbox buttons + confirm + **`useAlertCardShortcuts`** **`mutationsEnabled`**; filtered empty-state CTA copy; governance workflow **`GOVERNANCE_WORKFLOW_IDLE_READER`**; alert rules / routing / composite create+toggle soft-disable; Vitest + **`enterprise-mutation-capability`** docstring + **`archlucid-ui/README.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — Enterprise page **`LayerHeader`** + alert tooling rank cue | **`layer-guidance.ts`** keys (**`governance-resolution`**, **`governance-workflow`**, **`policy-packs`**, **`alert-rules`**, **`alert-routing`**, **`alert-simulation`**, **`alert-tuning`**, **`composite-alert-rules`**); **`AlertOperatorToolingRankCue`** + copy; pages wired; removed unused **`governanceResolutionContextLine`** / **`enterpriseOperatorConfigSurfaceOperatorPlusLine`**; Vitest + **`archlucid-ui/README.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`ArchLucid.Cli`** — NSwag comparison history call | **`ArchLucidCliApiClient.SearchComparisonsAsync`** → **`ComparisonsGETAsync`** (was **`ComparisonsGET4Async`**, now run-only). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | CI — Trivy binary pin | **`.github/workflows/ci.yml`**: all **`aquasecurity/trivy-action`** steps pass **`version: v0.70.0`** (default **`v0.69.3`** no longer installs via **`setup-trivy`**). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | CodeQL **`cs/log-forging`** — comparison replay warning sink | **`SanitizedLoggerWarningExtensions.LogWarningComparisonReplayFailed`** (sanitized strings adjacent to **`LogWarning`** + suppression on sink line); **`ComparisonReplayApiService`** uses it; **`SanitizedLoggerWarningExtensionsTests`** + **`CODEQL_TRIAGE.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | OpenAPI snapshot — cross-OS stable contract test | **`OpenApiJsonCanonicalizer`** (sorted object keys + stable **`tags`** ordering); **`OpenApiContractSnapshotTests`** compare/write canonical JSON; **`OpenApiJsonCanonicalizerTests`**; snapshot regen + **`OPENAPI_CONTRACT_DRIFT`**, **`TEST_EXECUTION_MODEL`** notes. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | **`archlucid-ui`** — inline style `rem` values quoted for SWC/Next build | Operator pages: **`maxWidth: "40rem"`** / **`"42rem"`** / **`"52rem"`** on intro `<p>` / `<details>` styles (invalid JS **`40rem`** token broke webpack). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | Operator UI — Enterprise mutation controls vs authority rank | **`useEnterpriseMutationCapability`**, **`enterprise-mutation-capability.ts`**, soft-disable governance dashboard/workflow + policy pack write buttons for read-tier principals; **`enterpriseMutationControlDisabledTitle`**; Vitest + **`archlucid-ui/README.md`** + **`nav-authority`** doc touch. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | Operator UI — Enterprise page-level authority cues (wave 2) | **`AlertsInboxRankCue`**, **`AuditLogRankCue`**, governance dashboard **`EnterpriseExecutePlusPageCue`** + copy in **`enterprise-controls-context-copy.ts`**; Vitest + **`archlucid-ui/README.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | Operator UI — runtime principal in shell context | **`OperatorNavAuthorityProvider`** exposes **`currentPrincipal`** (same fetch as rank); **`current-principal.ts`** adds **`shellBootstrapReadPrincipal`** / **`operatorNavOutsideProviderPrincipal`**; **`getCurrentAuthorityRank`** test; README + **`archlucid-ui/docs/ARCHITECTURE.md`** + **`nav-shell-visibility`** doc touch. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-18 | Operator UI — in-product product-layer guidance | **`layer-guidance.ts`**, **`LayerHeader`**, **`PostCommitAdvancedAnalysisHint`**, **`AfterCorePilotChecklistHint`** + **`core-pilot-checklist-storage`** (custom event on checklist toggle); **`nav-config`** optional **`caption`** + **`SidebarNav`** / **`MobileNavDrawer`** captions + **`aria-label`** on group triggers; **`compare`**, **`replay`**, **`graph`**, **`governance/dashboard`**, **`alerts`**, **`audit`** pages; **run detail** post-commit strip; **Home** post-checklist strip; **onboarding** pointer to **`OPERATOR_DECISION_GUIDE`**. Docs: **`operator-shell.md`**, **`archlucid-ui/README.md`**, **`PRODUCT_PACKAGING.md`**. Vitest: **`LayerHeader.test.tsx`**, **`AfterCorePilotChecklistHint.test.tsx`**, **`ShellNav.test.tsx`** fix. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt **B8** — live trial end-to-end + V1 gate | **`E2eHarnessController`** (`POST /v1/e2e/trial/set-expires`, `POST /v1/e2e/billing/simulate-subscription-activated`); **`E2eHarnessOptions`** bind in **`Program`**; **`SkipTrialWriteLimit`** on **`BillingCheckoutController.CheckoutAsync`** (checkout when trial metering blocks other writes). Playwright **`archlucid-ui/e2e/live-api-trial-end-to-end.spec.ts`** + helpers **`getTenantTrialStatus`**, **`postHarnessTrialSetExpires`**, **`postHarnessBillingSimulateActivated`**; CI **`ui-e2e-live`** — Prometheus on, registration headroom, shared **`LIVE_E2E_HARNESS_SECRET`**. Docs **`docs/runbooks/TRIAL_END_TO_END.md`**, **`V1_RELEASE_CHECKLIST.md`**, **`V1_READINESS_SUMMARY.md`**, **`docs/go-to-market/PRICING_PHILOSOPHY.md`** gate **#3** note (review, no auto price). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt **B7** — trial funnel observability | **`ArchLucidInstrumentation`** trial + billing checkout metrics; **`SqlTrialFunnelCommitHook`** + migration **`081_Tenants_TrialFirstManifestCommittedUtc.sql`**; Grafana **`dashboard-archlucid-trial-funnel.json`** + Terraform **`grafana_dashboard.trial_funnel`**; Prometheus **`archlucid-trial-funnel`** alert group + **`infra/prometheus/tests/trial-funnel-alerts.test.yml`**; audit types **`TrialSignupAttempted`**, **`TrialSignupFailed`**, **`TrialFirstRunCompleted`**, **`BillingCheckoutInitiated`**, **`BillingCheckoutCompleted`** (`AUDIT_COVERAGE_MATRIX` count **92**); docs **`docs/runbooks/TRIAL_FUNNEL.md`**, **`docs/OBSERVABILITY.md`** trial section; tests **`TrialFunnelInstrumentationTests`**, **`PrometheusTrialFunnelMetricsSmokeTests`**, **`Migration081TrialFunnelSqlTests`**; Playwright **`live-api-trial-signup.spec.ts`** metrics assertions + scoped architecture helpers. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | B3 follow-up — build + concurrency | Duplicate **`using ArchLucid.Application.Tenancy`** removed (**`ServiceCollectionExtensions.TenancyMeteringSecrets.cs`**). **`Migration079TenantLifecycleSqlTests`** + **`Migration080PolicyPackVersionsSqlTests`**. Policy pack publish: **`IPolicyPackVersionRepository.UpsertPublishedVersionAsync`** (Serializable tx + **`UPDLOCK,HOLDLOCK`**), **`080_PolicyPackVersions_UniquePackVersion.sql`** + **`ArchLucid.sql`** unique index / dedupe block; **`HostLeaderElectionCoordinator`** treats **linked** leader cancellation on app shutdown as normal (**`OperationCanceledException`**). **`dotnet test ArchLucid.sln -c Release`** green. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt **B3** — trial expiry lifecycle + hard purge | **`TrialLifecycleStatus`**: **`Expired`**, **`ReadOnly`**, **`ExportOnly`**, **`Deleted`**; **`Trial:Lifecycle`** options + **`TrialLifecyclePolicy`** / **`TrialLifecycleTransitionEngine`**; **`079_TenantLifecycleTransitions.sql`** + **`ArchLucid.sql`**; **`TrialLifecycleSchedulerHostedService`** (lease **`hosted:trial-lifecycle-automation`**); **`SqlTenantHardPurgeService`** + **`NoOpTenantHardPurgeService`**; **`TrialLimitGate`** write/delete phases; **`GET /v1/tenant/trial-status`** **`DaysRemaining`** uses phase deadlines; audit **`TrialLifecycleTransition`** (**`AUDIT_COVERAGE_MATRIX`** count **87**); docs **`docs/runbooks/TRIAL_LIFECYCLE.md`**, **`DATA_CONSISTENCY_MATRIX`** row; integration **`TenantHardPurgeServiceSqlIntegrationTests`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt **B2** — billing bridge (Stripe + Azure Marketplace) | **`ArchLucid.Core.Billing`**: **`IBillingProvider`**, **`IBillingLedger`**, checkout DTOs; **`078_BillingSubscriptions.sql`** + **`ArchLucid.sql`** (`dbo.BillingSubscriptions` RLS + **`dbo.BillingWebhookEvents`** + **`sp_Billing_*`** + **`ArchLucidApp` DENY**); **Persistence** providers (**`StripeBillingProvider`**, **`AzureMarketplaceBillingProvider`**, **`NoopBillingProvider`**, **`MicrosoftMarketplaceJwtVerifier`**); **API** **`BillingCheckoutController`**, anonymous webhooks **`/v1/billing/webhooks/{stripe|marketplace}`**; **`CollectBillingStripeSecret`** production guard; **`TenantTrialController`** + **`IBillingTrialConversionGate`**; **`MarkTrialConvertedAsync(..., TenantTier?)`**; docs **`docs/BILLING.md`**, **`docs/AZURE_MARKETPLACE_SAAS_OFFER.md`**, **ADR 0016**, **`SYSTEM_THREAT_MODEL`** row; inventory **`dbo.BillingSubscriptions`**. Migration **078** (not **074** — already **trial seat occupants**). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | B4 follow-up — CI / contract hardening | **`ArchLucidConfigurationRulesTests`**: Production API + Worker require **`Email:AzureCommunicationServicesEndpoint`** when **`Email:Provider=AzureCommunicationServices`**; **`DatabaseMigrator.RunExcludingTrailingScripts`**: validate skip count before opening SQL (invalid skip no longer surfaces as connection-string parse error); **`DatabaseMigratorTests`**: expected manifest list excludes **`Migrations/Baseline/`** (matches **`GetOrderedMigrationResourceNames`**); OpenAPI snapshot regen (**`openapi-v1.contract.snapshot.json`**); **`TrialLocalIdentityAuthController`**: **`NotFoundProblem`** when local identity disabled (**`ApiControllerProblemDetailsSourceGuardTests`**). **`dotnet test ArchLucid.sln -c Release`** green. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt **B4** — trial lifecycle transactional email | **`IEmailProvider`** + **`EmailMessage`** in **Core**; **Razor** templates + **`RazorLightEmailTemplateRenderer`**; **Persistence** providers (**ACS** / **SMTP** / **Noop**) + **`IAzureCommunicationEmailApi`** contract tests; **`TrialLifecycleEmailPublishingAuditDecorator`**, **`TrialLifecycleEmailIntegrationEventHandler`**, **`TrialLifecycleEmailScanHostedService`**; **`dbo.SentEmails`** idempotency (**076**); production **`CollectTransactionalEmailAcs`**; integration schema + catalog row; docs **`docs/EMAIL_NOTIFICATIONS.md`**, **`docs/security/PII_EMAIL.md`**; **`PreserveCompilationContext`** for RazorLight tests. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt **B5** — public trial signup UI (`archlucid-ui`) | **`(marketing)`** routes **`/welcome`**, **`/signup`**, **`/signup/verify`**; **`/onboarding/start`** + **`registration-session`** scope merge for proxy fetches; **`TrialBanner`** + trial-aware **`WelcomeBanner`**; **`NewRunWizardClient`** **`sampleRunId`** callout; **`generate:pricing`** before **`next build`**; docs **`archlucid-ui/docs/TRIAL_SIGNUP_UI.md`**, **`docs/operator-shell.md`** trial banner §; Vitest + jest-axe + Playwright **`live-api-trial-signup`** UI row + **`live-api-accessibility`** routes. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt **B6** — trial auth (External ID + local identity) | **`Auth:Trial`** modes, **`TrialTenantBootstrapService`** email gate, **`dbo.IdentityUsers`** (**077** + **`ArchLucid.sql`**), **`PwnedPasswordRangeClient`** (**24h** cache), JwtBearer External Id issuer hook + **`LocalTrialJwtIssuer`**, production **`CollectTrialAuthExternalId`**, **`infra/terraform-entra/external_id.tf`**, **`docs/security/TRIAL_AUTH.md`**, **`SYSTEM_THREAT_MODEL`** row, **ADR 0015**, tests (**policy**, **pwned cache**, **bootstrap skip**, **Jwt role integration**, **config rule**), **`archlucid-ui/e2e/live-api-trial-signup.spec.ts`** matrix env. **ADR numbering:** next sequential slot was **0015** (prompt text said 0016). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Trial limits enforcement (runs + seats) + read-only after expiry | Server-side **`TrialLimitGate`**, **402** **`application/problem+json`** (`trial-expired`), authorization **`TrialActive`** on Execute/Admin, atomic **`TrialRunsUsed`** increment in run save UoW, **`TrialSeatAccountant`** + seat occupants table (**074**), **`docs/security/TRIAL_LIMITS.md`**, **ADR 0014**, **`API_CONTRACTS`** 402 example; **`GreenfieldSqlApiFactory`** RLS break-glass env + **`ArchLucidEnvMutation`** collection for stable parallel test runs. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Explainability evidence + faithfulness budget alert (`close-audit-and-explainability-faithfulness-gaps`) | **`FindingExplainabilityEvidence`** in **Contracts** + **`FindingExplainabilityResult.Evidence`**; **`FindingExplainabilityNarrativeBuilder.BuildEvidence`**; **`FindingTraceConfidenceDto`** **`RuleId`** / **`EvidenceRefCount`** via mapper; **Prometheus** **`archlucid:slo:explanation_faithfulness_fallback_budget`** + **`ArchLucidExplanationFaithfulnessFallbackBudgetExceeded`**; **`docs/EXPLAINABILITY_TRACE_COVERAGE.md`**; **UI** explainability table + dialog; OpenAPI snapshot + **NSwag** client regen. **`AUDIT_COVERAGE_MATRIX`** “Known gaps” remains **0** (no new durable audit routes this session). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | **Onboarding consolidation + migration baseline** (`collapse-onboarding-and-migration-sprawl`) | **Docs:** Canonical Day-1 paths = **`docs/onboarding/day-one-developer.md`**, **`day-one-sre.md`**, **`day-one-security.md`**, **`docs/OPERATOR_QUICKSTART.md`** (self-contained updates where needed). **`START_HERE`**, **`GOLDEN_PATH`**, **`GOLDEN_CHANGE_PATH`**, **`ONBOARDING_HAPPY_PATH`**, **`PILOT_GUIDE`**, **`CONTRIBUTOR_ONBOARDING`** → short stubs; prior bodies → **`docs/archive/ONBOARDING_*_2026_04_17.md`**; **`docs/archive/README.md`** table. **`README.md`** Key docs table trimmed to **5** rows + pointer to **`ARCHITECTURE_INDEX`**. Cross-links: **`ARCHITECTURE_INDEX`**, **`DEPLOYMENT`**, **`infra/README`**, **`diagrams/c4`**, **`archlucid-ui/README`**, **`CURSOR_PROMPTS_CANONICAL`**. **SQL:** **`Migrations/Baseline/000_Baseline_2026_04_17.sql`** (mechanical union **001–050** for audit/review) + **`GreenfieldBaselineMigrationRunner`** replays embedded **001–050** then stamps **`SchemaVersions`**; **`DatabaseMigrator`** excludes **Baseline** from DbUp list. **`docs/SQL_SCRIPTS.md`**, **`DATABASE_MIGRATION_ROLLBACK.md`**. **`python scripts/ci/check_doc_links.py`**: OK. **`dotnet test`** `GreenfieldSqlBoot` + **`DatabaseMigrationScriptTests`**: green. *Doc mass:* entry-point bodies moved to archive (~**>95%** line reduction on those six paths vs pre-stub). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | **`QUALITY_IMPROVEMENT_PROMPTS.md`** §2–6 — verify + status table | Confirmed prompts **2–6** already shipped (trace blobs **`053`**/**`062`**, k6 **`smoke.js`** + CI jobs, audit **`ComparisonSummaryPersisted`** + `/audit` UX, **`NEXT_REFACTORINGS`** + Phase **7.5** runbook, pre-commit gate **`054`**). Added **Implementation status** table to **`docs/QUALITY_IMPROVEMENT_PROMPTS.md`** mapping spec → repo. **`dotnet test`** (Release): **`ArchLucid.AgentRuntime.Tests`** (`AgentExecutionTraceRecorder`, **15** passed); **`ArchLucid.Application.Tests`** (`PreCommitGovernanceGateTests` + `ArchitectureRunCommitPipelineIntegrationTests`, **18** passed). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Prompt 1 (rename hygiene) — CI archiforge guards + TF comment | **CI:** `.github/workflows/ci.yml` — C# / TS **`archiforge`** substring guards no longer use **`scripts/ci/archiforge-rename-allowlist*.txt`** (files removed; product sources have zero hits). **`infra/terraform/moved_archlucid_apim.tf`** comment aligned with **`moved_archlucid_monitoring.tf`**. Checklist **§7.9** updated. **Still on you for true 7.5:** `terraform plan` per stack with remote state after IaC **`moved`** merges (see **`docs/runbooks/TERRAFORM_STATE_MV_PHASE_7_5.md`**). **Not done this batch:** new SQL migration to rename RLS policy / function (brownfield — separate change). Phase **7.6–7.8** unchanged (deferred). |
| 2026-04-17 | Six quality improvements (golden path, onboarding, SaaS ref, explainability alert, parity tests, JWT option + DDL guard) | **`docs/GOLDEN_CHANGE_PATH.md`**, **`docs/REFERENCE_SAAS_STACK_ORDER.md`**, **`templates/archlucid-api-endpoint/`** (`dotnet new archlucid-api-endpoint`). UI: **`OnboardingWizardClient`** + tests; **`cd-post-deploy-verify.sh`** asserts OpenAPI **`.info.title`**. Prometheus: **`ArchLucidExplainabilityTraceCompletenessP10Low`**. Tests: **`StorageProviderPublicSurfaceParityIntegrationTests`**, **`ArchLucidAuth:RequireJwtBearerInProduction`**. CI: **`assert_tenant_inventory_tables_in_archlucid_sql.py`**. Docs: **`SECURITY.md`**, **`ARCHITECTURE_INDEX`**, **`START_HERE`**, **`DEPLOYMENT_TERRAFORM`**, **`OBSERVABILITY`**, **`FIRST_RUN_WIZARD`**, **`TENANT_SCOPED_TABLES_INVENTORY`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Improvements **3–6** (rename hygiene, traceability, explainability ops, wizard E2E + guard) | **Imp 3:** **`infra/terraform-monitoring`** — **`moved_archlucid_monitoring.tf`** + Grafana / Prometheus rule resource labels **`archlucid`**; **`docs/DI_REGISTRATION_MAP.md`** — order matches **`ServiceCollectionExtensions.cs`** (Retrieval before Governance), documents **`RegisterIntegrationEventConsumer`** + **`RegisterCosmosPolyglotPersistence`**, adds **`CosmosPolyglotPersistence`** source row; connection-string bullet de-duplicated. **Imp 4:** **`V1_REQUIREMENTS_TEST_TRACEABILITY.md`** — NFR row **Integration event outbox (async delivery)** with SLI / alert / TF pointers. **Imp 5:** **`infra/prometheus/archlucid-alerts.yml`** — explainability alert description links **`docs/EXPLAINABILITY_TRACE_COVERAGE.md`** + OpenAPI **`narrativeText`**. **Imp 6:** **`archlucid-ui/e2e/first-run-wizard.spec.ts`**; **`FIRST_RUN_WIZARD.md`** + **`operator-shell.md`** wizard path; **`ApiControllerProblemDetailsSourceGuardTests`** flags **`return StatusCode(StatusCodes.Status404NotFound)`**. Runbook **`TERRAFORM_STATE_MV_PHASE_7_5.md`** monitoring § notes IaC **`moved`**. Phase **7.5–7.8** still deferred for Entra / GitHub / workspace path; monitoring **resource addresses** in repo now **`archlucid`**. |
| 2026-04-17 | Improvements 1–2 (coverage + idempotency + APIM Phase 7.5 IaC) | **CI:** merged coverage gates **74→76%** line, **57→60%** branch (`.github/workflows/ci.yml`); **`stryker-baselines.json`** mutation floors **65→67**. **Tests:** **`ArchitectureRunCreateOrchestratorIdempotencyConcurrencyTests`** same-key burst **8→64**; new **`Parallel_create_with_distinct_idempotency_keys_invokes_coordinator_each_time`**; **`CreateRunIdempotencyConcurrencyIntegrationTests`** parallel **64**. **Terraform (7.5 partial):** root **`infra/terraform`** — **`moved_archlucid_apim.tf`** + **`azurerm_api_management` / `azurerm_api_management_api`** addresses **`archlucid`** (Azure API name **`archiforge-api`** unchanged). **`docs/CODE_COVERAGE.md`**, runbook cross-link. **Follow-up:** `rg "archiforge" infra --glob "*.tf"` for any remaining **resource address** labels outside APIM + monitoring. |
| 2026-04-16 | Operator usability + docs/CI (six improvements) | **UI:** **`alerts/page`**, **`compare/page`** — Tailwind/shadcn patterns, **`Dialog`** for alert actions (no **`window.prompt`**); **`Select`** sentinel **`__all__`** for “All statuses”. **`loading.tsx`** for **`runs/[runId]`**, **`runs/[runId]/provenance`**, **`manifests/[manifestId]`**. **Explainability:** **`getFindingExplainability`**, **`FindingExplainabilityDialog`**, **`RunFindingExplainabilityTable`** on run detail when trace confidences exist. **Tests:** **`FindingExplainabilityDialog.test.tsx`** (removed flaky null-id timer case). **API:** **`ApiControllerProblemDetailsSourceGuardTests`** flags **`return NotFound(`** / **`Conflict(`** / **`BadRequest(`**. **E2E:** **`accessibility.spec.ts`** — provenance + manifest routes. **Docs/CI:** **`check_doc_links.py`** (skip **`docs/archive`**, directory + optional **`.md`** resolution), **`check_doc_freshness.py`**, link fixes, **`docs/runbooks/*`** **`Last reviewed`**, **`docs/CURSOR_PROMPTS_INDEX.md`**, **`ARCHITECTURE_INDEX`** link; **`ci.yml`** **`doc-markdown-links`** job + downstream **`needs`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | Six **correctness** improvements (session carry-over) | **(1)** **`TimeProvider`** + **`CircuitBreakerGate`**: compatibility **`Func<DateTimeOffset>`** constructors retained **without** **`[Obsolete]`** so **`TreatWarningsAsErrors`** test projects compile. **(2)** Create-run **idempotency**: bounded in-process gate + **`IDistributedCreateRunIdempotencyLock`** / SQL **`sp_getapplock`** (prior commits in branch). **(3)** Contract testing: **`docs/CONTRACT_TEST_COVERAGE_GAP_ANALYSIS.md`** + **`OpenApiContractInvariantsTests`** (OpenAPI **3.1.1**, title, **`/v1/register`**). **(4)** DTO immutability surface: **`DecisionNode`** collection props + **`ArchitectureRun.TaskIds`** → **`IReadOnlyList<>`**. **(5)** Tenant DDL: **`TenantScopedTableDdlTests`** expanded (**`Runs`** + **`ScopeProjectId`**; theory for **`AuditEvents`**, **`IntegrationEventOutbox`**, **`GoldenManifests`**); **`docs/TENANT_SCOPED_TABLES_INVENTORY.md`**. **(6)** **`FindingsOrchestrator`** partial success + schema bump + OTel (prior commits). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | Six quality prompts — remainder (auth prod guard, Azure Content Safety, prompt regression floors, ops docs) | **`AuthSafetyGuard`**: production-like env detection (**`ARCHLUCID_ENVIRONMENT`**, **`IsProduction`**, excludes **`non-production`**); tests in **`AuthSafetyGuardTests`**. **`Azure.AI.ContentSafety`**: **`AzureContentSafetyGuard`**, **`ContentSafetyOptions`** (**`BlockSeverityThreshold`**, **`FailClosedOnSdkError`**), DI in **`ServiceCollectionExtensions.AgentsGovernanceRetrieval`**, composition test for real guard resolution; **`AzureContentSafetyGuardMapResultTests`**. **`prompt_regression_baseline.json`** + **`assert_prompt_regression.py`** + **`PromptRegressionBaselineContractTests`**: Cost / Compliance / Critic structural + semantic floors (shared **`golden-agent-result-valid.json`**). Docs: **`SECURITY.md`**, **`INTEGRATION_EVENT_SCHEMA_REGISTRY.md`**, **`FIRST_RUN_WALKTHROUGH.md`**, **`FIRST_RUN_WIZARD.md`** link, **`INTEGRATION_EVENT_CATALOG.md`** / **`ARCHITECTURE_INDEX.md`** registry cross-links. **Not in this batch:** exhaustive persistence test assault (Imp 1), per-controller auth policy sweep beyond existing guards (Imp 2), extra archival tables without **`ArchivedUtc`** (Imp 3), RFC 9457 sweep every action (Imp 4 — largely guard-tested already), new API versioning surface (Imp 5 — already present). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | **Cosmos polyglot persistence (wiring + validation)** | **`CosmosDbOptions`** + **`Newtonsoft.Json`** (Cosmos SDK check); **`CosmosAuditRepository`**, **`AuditEventChangeFeedHostedService`**, **`RegisterCosmosPolyglotPersistence`** (after **`RegisterAgentExecution`**); **`CosmosPolyglotRules`** (connection string + block emulator in Production); **`AuthorityPipelineStagesExecutor`** saves graph outside SQL UoW when **`CosmosDb:GraphSnapshotsEnabled`**; **`StartupConfigurationFacts`** Cosmos summary; **`appsettings*.json`** **`CosmosDb`** (dev emulator string, flags off). Tests: **`ArchLucidConfigurationRulesTests`**, **`StartupConfigurationFactsReaderTests`**, **`AuthorityPipelineStagesExecutorTests`**, parity. **`CosmosAgentExecutionTraceRepository`** COUNT page fix. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | **`RegistrationControllerTests`** — SQL integration categorization | **`[Trait("Category", "Integration")]`** so **`dotnet test`** fast-core filter (**`Category!=Integration`**, no SQL on runner) skips **`GreenfieldSqlApiFactory`** tests; avoids fixture ctor failures on Linux without **`ARCHLUCID_SQL_TEST`**. **`GreenfieldSqlApiFactory`**: wrap catalog prep exceptions with **`InvalidOperationException`** + env var hints. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | Self-service trial bootstrap — RLS + multi-tenant demo PKs | **`TrialTenantBootstrapService`**: **`SqlRowLevelSecurityBypassAmbient.Enter()`** + **`ContosoRetailDemoIds.ForTenant`** for trial sample run id. **`DemoSeedService`**: per-tenant demo keys (deterministic run GUIDs + suffixed strings) so a second registered tenant in one SQL catalog does not collide on **`dbo.Runs`**, **`ArchitectureRequests`**, **`AgentTasks`**, etc.; **`ScopeIds.DefaultTenant`** keeps canonical **`ContosoRetailDemoIdentifiers`** for dev URLs. Docs: **`TRUSTED_BASELINE.md`**, **`demo-quickstart.md`**, **`CI_MIGRATION_CHECKLIST.md`**. Tests: **`ContosoRetailDemoIdsTests`**, **`RegistrationControllerTests`** + **`DemoSeedServiceTests`**. No checklist phase items to check (7.5–7.8 still deferred). |
| 2026-04-16 | Azure/SaaS readiness — six follow-ups (RLS tests, staging guard, registration, Entra multi-tenant, secondary region TF, CD canary) | **RLS:** Staging runs **`CollectSqlRowLevelSecurity`**; **`ARCHLUCID_ALLOW_RLS_BYPASS`** escape hatch; **`CrossTenantSessionContextSqlConnectionIsolationTests`** (factory + applicator path). **Registration:** **`POST /v1/register`**, rate limit **`registration`**, audit **`TenantSelfRegistered`**. **Entra:** **`MultiTenantEntra`** + issuer + optional **`tid`** allowlist; migration **`071_Tenants_EntraTenantId`**, repo lookup; Terraform Entra **`sign_in_audience`**. **Infra:** **`secondary_region.tf`**. **CD:** **`cd.yml`** canary split + bake + promote after smoke; **`CANARY_DEPLOYMENT.md`**. Tests: **`EntraMultiTenantJwtBearerConfiguratorTests`**, **`RegistrationControllerTests`**, **`ArchLucidAllowRlsBypassConfigurationRulesTests`**, staging RLS rule, **`TenantProvisioningService`** Entra/override. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | Create-run idempotency concurrency (in-process gate) | **`ArchitectureRunCreateOrchestrator`**: after fast-path replay miss, **`SemaphoreSlim`** per idempotency gate key (tenant/workspace/project + key hash) + second **`TryReplay`** under lock before **`ICoordinatorService.CreateRunAsync`** so parallel **`Idempotency-Key`** posts do not create orphan **`dbo.Runs`** (single API process); XML remarks note multi-replica need for distributed lock (for example **`sp_getapplock`**). **`ArchitectureRunCreateOrchestratorIdempotencyConcurrencyTests`**: eight parallel creates → coordinator invoked once. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Azure/SaaS readiness — tenancy, metering, secrets, App Insights OTEL, Terraform | **`ITenantRepository`** / **`IUsageEventRepository`** (Dapper + InMemory); **`UsageMeteringService`**; **`TenantProvisioningService`** + admin **`TenantsAdminController`** / **`MeteringAdminController`**; **`ApiRequestMeteringMiddleware`**; **`LlmCompletionAccountingClient`** + **`ArchitectureRunCreateOrchestrator`** metering hooks; **`ISecretProvider`** (env + Key Vault); **`AddAzureMonitor*Exporter`** when connection string set; **`infra/terraform-keyvault`**, CMK block on **`terraform-storage`**, **`api_revision_mode`** vars on **`terraform-container-apps`**, optional **`azurerm_application_insights`** in **`terraform-monitoring`**; CI matrix + CD canary placeholder; runbooks **`CANARY_DEPLOYMENT.md`**, **`CMK_ENCRYPTION.md`**; **`AuditEventTypes.TenantProvisioned`** + matrix **82**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Weighted six UX improvements — remainder (nav, pickers, k6, tests) | **`RunDetailSectionNav`** wired on **`runs/[runId]/page.tsx`** with section **`id`s** + **`scroll-mt-20`** wrappers; **`RunIdPicker`** on **`compare/page.tsx`** + **`replay/page.tsx`** (trimmed replay **`runId`**); **`RunIdPicker`** list fetch on focus only (no debounced refetch per keystroke). **`tests/load/ci-smoke.js`**: **`get_run_detail`** + **`client_error_telemetry`**; **`assert_k6_ci_smoke_summary.py`** per-tag caps; **`LOAD_TEST_BASELINE.md`**. UI tests: **`error-telemetry.test.ts`**, **`RunDetailSectionNav.test.tsx`**, **`RunIdPicker.test.tsx`**. OpenAPI snapshot refresh if **`POST /v1/diagnostics/client-error`** not yet in contract. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Azure/SaaS prompts — RLS BLOCK + tenant blob prefixes | **`068_RlsBlockPredicatesTenantScope.sql`** + **`Rollback/R068_*.sql`**: **BLOCK** predicates (**AFTER INSERT** / **AFTER UPDATE** / **BEFORE DELETE**) on **`rls.ArchiforgeTenantScope`** tables (same scope function as FILTER). **`ArchLucid.sql`** master DDL aligned. **`ArtifactBlobTenantPaths`**, **`AzureBlobArtifactBlobStore`** / **`LocalFileArtifactBlobStore`** tenant-prefixed paths + read guards; **`ArchLucidStorageServiceCollectionExtensions`** inject **`IScopeContextProvider`**. Tests: **`ArtifactBlobTenantPathsTests`**, **`LocalFileArtifactBlobStoreTests`**, **`RlsArchLucidScopeIntegrationTests`** (cleared session + cross-tenant insert). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 6 — navigation aliases + runbook priorities + project map | **`docs/FIRST_FIVE_DOCS.md`**, **`docs/API_CONTROLLER_MAP.md`** (aliases to **FIRST_5** / **CONTROLLER_AREA_MAP**); **`docs/PROJECT_MAP.md`** (solution ↔ bounded context); **`docs/runbooks/README.md`** **P1/P2/P3** tags + missing runbooks (**COMPARISON_RECORD_ORPHAN_REMEDIATION**, **API_KEY_ROTATION**, **TERRAFORM_STATE_MV_PHASE_7_5**); cross-links **`ARCHITECTURE_INDEX`**, **`FIRST_5_DOCS`**, **`bounded-context-map`**, **`CONTROLLER_AREA_MAP`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 5 — agent quality gates + prompt regression + content safety docs | **`prompt_regression_baseline.json`**: Topology floors **0.95** / **0.85**. **`assert_prompt_regression.py`**: shape + anti-placeholder Topology mins. **`PromptRegressionBaselineContractTests`** + csproj link copy baseline to output. **`docs/AGENT_OUTPUT_EVALUATION.md`** (quality gate defaults), **`AI_AGENT_PROMPT_REGRESSION.md`**, **`SECURITY.md`** (content safety status + extension path), **`OBSERVABILITY.md`** (reference eval off by default), **`QUALITY_ASSESSMENT_2026_04_14_WEIGHTED.md`**, **`appsettings.Advanced.json`** **`ArchLucid:ContentSafety`**. No Azure Content Safety SDK (deferred). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 4 — traceability (orphans + explainability + RTM NFR) | **`DataConsistencyProbeOptions.OrphanProbeRemediationDryRunLogMaxRows`**: optional probe-attached **`SELECT`** sample logging (no delete). **`TopologyCoverageFindingEngine`**: **`AlternativePathsConsidered`** on both emitted branches + tests. **`docs/EXPLAINABILITY_TRACE_COVERAGE.md`**, **`V1_REQUIREMENTS_TEST_TRACEABILITY.md`** (NFR subsection + detection row), **`appsettings.Advanced.json`**, **`COMPARISON_RECORD_ORPHAN_REMEDIATION.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | Improvement 3 — secure-by-default auth defaults | **`appsettings.json`**: **`ArchLucidAuth:Mode`** **`ApiKey`** (was **`DevelopmentBypass`**); **`Authentication:ApiKey`** remains **`Enabled: false`**, **`DevelopmentBypassAll: false`** (fail closed until keys + enable). **`appsettings.Development.json`**: **`ArchLucidAuth:Mode`** **`DevelopmentBypass`**; **`DevelopmentBypassAll`** **`false`** (removes prior dev-only open API-key bypass). **`ArchLucidAuthOptions.Mode`** default **`ApiKey`**. **`docs/SECURITY.md`** shipped-defaults section; **`PILOT_GUIDE.md`** auth note. ZAP per-PR / audit matrix / PII retention docs unchanged (already met). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | Improvement 2 — solution hygiene + controller areas | **`ArchLucid.sln`**: removed non-portable **`docs`** solution folder (**OneDrive** `archiforge_design.md` shortcut). **`ArchLucid.Api/Controllers/`**: **`git mv`** into **`Authority/`**, **`Governance/`**, **`Alerts/`**, **`Admin/`**, **`Advisory/`**, **`Evolution/`**, **`Planning/`**; namespaces **`ArchLucid.Api.Controllers.{Area}`**; validators + **`Api.Tests`** usings; XML cref fixes (**`IAdminDiagnosticsService`**, **`RecommendationRecordResponse`**, **`IArchitectureRunService`**, **`AdvisoryControllerImprovementsIntegrationTests`**). Docs: **`BUILD.md`**, **`CONTROLLER_AREA_MAP.md`**, **`START_HERE.md`**, **`CODEQL_TRIAGE.md`**, **`GOVERNANCE_WORKFLOW_UI.md`**, **`day-one-developer.md`**, **`CORRECTNESS_QUALITY_ASSESSMENT_2026_04_15.md`**, **`API_SLOS.md`**. **`ArchiForge.sln`**: absent at repo root (canonical **`ArchLucid.sln`** only). **`dotnet build ArchLucid.sln -c Release`** green. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | Improvement 1 — CI coverage ratchet (partial) | **`.github/workflows/ci.yml`**: merged line **71% → 74%**, merged branch **50% → 57%** (`assert_merged_line_coverage_min.py` invocation). Docs: **`coverage-exclusions.md`**, **`CODE_COVERAGE.md`**, **`CURSOR_PROMPTS_SIX_QUALITY_IMPROVEMENTS.md`**, **`CORRECTNESS_QUALITY_ASSESSMENT_2026_04_15.md`**, **`QUALITY_ASSESSMENT_2026_04_14_WEIGHTED.md`**; script header comment. **`.gitignore`**: `_cov_merge/`. **`CreateRunIdempotencyConcurrencyIntegrationTests`**: parallel **12**, one **503** retry burst, explicit response dispose. **Stryker** baselines unchanged (**65.0**) — next step **`python scripts/ci/refresh_stryker_baselines.py`** then raise **`stryker-baselines.json`** in **3 pp** steps toward **75**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Product usability — Imp 2–6 (code): progressive nav, Advanced appsettings, CLI exit codes + `--json`, SSE run events, Help panel | **UI:** `nav-tier` / `nav-config` / `useNavProgressiveDisclosure`, `SidebarNav` + `MobileNavDrawer` tier filters, `ShellNav.test`. **API:** `appsettings.Advanced.json` + `Program.cs` chain; `AuthorityRunEventsController` SSE; **CLI:** `CliExitCode`, `CliExecutionContext` + leading `--json`, `TryConnectToApiAsync` / `ApiConnectionOutcome`, command exit mapping; `docs/CLI_USAGE.md`. **Help:** `HelpPanel` + `AppShellClient` / `KeyboardShortcutProvider` `onHelpRequested`. **Docs:** `docs/PILOT_GUIDE.md` Advanced config row; `archlucid-ui/.env.example` `NEXT_PUBLIC_DOCS_BASE_URL`. **`dotnet test ArchLucid.Cli.Tests`** green. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | GTM — Execute Imp 2–6 (15 docs + CI/CD examples) | Executed all 15 prompts from `CURSOR_PROMPTS_SAAS_IMPROVEMENTS_2_TO_6.md`. Created: **Imp 2** (`SLA_SUMMARY`, `BACKUP_AND_DR`, `OPERATIONAL_TRANSPARENCY`), **Imp 3** (`PRICING_PHILOSOPHY`, `TRIAL_AND_SIGNUP`, `ORDER_FORM_TEMPLATE`), **Imp 4** (`CUSTOMER_ONBOARDING_PLAYBOOK`, `CUSTOMER_HEALTH_SCORING`, `RENEWAL_EXPANSION_PLAYBOOK`), **Imp 5** (`INTEGRATION_CATALOG`, `SIEM_EXPORT`, `CICD_INTEGRATION` + `examples/` pipelines), **Imp 6** (`IDEAL_CUSTOMER_PROFILE`, `REFERENCE_NARRATIVE_TEMPLATE`, `PMF_VALIDATION_TRACKER`). Rescored SaaS assessment **37.6% → 46.1%**. Critical dims **12 → 8**. Archived prior to `docs/archive/…_PRE_IMP2_6.md`. |
| 2026-04-15 | GTM — SaaS assessment rescore + Imp 2–6 prompts | **`docs/MARKETABILITY_ASSESSMENT_2026_04_15_SAAS_ONLY.md`** rescored **34.8% → 37.6%** (post-Trust Center); archived prior as **`docs/archive/…_PRE_TRUST_CENTER.md`**. **`docs/CURSOR_PROMPTS_SAAS_IMPROVEMENTS_2_TO_6.md`** (15 prompts across 5 improvements). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | GTM — Trust Center spine (SaaS Improvement 1) | **`docs/go-to-market/TRUST_CENTER.md`**, **`SUBPROCESSORS.md`**, **`DPA_TEMPLATE.md`**, **`INCIDENT_COMMUNICATIONS_POLICY.md`**, **`SOC2_ROADMAP.md`**, **`TENANT_ISOLATION.md`**. Cross-links: **`docs/MARKETABILITY_ASSESSMENT_2026_04_15_SAAS_ONLY.md`**, **`docs/MARKETABILITY_ASSESSMENT_2026_04_15.md`**, **`docs/go-to-market/POSITIONING.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Docs — SaaS-only marketability assessment | **`docs/MARKETABILITY_ASSESSMENT_2026_04_15_SAAS_ONLY.md`** (SaaS-only weights, **~34.8%** weighted); cross-links from **`docs/MARKETABILITY_ASSESSMENT_2026_04_15.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Marketability Improvement 1 — Docker demo overlay + scripts | **`docker-compose.demo.yml`** (**`Demo:*`**, **`AgentExecution:Simulator`** on **`api`**). **`scripts/demo-start.ps1`**, **`demo-start.sh`**, **`demo-stop.ps1`**, **`demo-stop.sh`**. **`docs/go-to-market/DEMO_QUICKSTART.md`**, **`docs/go-to-market/screenshots/.gitkeep`**, **`README.md`** + **`docs/demo-quickstart.md`** + **`PRODUCT_DATASHEET.md`** + **`CONTAINERIZATION.md`** (Workflow **2a**) + **`SCREENSHOT_GALLERY.md`** cross-links. Removed stray **`ArchiForge.sln`** (canonical **`ArchLucid.sln`** only). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | archlucid-ui — usability handoff verification | **`governance-status-badge-class`** extracted + unit tests; removed Vitest-stalling **`governance-dashboard-page`** integration test (real **`fetch`** + heavy graph). **`data-testid="new-run-wizard-step-line"`** and **`NewRunWizardClient.test`** fixes (ambiguous **`/v1/architecture/request`** match, **30s** timeout). **`ShellNav.test`** extended timeout for parallel runs. **`governance/dashboard/page`**: **`useMemo`** for pending approvals; **`runs/page`**: removed unused **`totalPages`**. **`npm run test`** **344** passed; **`npm run build`** clean. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Six Cursor prompts — execute (archival cascade / outbox SLO / audit retry / tests / Stryker Coord / ProblemDetails guard) | **`066_GoldenManifestsFindingsSnapshots_ArchivedUtc`**, **`ArchLucid.sql`** brownfield **`ArchivedUtc`**; **`SqlRunRepository`** transactional cascade + rollback; **`DataArchivalOrphanProbeSqlIntegrationTests`** manifest/findings **`ArchivedUtc`**. **`DurableAuditLogRetry`**, **`ArchitectureRunCreateOrchestrator`**. Outbox: **`archlucid-slo-rules.yml`** recording + **`ArchLucidIntegrationEventOutboxConvergenceSlow`**, **`API_SLOS.md`**. **`stryker-config.persistence-coordination.json`**, **`stryker-scheduled.yml`**, **`stryker-baselines.json`**, **`TEST_STRUCTURE.md`**, **`MUTATION_TESTING_STRYKER.md`**. Tests: outbox multi-entry, coordinator zero-retention, **`DurableAuditLogRetryTests`**, **`ApiControllerProblemDetailsSourceGuardTests`** (**`Conflict()`** / **`BadRequest()`**). **`docs/DATA_CONSISTENCY_MATRIX.md`**, **`AUDIT_COVERAGE_MATRIX.md`**, **`COVERAGE_GAP_ANALYSIS_RECENT.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Correctness improvements 1/2/3/5/6 + throw-style Cursor rule | **`ManifestVersionIncrementRules`** extraction + FsCheck/edge tests; parallel **idempotency** SQL test; **fatal-exception** filters (**`CircuitBreakerGate`**, **`IntegrationEventPublishing`**, **`CircuitBreakerAuditBridge`**) + Core tests; **`DataArchivalOrphanProbeSqlIntegrationTests`**; **`AgentExecutionTraceRecorderRecordAsyncEdgeTests`**; **`.cursor/rules/SingleLineThrowNoBraces.mdc`**. **`docs/COVERAGE_GAP_ANALYSIS_RECENT.md`** bullet. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Schemathesis PR light — CLI v4 **`--phases`** | **`ci.yml`** **`api-schemathesis-light`**: **`--phases=examples`** (Schemathesis **v4** no longer accepts **`explicit`**; valid: **`examples`**, **`coverage`**, **`fuzzing`**, **`stateful`**). **`docs/API_FUZZ_TESTING.md`** PR-job note. Transient **`curl: (56)`** during health wait is separate (retries / container logs if persistent). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Live E2E JWT — governance actor (SoD) | **Local PEM JwtBearer** (`JwtSigningPublicKeyPemPath`): **`TokenValidationParameters.NameClaimType`** → short **`name`** when options default or empty (**`MapInboundClaims=false`** vs inbound JWT **`name`**). **`ActorContext.GetActor`**: **`User.FindFirst("name")`** fallback. **`ActorContextTests`**: **`GetActor_WhenOnlyShortJwtNameClaim_ReturnsClaimValue`**. Fixes Playwright **`live-api-journey`** self-approval ( **`RequestedBy`** aligned with **`liveAuthActorName`** ). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Six Cursor prompts — execute (coverage / ProblemDetails / RTM / versioning / fuzz / eval) | **Imp 1:** deferred in-repo (prior coverage work logged above); no new Persistence/API coverage files this pass. **Imp 2:** **`ApiControllerProblemDetailsSourceGuardTests`** (no bare **`NotFound()`** in **`ArchLucid.Api/Controllers`**). **Imp 3:** **`assert_rtm_test_filters_resolve.py`** + CI; **`V1_REQUIREMENTS_TEST_TRACEABILITY`** filter tokens aligned with real test types; orphan **`POST .../orphan-golden-manifests`** + **`POST .../orphan-findings-snapshots`** + SQL + audit **`GoldenManifestOrphansRemediated`** / **`FindingsSnapshotOrphansRemediated`**; **`AdminDataConsistencyOrphanRemediationEndpointsIntegrationTests`**. **Imp 4:** **`ApiVersioningResponseHeadersIntegrationTests`** (**`api-supported-versions`**). **Imp 5:** CI **`api-schemathesis-light`** **`--phases=examples`** (Schemathesis v4+ no longer supports **`explicit`**; use **`coverage`** / **`fuzzing`** / **`stateful`** for heavier phases in **`schemathesis-scheduled.yml`**). **Imp 6:** **`tests/eval-datasets/*`**, **`scripts/ci/eval_agent_quality.py`**, **`AgentEvalDatasetShapeTests`**. OpenAPI snapshot + NSwag client regen. Phase **7.5–7.8** still deferred. |
| 2026-04-15 | Six weighted improvements — full execute | **Imp 1:** `POST /v1/admin/runs/archive-by-ids`, `POST /v1/governance/approval-requests/batch-review`, `POST /v1/alerts/acknowledge-batch`; operator UI **`ColorModeToggle`** + root layout FOUC script + **`tailwind` `darkMode: 'class'`** + dark body styles. **Imp 2:** **`CompositeAlertDeduplicationKeyBuilderPropertyTests`** (FsCheck.Xunit) + **`RunRepositoryContractTests.ArchiveRunsByIds_*`**. **Imp 3:** Orphan comparison remediation already shipped; **Compliance**/**Cost** traces already carry **`AlternativePathsConsidered`**. **Imp 4:** **`adr/0006`** v2/deprecation section; integration schemas already require **`schemaVersion`**. **Imp 5:** **`assert_prompt_regression.py`** wired in **`ci.yml`** (baseline shape gate); **`ArchLucid:AgentOutput:QualityGate:Enabled`** remains default **true** in **`appsettings.json`**. **Imp 6:** **`FIRST_5_DOCS.md`**, **`CONTROLLER_AREA_MAP.md`**, **`CURSOR_PROMPTS_CANONICAL`** index table, **`START_HERE`** link. OpenAPI snapshot regen for new routes. Phase **1.13** doc batch; **7.5–7.8** still deferred. |
| 2026-04-15 | Weighted six improvements — handoff wrap | **`OpenApiContractSnapshotTests`**: committed **`openapi-v1.contract.snapshot.json`** updated for **`POST /v1/admin/diagnostics/data-consistency/orphan-comparison-records`** (and related doc/version-neutral metadata drift). **`dotnet test ArchLucid.Api.Tests -c Release`**: **695** passed. **`python scripts/ci/assert_prompt_regression.py`**: OK (shape check). Rename checklist **7.5–7.8** still deferred (no **`terraform state mv`** this session). |
| 2026-04-15 | Six improvements — continue session | **Imp 1:** (no new API tests this slice; prior **`AdvisoryControllerImprovementsIntegrationTests`** retained). **Imp 2:** **`DataConsistencyOrphanProbeHostedService`** — **`GoldenManifests`** + **`FindingsSnapshots`** orphan counts + metric **`table`** label; **`V1_REQUIREMENTS_TEST_TRACEABILITY`** orphan row. **Imp 3:** **`FindingExplainabilityNarrativeBuilder.Build(..., graphNodeLabels)`** + tests. **Imp 4:** **`docs/c4/workspace.dsl`** **`ApiComponents`** view (nested components). **Imp 5:** **`Rollback/R056–R060`**, **`assert_rollback_scripts_exist.py`** top **10**, CI step text; **`assert_legacy_config_sunset_not_passed.py`** + pytest + **`ci.yml`** guard; **`DATABASE_MIGRATION_ROLLBACK.md`**. **Imp 6:** **`IContentSafetyGuard`**, **`ContentSafetyOptions`**, **`NullContentSafetyGuard`** / **`ContentSafetyEnabledButUnconfiguredGuard`**, composition registration + **`ServiceCollectionExtensionsCompositionResolveTests`**; **`RateLimitingRoleMultiplierOptions`**, **`RateLimitingRolePartitionBuilder`**, role/IP partitions for **`fixed`**/**`expensive`**; **`SECURITY.md`** + **`API_CONTRACTS.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Weighted six improvements — sequential execute (session) | **Imp 1:** **`GraphSnapshotRelationalReadNoEdgesDirectSqlIntegrationTests`** (relational nodes, zero edges → early **`HydrateAsync`** path). **Imp 2:** **`ExplainabilityTraceMarkers.RuleBasedDeterministicSinglePathNote`** on **`FindingFactory`** topology/policy traces + **`ComplianceFindingEngine`**; narrative + factory + compliance tests. **Imp 3:** **`ArchLucid.Persistence/Migrations/Rollback/R061–R065`**, **`scripts/ci/assert_rollback_scripts_exist.py`** + pytest + **`ci.yml`** guard; **`docs/DATABASE_MIGRATION_ROLLBACK.md`** (path = **`Migrations/Rollback/`**). **Imp 4:** **`docs/c4/workspace.dsl`** + **`docs/c4/README.md`**, **`ARCHITECTURE_INDEX.md`** link. **Imp 5:** **`AgentOutputQualityGateOptions.Enabled`** default **`true`**. **Imp 6:** deferred (no bulk-archive API this pass). **`RunLifecycleStatePropertyTests`**: **`ArchitectureRunStatus.Executing`** → **`WaitingForResults`** (enum alignment). **`dotnet build ArchLucid.sln -c Release`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Weighted six improvements — sequential wrap | **Imp 1:** **`AdvisoryControllerImprovementsIntegrationTests`** (unknown run **`RunNotFound`**; committed run + missing **`compareToRunId`** → **`RunNotFound`**). **Imp 2:** **`docs/SECURITY.md`** § PII / conversation retention; **`ApiControllerAuthorizationGuardTests`** uses reflection-only HTTP verb detection (slim MVC refs). **Imp 3:** **`sql/rollbacks/061–065`** + **`docs/DATABASE_MIGRATION_ROLLBACK.md`**. **Imp 4:** **`assert_traceability_coverage.py`** path normalize fix (`.github/…` tokens). **Imp 5:** unchanged (narrative + alert already landed). **Imp 6:** **`docs/API_ERROR_CONTRACT.md`**, **`docs/CURSOR_PROMPTS_CANONICAL.md`**, **`API_CONTRACTS.md`** + **`PILOT_GUIDE.md`** **`/runs/new`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Weighted Improvements 3–6 — prompts + partial execute | **`docs/CURSOR_PROMPTS_WEIGHTED_IMPROVEMENTS_3_TO_6.md`** (verification **`rename-artifacts-verify-session`** + Imp **4–6** prompts). **Imp 4:** **`V1_REQUIREMENTS_TEST_TRACEABILITY.md`**, **`COMPARISON_RECORD_ORPHAN_REMEDIATION.md`**, orphan probe **LeftRunId** + **RightRunId** + metric tag **`column`**, Prometheus alert text. **Imp 5:** **`FindingExplainabilityNarrativeBuilder`**, **`NarrativeText`**, **`ArchLucidExplanationFaithfulnessFallbackTrend`**. **Imp 6:** RFC **9457** Problem Details on **`Admin`**, **`Jobs`**, **`Demo`** controllers; wizard remains **`/runs/new`** (parity prompt deferred). **`V1_SCOPE.md`** link to traceability. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 1 — coverage gap + Persistence + governance FsCheck | **`coverage-gap-report`**: **`dotnet reportgenerator`** merge from **`coverage-gap-1a/**`** (partial run after **`GreenfieldSqlBootIntegrationTests`** SQL flake) + **`python scripts/ci/coverage_gap_analysis.py`** → **`docs/COVERAGE_GAP_ANALYSIS.md`**. **`lowest-assembly-tests`**: **`GoldenManifestPhase1RelationalReadDirectSqlIntegrationTests`** — decisions without evidence/node links; provenance **applied-rules-only** slice. **`governance-workflow-fscheck`**: **`GovernanceWorkflowDryRunSubmissionPropertyTests`** (dry submit, no **`CreateAsync`/audit**). **`docs/COVERAGE_GAP_ANALYSIS_RECENT.md`**. **`dotnet test`** Decisioning dry-run filter green. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 2 — consolidated dev-bypass guards | **`AuthSafetyGuard.GuardAllDevelopmentBypasses`**: Production via **`IHostEnvironment.IsProduction`** or **`ARCHLUCID_ENVIRONMENT=Production`**; rejects **`ArchLucidAuth:Mode=DevelopmentBypass`** and **`Authentication:ApiKey:DevelopmentBypassAll=true`**. **`ArchLucid.Api/Program.cs`**: single call before **`AddArchLucidAuth`**; removed duplicate post-validation block. **`GuardDevelopmentBypassInProduction`** delegates to **`GuardAllDevelopmentBypasses`**. **`AuthSafetyGuardTests`**: +4 (**22** total **`ArchLucid.Host.Composition.Tests`** Release). **`docs/SECURITY.md`**. **`AUDIT_COVERAGE_MATRIX`** lists **0** open durable-audit gaps (audit-closure prompt N/A). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Improvement 2 prompts — re-verify (`dev-bypass-production-guard`, `rbac-policies-and-sensitive-controllers`) | **`AuthSafetyGuard.GuardDevelopmentBypassInProduction`** + **`Program`** ordering (**`GuardDevelopmentBypassInProduction`** before **`AddArchLucidAuth`**) confirmed; **`docs/SECURITY.md`** § DevelopmentBypass + RBAC aligned with **`ArchLucidRoles`** / **`ArchLucidPolicies`**. Spot-check: **`GovernanceController`**, **`AdminController`**, **`PolicyPacksController`**, **`AdvisorySchedulingController`**, **`AuditController`** (export → **`RequireAuditor`**). **`dotnet test ArchLucid.Host.Composition.Tests`** (**Release**): **18** passed. Run filtered suites **one at a time** (or full project) if parallel **`dotnet test`** hits **`CS2012`** file locks. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 1 — wrap (`coverage-gap-report` + governance) | Full **`ArchLucid.sln`** Coverlet + ReportGenerator → **`docs/COVERAGE_GAP_ANALYSIS.md`** (merged line **77.81%**, branch **63.35%**). **`docs/COVERAGE_GAP_ANALYSIS_RECENT.md`** + script merge for narrative section. **`GovernanceWorkflowTransitionConflictPropertyTests`** (FsCheck conflict + invalid **`SubmitApprovalRequestAsync`** pairs); **`GoldenManifestPhase1RelationalReadDirectSqlIntegrationTests`** JSON fallbacks; **`PromoteAsync`** manifest version mismatch fact; duplicate **`[Fact]`** fix. **`dotnet test`** `FullyQualifiedName~Governance` **33** passed. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Weighted Improvement 3 — execute (`archive-superseded-quality-assessments`) | Removed **`ArchiForge.sln`** (duplicate of **`ArchLucid.sln`**); **`git mv`** **`docs/QUALITY_ASSESSMENT.md`**, **`QUALITY_ASSESSMENT_2026_04.md`**, **`QUALITY_ASSESSMENT_2026_04_14.md`** → **`docs/archive/`**; short stubs at former paths; **`docs/CURSOR_PROMPTS_WEIGHTED_IMPROVEMENT_3.md`** (prompts: **`rename-artifacts-single-sln`**, **`rename-artifacts-no-stub-archiforge-src`**, **`legacy-config-sunset-constant`**, **`archive-superseded-quality-assessments`**); **`docs/archive/README.md`** table rows; cross-links updated. **`QUALITY_ASSESSMENT_2026_04_14_WEIGHTED.md`** Cognitive Load gap mitigated + Improvement 3 status extended. **`dotnet build ArchLucid.sln -c Release`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 3 prompts — rename debris + sunset | Removed duplicate **`ArchiForge.sln`**; deleted orphan **`ArchiForge/`** stub **`.cs`** files; **`ArchLucid.Api.http`** (was **`ArchiForge.Api.http`**). **`ArchLucidLegacyConfigurationWarnings.LegacyConfigurationKeysHardEnforcementNoEarlierThan`** = **`2027-07-01`** + **`docs/CONFIG_BRIDGE_SUNSET.md`** § Sunset timeline; warning log text references doc. **`dotnet build ArchLucid.sln -c Release`**. Cursor prompts: **`rename-artifacts-single-sln`**, **`rename-artifacts-no-stub-archiforge-src`**, **`legacy-config-sunset-constant`** in **`docs/QUALITY_ASSESSMENT_2026_04_14_WEIGHTED.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 1 prompts (coverage + FsCheck) — execute | **`GoldenManifestPhase1RelationalReadDirectSqlIntegrationTests`**: relational **decisions** (evidence + node links ordering) + **provenance** (graph nodes + applied rules only, no source-finding rows). **`GraphSnapshotRelationalReadDirectSqlIntegrationTests`**: relational **warnings** override + **EdgesJson** merge when **`GraphSnapshotEdgeProperties`** empty. **`FindingsSnapshotRelationalReadDirectSqlIntegrationTests`**: full relational **FindingRecords** + trace/child tables. **`ArchLucid.Application.Tests/Runs/RunLifecycleStatePropertyTests`**: FsCheck **`CommitRunAsync`** invariants (empty agent results when **ReadyForCommit**, **Failed**, **Created**). **`Suite=Core`** on the three direct-SQL integration classes. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | Improvement 2 prompts — verification run | **`dotnet test ArchLucid.Host.Composition.Tests`** (**Release**): **18** passed (**`AuthSafetyGuardTests`**, **`ArchLucidAuthorizationPoliciesRegistrationTests`**). **`docs/QUALITY_ASSESSMENT_2026_04_14_WEIGHTED.md`**: Improvement 2 marked implemented; Cursor prompts renamed to **`dev-bypass-production-guard`** and **`rbac-policies-and-sensitive-controllers`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | **`QUALITY_ASSESSMENT_2026_04_14_WEIGHTED`** — Improvement 2 (2a–2b) | **2a** **`AuthSafetyGuard.GuardDevelopmentBypassInProduction`** (`ArchLucid.Host.Core/Startup/AuthSafetyGuard.cs`): Production via **`ASPNETCORE_ENVIRONMENT`** or **`ARCHLUCID_ENVIRONMENT`**; called from **`Program`** before **`AddArchLucidAuth`**. **`ArchLucid.Host.Composition.Tests`**: **`AuthSafetyGuardTests`**. **2b** **`ArchLucid.Core/Authorization/`** **`ArchLucidRoles`** + **`ArchLucidPolicies`** ( **`RequireAuditor`**, **`ReadOnly`** alias); **`AddArchLucidAuthorizationPolicies`** on Host.Core; API **`AddArchLucidAuthorization`** delegates; **`RequireAuditor`** on audit export; policy pack + advisory scheduling mutations → **`AdminAuthority`**; **`ArchLucidAuthorizationPoliciesRegistrationTests`**. **`docs/SECURITY.md`**: DevelopmentBypass guard + RBAC table. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | **`QUALITY_ASSESSMENT_2026_04_14_WEIGHTED`** — Improvement 1 (`coverage-gap-report`, `lowest-assembly-tests`, `governance-workflow-fscheck`) | **`coverage-gap-report`** Merged Cobertura + **`scripts/ci/coverage_gap_analysis.py`** → **`docs/COVERAGE_GAP_ANALYSIS.md`**; **`coverlet.collector`** on **`ArchLucid.Architecture.Tests`**. **`lowest-assembly-tests`** Direct-SQL integration tests (**`SqlServerContainer`**) for **`FindingsSnapshotRelationalRead`**, **`GoldenManifestPhase1RelationalRead`**, **`GraphSnapshotRelationalRead`**. **`governance-workflow-fscheck`** **`GovernanceWorkflowSegregationAndPromotionPropertyTests`** (FsCheck + prod **`PromoteAsync`** facts); **6** tests passed filtered. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality improvement 3 — execute all six prompts | **3.1** `assert_k6_ci_smoke_summary.py` per-tag p95 (`_CI_SMOKE_TAG_CAPS`, `--per-tag-ci-smoke`); `ci.yml` `k6-ci-smoke` passes new flag. **3.2** `k6-ci-smoke` → native k6 (Grafana APT, matching `k6-smoke-api`); Docker run step replaced; `TEST_STRUCTURE.md` + `PERFORMANCE_TESTING.md` updated. **3.3** `/version` scenario + threshold in `ci-smoke.js`; `LOAD_TEST_BASELINE.md` scenario table. **3.4** `ci-smoke.js` + `soak.js` accept `ARCHLUCID_BASE_URL`; `tests/load/README.md` env matrix; `PERFORMANCE_TESTING.md` cross-link. **3.5** `scripts/ci/start_api_for_k6.sh` (create catalog + start API + wait ready); both k6 CI jobs refactored to call it. **3.6** `k6-soak-scheduled.yml` prints summary via `print_k6_summary_metrics.py`; `LOAD_TEST_BASELINE.md` soak section + alerting guidance. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality improvement 3 — Cursor prompt pack | **`docs/CURSOR_PROMPTS_QUALITY_IMPROVEMENT_3.md`**: inventory of **`ci-smoke.js`**, **`k6-api-smoke.js`**, **`k6-ci-smoke`** / **`k6-smoke-api`** jobs, assert script; six paste-ready prompts (**3.1** per-tag p95 gate, **3.2** native k6, **3.3** `/version`, **3.4** env docs, **3.5** optional composite stand-up, **3.6** soak docs). Cross-link from **`QUALITY_IMPROVEMENT_PROMPTS_2026_04_14.md`** § Prompt 3. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality improvement Prompt 2 — `dotnet test` | Filter **`AgentOutputSemanticEvaluatorTests` \| `AgentExecutionTraceRecorderReproTests` \| `AgentOutputEvaluationRecorderTests`** on **`ArchLucid.sln`** **Release**: **28** passed (**`ArchLucid.AgentRuntime.Tests`**). Re-confirms **`QUALITY_IMPROVEMENT_PROMPTS_2026_04_14.md`** Prompt 2 already complete. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Live E2E `live-api-journey` manifest link | **`archlucid-ui/e2e/live-api-journey.spec.ts`**: assert **`main`** contains **`runId`**, then locate golden manifest by **`a[href="/manifests/{id}"]`** (avoids **`getByRole('link', { name: guid })`** flaky accessible-name mismatch in Chromium CI). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality improvement 1 (mandatory trace forensics + eval seam) | Migration **`064_AgentExecutionTrace_InlineFallbackFailed.sql`**, **`InlineFallbackFailed`** on **`AgentExecutionTrace`**, **`GetByTraceIdAsync`** / **`PatchInlineFallbackFailedAsync`**, **`AgentExecutionTraceRecorder`** post-inline verification + **`AgentTraceInlineFallbackFailed`** audit; **`ISemanticScorer`** placeholder; contract + **`AgentExecutionTraceRecorderReproTests`**; **`AUDIT_COVERAGE_MATRIX`**, **`AGENT_TRACE_FORENSICS`**, **`AGENT_OUTPUT_EVALUATION`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality improvement Prompt 2 (2026-04-14 doc) — alignment | **`QUALITY_IMPROVEMENT_PROMPTS_2026_04_14.md`** Prompt 2 was already implemented (blob retries, **`BlobUploadFailed`**, semantic evaluator, API, docs, core tests). **Aligned** blob retry backoff to spec: **fixed 500 ms** between attempts (3 total) in **`AgentExecutionTraceRecorder.WriteBlobWithRetryAsync`**; **`docs/AGENT_TRACE_FORENSICS.md`** Reliability section updated. **`AgentOutputEvaluationRecorderTests.EvaluateAndRecordMetricsAsync_records_structural_and_semantic_histograms_for_eligible_trace`** (MeterListener). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Governance approve concurrency (live E2E) | **`GovernanceApprovalRequestRepository.TryTransitionFromReviewableAsync`**: **`SERIALIZABLE`** transaction + **`SELECT 1 … WITH (UPDLOCK, ROWLOCK)`** on reviewable row, then **`UPDATE`** (explicit **`dbo.`**, **`Status = @Draft OR Status = @Submitted`**). Contract test **`TryTransitionFromReviewableAsync_parallel_approves_only_one_wins`** (32 parallel calls). Targets Playwright **`live-api-concurrency.spec.ts`** “exactly one 2xx” expectation. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six-quality Prompt 2 — golden regression guard | **`CURSOR_PROMPTS_SIX_QUALITY_IMPROVEMENTS.md`** Prompt 2 was already largely implemented (model metadata sentinels on traces, **`GoldenAgentExecutionTraceTests`**, **`GoldenAgentResultJsonEvaluationTests`**). Added **`golden-agent-result-claim-without-evidence.json`** + **`Golden_claim_without_evidence_refs_lowers_semantic_score_relative_to_valid_fixture`** so CI fails if per-claim **`evidenceRefs`**/**`evidence`** is stripped while findings stay complete; **`docs/AGENT_OUTPUT_EVALUATION.md`** golden-set section documents the pair. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality improvement 1 — k6 operator-path CI smoke | **`tests/load/k6-api-smoke.js`**: ready (**`status: Healthy`**), version, create run, authority runs list; **`handleSummary`** + **`K6_SUMMARY_PATH`**. **`.github/workflows/ci.yml`** **`k6-smoke-api`**: native k6 after **`dotnet-full-regression`**, **`assert_k6_ci_smoke_summary.py`** (p95 ≤ 2000 ms, failed ≤ 1%), artifact **`k6-smoke-results`**. Docs: **`tests/load/README.md`**, **`PERFORMANCE.md`**, **`LOAD_TEST_BASELINE.md`**, **`PERFORMANCE_TESTING.md`**, **`TEST_EXECUTION_MODEL.md`**. **`.gitignore`**: `tests/load/results/k6-summary.json`. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality improvement 2 (2026-04-14 prompt) — verification | Forensic blob **3× retry** + **`BlobUploadFailed`** + **`archlucid_agent_trace_blob_upload_failures_total`** + semantic layer were already in product code; **`AgentExecutionTraceRecorderReproTests`**: **`RecordAsync_retries_blob_write_three_times_before_abandoning_failed_part`**, **`RecordAsync_when_each_blob_exhausts_retries_increments_archlucid_agent_trace_blob_upload_failures_total`** (MeterListener). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Quality prompts 1A–1D (agent forensics + eval hook + UI) | **`IAgentOutputTraceEvaluationHook`** / **`AgentOutputTraceEvaluationHook`**: post-execute **`EvaluateAndRecordMetricsAsync`** from **`ArchitectureRunExecuteOrchestrator`** (swallowed failures). **`GoldenAgentResultJsonEvaluationTests`** + fixtures. **`archlucid-ui`**: **`RunAgentForensicsSection`**, **`getRunTraces`** / **`getRunAgentEvaluation`**, run detail integration. Docs: **`AGENT_TRACE_FORENSICS.md`** (inline persistence, audit, histogram), **`OBSERVABILITY.md`**, **`AUDIT_COVERAGE_MATRIX.md`** (**`AgentTraceBlobPersistenceFailed`** row). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Live E2E — JWT parity (local PEM + CI mint) | **`ArchLucid.Api`**: **`JwtBearer`** local PEM loads **`RsaSecurityKey` from exported parameters** (avoid disposed **`RSA`**); **`MapInboundClaims = false`** for **`roles`** alignment; **`JwtLocalSigningWebAppFactory`** uses **`UseSetting`** so tests override **`appsettings.json`** **`DevelopmentBypass`**. **`ci.yml`** **`ui-e2e-live-jwt`** (subset, **`continue-on-error: true`**); **`live-e2e-nightly.yml`** **`live-e2e-nightly-jwt`**; **`docs/LIVE_E2E_JWT_SETUP.md`**; updates **`LIVE_E2E_HAPPY_PATH`**, **`LIVE_E2E_AUTH_PARITY`**, **`LIVE_E2E_AUTH_ASSUMPTIONS`**; **`playwright.live.config.ts`** forwards **`ARCHLUCID_PROXY_BEARER_TOKEN`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Live E2E — ApiKey parity (prompts 1–5) | **`docs/LIVE_E2E_AUTH_ASSUMPTIONS.md`**, **`LIVE_E2E_AUTH_PARITY.md`**; **`live-api-client.ts`** **`LIVE_API_KEY`** / **`LIVE_API_KEY_READONLY`**, **`liveJsonHeaders`**, governance raw helpers optional **`apiKey`**; **`live-api-apikey-auth.spec.ts`**; journey/negative specs use **`liveAuthActorName`** / **`livePeerReviewerActorName`**; **`ci.yml`** **`ui-e2e-live-apikey`**; **`live-e2e-nightly.yml`** DevelopmentBypass + ApiKey; **`LIVE_E2E_HAPPY_PATH`**, **`TEST_STRUCTURE`**, **`TEST_EXECUTION_MODEL`**, **`QUALITY_ASSESSMENT_2026_04_14`**, **`ARCHITECTURE_INDEX`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | CodeQL `cs/log-forging` — Application + API pipeline | **`LogSanitizer.Sanitize`** + **`IsEnabled`**/braces on user- or externally influenced structured log args in **`RunDetailQueryService`**, **`ApprovalSlaMonitor`**, **`ArchitectureRunExecuteOrchestrator`**, **`ArchitectureRunCreateOrchestrator`**, **`ArchitectureRunCommitOrchestrator`** (durable-audit warning), **`CoordinatorRunFailedDurableAudit`**, **`DefaultAgentEvaluationService`**, **`MermaidCliDiagramImageRenderer`**; **`ArchitectureApplicationService`** information **`if`** braces; **`PipelineExtensions`** (sanitize **`Method`**, **`IsEnabled`** on unhandled-exception log); **`WorkerHostPipelineExtensions`** **`IsEnabled`** on unhandled-exception log; **`HostLeaderElectionCoordinator`** lease-loss warning guard; **`Program.cs`** startup configuration error lines + pipeline start **`IsEnabled`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six prompts — residual doc + tests | **`LOAD_TEST_BASELINE.md`** / **`TEST_STRUCTURE.md`**: merge-blocking k6 summary assert; **`AGENT_TRACE_FORENSICS.md`**: sentinel table aligned to **`AgentExecutionTraceModelMetadata`**; **`RLS_RISK_ACCEPTANCE.md`**: concrete uncovered-table mirror of **`MULTI_TENANT_RLS.md`** §9; **`API_KEY_ROTATION.md`**: **`IOptionsMonitor`** reload (no restart when host refreshes config); **`TERRAFORM_STATE_MV_PHASE_7_5.md`**: inventory stamp **2026-04-14**; audit UI: **`role="status"`** + **`aria-live="polite"`** on result summary; **`GoldenAgentExecutionTraceTests`** + JSON fixtures; **`ExplainabilityTraceCompletenessAnalyzerPropertyTests`** (FsCheck); **`EXPLAINABILITY_TRACE_COVERAGE.md`** / **`AGENT_OUTPUT_EVALUATION.md`** cross-refs. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six quality prompts — verification | **`OpenApiContractSnapshotTests`** green after explainability DTO changes; **`docs/coverage-exclusions.md`** merged-line floor aligned to **71%**; filtered **`dotnet test ArchLucid.sln`** for **`RunExplanationSummaryServiceTests`**, **`CoordinatorRunFailedDurableAuditTests`**, **`TraceConfidenceLabelsPropertyTests`**, **`ExplainabilityTraceCompleteness`** (13 + 2 + 9 passed in respective assemblies). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six-prompt build follow-up | **`RunGoldenManifestConsistencyHealthCheck`**: `using ArchLucid.Persistence.Connections` for **`SqlTransientDetector`**. **`DataConsistencyOrphanProbeHostedService`**: **`Counter.Add`** with **`KeyValuePair<string, object?>`** tag (avoid **`TagList`** compile issue in Host.Core). **`ApiKeyAuthenticationHandlerTests`**: **`using Microsoft.Extensions.Logging`** for **`ILoggerFactory`**. **Release** **`dotnet build ArchLucid.sln`**; **139** **`ArchLucid.AgentRuntime.Tests`**; **677** **`ArchLucid.Api.Tests`**; **74** Vitest files / **340** tests. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | Six quality prompts (execution) | **1** Live E2E: `live-api-search-ask-graph.spec.ts`, `live-api-alert-rules.spec.ts` + `toRunGuidPathSegment` helper. **2** `IAgentOutputEvaluationHarness` / `AgentOutputEvaluationHarness` + Core tests. **3** `ApiKeyAuthenticationOptions` + `IOptionsMonitor` in `ApiKeyAuthenticationHandler` + rotation-style unit test. **4** Sonner `AppToaster`, `@/lib/toast`, governance dashboard toasts, `CollapsibleSection` on run detail (explanation + artifacts). **5** `RunGoldenManifestConsistencyHealthCheck`, `DataConsistencyOrphanProbeHostedService`, `archlucid_data_consistency_orphans_detected_total`, Prometheus alert, matrix + observability docs. **6** `NEXT_REFACTORINGS` triage note + §8 archive pointer. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (Six prompts — completion) | **Prompt 3:** `AgentOutputQualityGate` + `archlucid_agent_output_quality_gate_total`; `RunExplanationAggregateOptions` + deterministic aggregate fallback + `archlucid_explanation_aggregate_faithfulness_fallback_total`. **Prompt 4:** `GET /v1/authority/runs/{runId}/pipeline-timeline`, `IRunPipelineAuditTimelineService`, UI `AuthorityPipelineTimeline`. **Prompt 5:** `docs/runbooks/API_KEY_ROTATION.md`, `docs/security/RLS_RISK_ACCEPTANCE.md` + cross-links. **Prompt 6:** `CombinedFailureChaosTests`, `tests/load/soak.js`, `.github/workflows/k6-soak-scheduled.yml` + perf/chaos/test docs. OpenAPI snapshot updated. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Six quality improvements — wrap-up) | Cross-links: **`SECURITY.md`** → **`SYSTEM_THREAT_MODEL.md`**; **`ARCHITECTURE_CONSTRAINTS.md`** DDL smoke prose aligned with **`dbo.Runs`**; **`EXPLAINABILITY_TRACE_COVERAGE.md`** faithfulness section; **`OBSERVABILITY.md`** **`archlucid_explanation_faithfulness_ratio`**; **`LOAD_TEST_BASELINE.md`** keyset note; **`CAPACITY_AND_COST_PLAYBOOK.md`** per-tenant LLM tags; **`ARCHITECTURE_INDEX.md`** (coverage, Stryker ratchet, threat model, geo drill, explainability). **`TenantScopedTableDdlTests`**: match **`CREATE TABLE dbo.Runs`** + unbracketed column ids; **`ExplanationFaithfulnessCheckerTests`**: isolate rule-id token. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (Quality prompts + Phase 7.5 runbook) | Live E2E / k6 docs + CI k6 job; audit UI CSV + summary; **`ComparisonSummaryPersisted`** matrix; agent trace blob keys (**`053`**), **`AgentExecution:TraceStorage:PersistFullPrompts`**; pre-commit gate (**`054`**, **`ArchLucid:Governance:PreCommitGateEnabled`**, **`#governance-pre-commit-blocked`**); **`docs/runbooks/TERRAFORM_STATE_MV_PHASE_7_5.md`** for **`state mv`**. Item **7.5** → deferred with runbook link. **7.6–7.8** still deferred. |
| 2026-04-12 | (Session A — explainability + governance + traces) | **`IDeterministicExplanationService`** / **`DeterministicExplanationService`**; **`AgentExecutionTrace`** token counts + optional **`EstimatedCostUsd`** (**`LlmCostEstimationOptions`**, **`ILlmCostEstimator`**); **`GET …/approval-requests/{id}/rationale`** + **`IGovernanceRationaleService`**; lineage finding **`sourceAgentExecutionTraceId`**; Simulator trace recorder passes explicit null token counts; UI governance dashboard **Approve/Reject** with **`ConfirmationDialog`**; **`getting-started`** page + nav; **`appsettings.Development.json`** sample **`AgentExecution:LlmCostEstimation`**; OpenAPI snapshot + NSwag client regen. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Docs + governance + pipeline + quality gates) | **`GET /v1/governance/compliance-drift-trend`** + **`ComplianceDriftTrendService`**; promotion/approval **dev → test → prod** only (**`GovernanceEnvironmentOrder`**). **`AuthorityPipelineOptions`** + orchestrator **linked CTS** timeout + **`archlucid_authority_pipeline_timeouts_total`**. UI **`ComplianceDriftChart`** on governance dashboard. **`RunId`** value type (**`ArchLucid.Core.Identity`**) + tests. Docs: **`DEGRADED_MODE.md`**, **`START_HERE.md`** (reading order + tiers + degraded link), **`DATA_CONSISTENCY_MATRIX.md`** (replica lag + **`RunId`** note), **`archive/README.md`**, **`ARCHITECTURE_INDEX.md`** (archive section), **`API_FUZZ_TESTING.md`**, **`UI_COMPONENTS.md`**. CI: **`api-schemathesis-light`** in **`ci.yml`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (CI — Docker actions Node 24) | **`docker/setup-buildx-action@v4`**, **`docker/build-push-action@v7`** in **`ci.yml`** (docker-build-smoke, ZAP), **`cd.yml`**, **`cd-staging-on-merge.yml`** — official Docker actions ship Node **24** runtimes (runner **≥ v2.327.1**), clearing Node **20** deprecation noise under **`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Greenfield SQL boot — quality improvement #1) | **`GreenfieldSqlApiFactory`**, **`GreenfieldSqlBootIntegrationTests`** (`ArchLucid.Api.Tests`, **`Xunit.SkippableFact`**); CI **`api-greenfield-boot`** after **`dotnet-fast-core`**; **`dotnet-full-regression`** **`needs`** **`api-greenfield-boot`**. Docs: **`TEST_EXECUTION_MODEL.md`**, **`TEST_STRUCTURE.md`**, **`QUALITY_ASSESSMENT_2026_04.md`** supplement. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Execute → `ReadyForCommit` on `dbo.Runs`) | **`ArchitectureRunExecuteOrchestrator`**: after persist (and on idempotent execute when results already exist), **`LegacyRunStatus` → `ReadyForCommit`** when Topology/Cost/Compliance/Critic each have exactly one result — fixes live Playwright **`waitForReadyForCommit`** timeouts (ADR-0012 had stopped updating the header). Unit test **`ExecuteRunAsync_after_persist_sets_LegacyRunStatus_ReadyForCommit_when_four_required_agents_complete`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (Storage provider composition tests) | **`InMemoryStorageProviderRegistrar`** calls **`RegisterHotPathReadCaching`** (parity with Sql + **`StorageProviderRegistrationParityTests`**). Parity allowlist: **`IOptionsChangeTokenSource`/`IConfigureOptions` for `SqlServerOptions`**. **`StorageProviderDiGraphValidationTests`**: **`AddHttpContextAccessor`**, no-op **`IAuditService`** (registered in **`Program.cs`**, not composition). Filtered **`StorageProvider`** tests: **4 passed** in ~0.2s after build. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (CodeQL `cs/log-forging` — Host.Core) | **`LogSanitizer.Sanitize`** on structured log args derived from requests, queue payloads, Service Bus messages, and **`ex.Message`** in **`ComparisonReplayApiService`**, **`ArchitectureApplicationService`**, **`WorkerHostPipelineExtensions`**, **`BackgroundJobQueueProcessorHostedService`**, **`InMemoryBackgroundJobQueue`**, **`AzureServiceBusIntegrationEventPublisher`/`Consumer`**, **`CircuitBreakerAuditBridge`**, **`AuthorityPipelineWorkProcessor`**, **`HostLeaderElectionCoordinator`**, **`LoggingIntegrationEventHandler`**, **`AskService`**. Braces on **`ComparisonReplayApiService`** information log **`if`**. **`dotnet build ArchLucid.Host.Core` (Release)** OK. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (Host.Composition coverage + CPM) | **`ServiceCollectionExtensionsCompositionResolveTests`**: build **`ServiceProvider`** after **`AddArchLucidApplicationServices`** (Real Azure + optional fallback LLM) to cover composition lambdas; **`IConfiguration`** via **`AddSingleton(typeof(IConfiguration), configuration)`**; **`ArchLucid.Core.Tests`**: drop bogus **`Microsoft.Extensions.Configuration.Memory`** reference (**`AddInMemoryCollection`** lives on **`Microsoft.Extensions.Configuration`**). **`docs/BUILD.md`** note corrected. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (CLI `trace` command) | **`archlucid trace <runId>`**: **`TraceCommand`** uses **`GetRunAsync`** (same as **`status`**, **`GET v1/architecture/run/{runId}`**), reads **`run.otelTraceId`**, **`ARCHLUCID_TRACE_VIEWER_URL_TEMPLATE`** (+ optional **`ARCHLUCID_TRACE_OPEN_BROWSER`**). **`RunInfo.OtelTraceId`** on CLI client DTO. Tests: **`TraceCommandTests`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (run detail `OtelTraceId`) | **`ArchitectureRun.OtelTraceId`** + **`RunRecordToArchitectureRunMapper`**; run detail JSON already includes **`run.otelTraceId`** via **`RunRecord`**. UI: **`Creation trace`** + **`RunTraceViewerLink`** on **`runs/[runId]/page.tsx`**; **`RunDetail.run.otelTraceId`** in **`authority.ts`**. OpenAPI snapshot: **`otelTraceId`** on **`RunRecord`** + **`ArchitectureRun`**; **`RunDetailDto`** remarks → regenerate with **`ARCHLUCID_UPDATE_OPENAPI_SNAPSHOT=1`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (audit retention docs) | **`docs/AUDIT_RETENTION_POLICY.md`** — hot/warm/cold tiers, Migration **051**, **`GET /v1/audit/export`** workflow (`curl`), regulatory notes, future archival/Terraform. **`CHANGELOG.md`**, **`AUDIT_COVERAGE_MATRIX.md`** (read-path table), **`ARCHITECTURE_INDEX.md`** cross-links. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (audit export API) | **`GET /v1/audit/export`**: JSON + **`text/csv`** (**`AuditEventCsvFormatter`**, CSV **`Content-Disposition`** via formatter + **`HttpContext.Items`**); **`IAuditRepository.GetExportAsync`** (Dapper + in-memory, **`TOP`/`ORDER BY OccurredUtc ASC`**); controller + repo clamp **`maxRows`** 1–10,000; **`[EnableRateLimiting("expensive")]`**; validate **`fromUtc` < `toUtc`** and range ≤ 90 days. Tests: **`AuditExportControllerTests`**, **`AuditEventCsvFormatterTests`**, audit repo contract additions; **`openapi-v1.contract.snapshot.json`** regenerated. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (policy packs E2E) | **`policy-packs/page.tsx`**: after create + `load()`, **`setSelectedPackId(created.policyPackId)`** so Publish is not gated on `useEffect` races. **`e2e/policy-packs-mock-state.ts`**: **`listMockVersions`** returns full version row (**`policyPackVersionId`**, **`createdUtc`**, **`policyPackId`**) so the UI does not throw on **`.slice`**. **`e2e/policy-packs-journey.spec.ts`**: **`test.setTimeout(120_000)`** so expects are not capped by the default 30s test timeout. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (doc/DDL header alignment) | **`ArchLucid.sql`** header — DOCUMENTATION line: "run header model" (not "two Run tables"); CONTENT OVERVIEW — note authority is **`dbo.Runs`** after **049**. **`docs/GLOSSARY.md`** — remove legacy **`dbo.ArchitectureRuns`** sentence. |
| 2026-04-12 | (drop **`ArchitectureRuns`**) | Migration **`049_DropArchitectureRunsTable.sql`**; **`ArchLucid.sql`** — remove **`dbo.ArchitectureRuns`**; delete **`IArchitectureRunRepository`** + Dapper/in-memory impls + contract tests; remove dual-persistence health checks + options + API tests; remove CI **`assert_architecture_run_write_pragma`** step/scripts; **`CoordinatorService`** / **`DemoSeedService`** no legacy insert; **`ArchitectureCommitTestSeed`** → **`dbo.Runs`**; planning link repo validates **`dbo.Runs`**. Docs: **ADR 0012** completed, **ADR 0002** superseded, **`DATA_CONSISTENCY_MATRIX`**, **`ARCHITECTURE_COMPONENTS`**, **`DATA_MODEL`**, **`TRUSTED_BASELINE`**, **`API_CONTRACTS`**, **`SQL_DDL_DISCIPLINE`**, runbooks. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (analysis + commit integration fixes) | **`ArchitectureAnalysisService`**: load manifest when **`CurrentManifestVersion`** is empty using **`v1-{runId}`** (matches **`RunDetailQueryService`** / first-commit convention). **`ArchitectureRunCommitOrchestrator`**: **`ConflictException`** (409) when no agent results; run that check **before** evidence-package prerequisite so commit-before-execute is not masked by 400. Tests: **`ArchitectureAnalysisServiceCanonicalPreloadTests`** (null version + preloaded manifest). **`DATA_CONSISTENCY_MATRIX`**: row-reconciliation note for **`LegacyRunStatus`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (runs read path / ADR-0012) | Migration **`048_RunsLegacyReadAlignment.sql`** + **`ArchLucid.sql`** — **`dbo.Runs`** **`ArchitectureRequestId`**, **`LegacyRunStatus`**, **`CompletedUtc`**, **`CurrentManifestVersion`**; **`IRunRepository.ListRecentInScopeAsync`**; **`RunDetailQueryService`** / **`RunsController`** / orchestrators / **`AuthorityPipelineWorkProcessor`** read **`dbo.Runs`**; **`CoordinatorService`** patches authority header after coordination; **`ContextIngestionRequest.ArchitectureRequestId`**. Docs: **ADR 0012** read section, **`DATA_CONSISTENCY_MATRIX`**. Tests: run repo contract, **`RunRecordArchitectureRunMappingParityTests`**, updated service tests. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-12 | (dual persistence row reconciliation) | **`DualPersistenceRowReconciliationHealthCheck`** + **`DualPersistenceRowReconciliationOptions`** (`HealthChecks:DualPersistenceRowReconciliation`); **`DualPersistenceRowReconciliationRow`** probe DTO; Composition registers **`dual_persistence_row_reconciliation`** alongside count-based check. Docs: **`DATA_CONSISTENCY_MATRIX.md`**. Tests: **`DualPersistenceRowReconciliationHealthCheckTests`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-10 | Operator UI a11y baseline | **`archlucid-ui`:** `@axe-core/playwright` + `e2e/helpers/axe-helper.ts` + `e2e/accessibility.spec.ts` (Home, Runs, Audit, Policy packs, Alerts) — CI gates **critical/serious** axe violations via existing `ui-e2e-smoke`. Root **`ACCESSIBILITY.md`** (WCAG 2.1 AA target). Fixes: **`.workflow-inline-link`** underline + first-run footer text contrast; **Runs** pagination disabled labels **#64748b** + **`aria-disabled`**. **`policy-packs-mock-state`** `createMockPack` body types align with POST JSON. **Phase 7.5–7.8** unchanged (deferred). |
| 2026-04-10 | Phase 7 (docs + infra naming) | **Doc/infra sweep:** `docs/**` + project READMEs → ArchLucid naming; **`ArchLucid.sql`** references; Terraform **`archlucid_api_backend_url`** (replaces `archiforge_api_backend_url`); private endpoints **`pe-archlucid-*`**; sample **`terraform.tfvars.example`** defaults (APIM, Entra, edge, container-apps, SQL failover); **CI** rename-guard **echo** copy; **`codeql.yml`** UI path comment; **`ArchLucid.Worker`** **`launchSettings.json`** profile; **`.gitignore`** + **`git rm --cached`** **`_docker_publish_*`**, **`_docker_r2r_noflag`**, **`artifacts-bench/results/`**. **Unchanged:** migrations **001–043** file bodies, RLS **DB** identifiers, Terraform **resource addresses** (`azurerm_api_management.archiforge`, `grafana_folder.archiforge`, …), allowlist **filenames**. **Deferred:** 7.5–7.8. |
| 2026-04-10 | (authority pipeline OTel) | **`AuthorityPipelineStagesExecutor`**: stage activities parented to **`ctx.RunActivity`**; tags **`archlucid.run_id`**, **`archlucid.stage.name`**; histogram **`archlucid_authority_pipeline_stage_duration_ms`** (`stage`, `outcome`); error status + **`error.type`** on failure. **`ArchLucidInstrumentation`**. Tests: **`AuthorityPipelineStagesExecutorTests`** (`Suite=Core`). Docs: **`BACKGROUND_JOB_CORRELATION.md`** §10, **`ARCHITECTURE_FLOWS.md`**, **`OBSERVABILITY.md`**, **`TEST_STRUCTURE.md`**. **Verified:** `dotnet test` filter **`AuthorityPipelineStagesExecutorTests`** (4 passed). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-08 | Repo / release surface | **`docs/REPO_HYGIENE.md`** (committed vs generated, `artifacts/`, NSwag `Generated/` policy); root **README** + **START_HERE** + **PILOT_GUIDE** + **RELEASE_LOCAL** cross-links; **CHANGELOG** 56R/57R bullets aligned with **`archlucid`** CLI and **`mock-archlucid-api-server.ts`**; `.gitignore` root transcript patterns + NSwag path comment; **`git rm --cached`** root `*.txt` logs (files may remain locally). Phase **7.5–7.8** unchanged. |
| 2026-04-08 | Phase 7 (finish) | **Docs + tooling:** runbooks (SLO/Redis/provenance/migration/secret/load), **`docs/terraform-azure-variables.md`**, **`archlucid-ui`** README + operator docs, **`.devcontainer`**, **`api-synthetic-probe.yml`**. **`package-release.ps1`** + **`Write-ReleasePackageArtifacts.ps1`** → **ArchLucid.Api** / **`ArchLucid.*`** package kinds. Sample **`appsettings.*`** → **`api://archlucid-api`**, Key Vault secret name examples **`archlucid-*`**, `_docker_*` parity. **Terraform:** Phase **7.5** comment headers on module **`main.tf`** + **`apim.tf`**; human-facing **defaults** (`law-archlucid-ca`, `cae-archlucid`, budgets, VNet, Grafana, Entra **`api://archlucid-api`**). **`scripts/assert_context_ingestion_di_guards.py`** → **ArchLucid.Host.Composition**. C#/TS **`archiforge`** grep guards: **zero** product hits; allowlists comment-only. **Deferred:** checklist **7.5–7.8**. |
| 2026-04-09 | (hot-path cache / archival) | Reviewed **`IHotPathReadCache`** usage (**runs** `GetById`, **golden manifest** `GetById`, **policy pack** `GetById` only; lists/artifacts/alerts not cached). **`ArchiveRunsCreatedBeforeAsync`** now returns **`RunArchiveBatchResult`** with **`OUTPUT`** keys; **`CachingRunRepository`** evicts each archived run. Docs: **`DATA_CONSISTENCY_MATRIX.md`**, **`REDIS_AND_MULTI_REGION.md`**. Tests: **`CachingRunRepositoryArchiveInvalidationTests`**. Phase 7 unchanged. |
| 2026-04-08 | Phase 7 (code) | Removed config/auth/env/OIDC/CLI bridges; **`ArchLucid.sql`** + migrations **`044`**/**`045`**; Docker/compose **`archlucid-*`**; CLI **`archlucid`/`archlucid.json`**; integration events canonical-only; **`BREAKING_CHANGES.md`**; CI **`ARCHLUCID_SQL_TEST`**; C#/TS rename guards clean without broad allowlists. **Deferred:** Terraform **`state mv`** (7.5), repo/Entra/workspace renames (7.6–7.8). |
| 2026-04-08 | (list pagination) | **`GET …/authority/projects/{projectId}/runs`**: optional **`page`**/**`pageSize`** → **`PagedResponse&lt;RunSummaryResponse&gt;`**; legacy **`take`** + array unchanged. **`IRunRepository.ListByProjectPagedAsync`**, SQL **`OFFSET`/`FETCH`** + count. Operator UI **Runs** (paged + nav) and **Alerts** (**`listAlertsPaged`**, 25/page). Vitest **`coerceRunSummaryPaged`**. **`docs/API_CONTRACTS.md`**. Regenerate OpenAPI contract snapshot when CI requires. Phase 7 unchanged. |
| 2026-04-09 | (support bundle) | **`README.txt`** + **`manifest.json`** **1.1** (`triageReadOrder`, **`archlucidJsonPath`** / present); **`api-contract.json`** bounded **`GET /openapi/v1.json`**; **`GetBoundedUtf8BodyAsync`** on **`ArchLucidApiClient`**. **`SupportBundleTriageCatalog`**, **`SupportBundleReadme`**. Docs: **`TROUBLESHOOTING.md`**, **`CLI_USAGE.md`**, **`PILOT_GUIDE.md`**, **`CHANGELOG.md`**. Tests: **`SupportBundleTests`**, **`SupportBundleHealthProbePathsTests`**. Phase 7 unchanged. |
| 2026-04-09 | (correlation / error surfacing) | API **`application/problem+json`** includes **`correlationId`** (and **`InvalidModelStateResponseFactory`** validation errors); **`ApplicationProblemMapper`** + global exception handler use **`ProblemCorrelation.Attach`**. UI proxy **502/413/503/429** JSON + **`X-Correlation-ID`**; **`buildApiRequestErrorFromParts`** falls back to body **`correlationId`**. Docs: **`API_CONTRACTS.md`**, **`TROUBLESHOOTING.md`**, **`operator-shell.md`**; CLI support-bundle reference + **`CliOperatorHints`**. Tests: mapper/filter assertions; Vitest updates. Phase 7 unchanged. |
| 2026-04-08 | (replay / compare / export data) | **`GetRunDetailAsync`** loads **artifact bundles by golden manifest id** even when **`ArtifactBundleId`** on the run is null (Dapper + in-memory query services). **Run compare** adds a **GoldenManifestId** run-level diff when commits differ; **`GET …/authority/compare/manifests`** returns **409 Conflict** (problem details) when both manifests exist but stored scopes disagree. Tests: query service bundle hydration, compare runs asymmetry, **`AuthorityCompareController`** unit test. **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-09 | (diagram export quality) | **`IDiagramImageRenderer`** moved to **`ArchLucid.Core`** (`Core/Diagrams/`) to avoid **Persistence → ArtifactSynthesis → Application** cycle. **`DocxExportService`** injects it: Mermaid artifacts rasterize to PNG when renderer returns bytes; **`MermaidCliDiagramImageRenderer`** logs and returns **null** on CLI failure. **`ArchLucid:MermaidCli:Enabled`** selects **Mermaid CLI** vs **`NullDiagramImageRenderer`**. Analysis DOCX usings updated to **Core.Diagrams**. Tests: **`DocxExportServiceGoldenTests`** (+ mock PNG). **`appsettings.json`**, **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-09 | (exports / packages) | **`DocxExportService`**: architecture diagram section uses **PNG artifact** (base64), else **Mermaid** from synthesized diagram artifact, else explicit empty + manifest counts; removed **DiagramPlaceholderBytes**. **`WordDocumentBuilder.AddMonospaceSourceLines`**. **`ArtifactPackagingService.BuildRunExportPackage`** + **`RunExportReadmeContext`**; **`ArtifactExportController`** passes manifest name/hash/rule set into **README.txt**. Tests: **`DocxExportServiceGoldenTests`**, **`ArtifactPackagingServiceTests`**. **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-08 | (UI nav / V1 discoverability) | **`ShellNav`**: group label **Start here · runs & review**, **`New run`**, **Compare two runs** / **Replay a run**, nav **`aria-label`** **Primary operator workflows**. **Home**: **Typical V1 path** callout + quick links map to header groups. **`OperatorFirstRunWorkflowPanel`**: clearer primary labels; **Hide checklist** / **Show V1 workflow checklist**. **Onboarding** link text aligned. Tests: **`ShellNav`**, **`HomePage`**, **`OperatorFirstRunWorkflowPanel`**. **`docs/operator-shell.md`**. Phase 7 unchanged. |
| 2026-04-08 | (UI operator states) | **`archlucid-ui`**: **`OperatorTryNext`** + consistent **Try next:** copy on **runs** (malformed outside callout), **graph** (load/malformed/viewer/dynamic loading), **compare** (legacy/golden/AI fail + malformed), **replay**, **manifest** summary/artifacts; **alerts** loading vs empty vs failure; **planning** refresh uses **`OperatorLoadingNotice`** + failure/empty next steps. **`docs/operator-shell.md`** — empty/loading/error pattern. Vitest **`OperatorShellMessage.test.tsx`**. Phase 7 unchanged. |
| 2026-04-08 | (UI first-run) | **`archlucid-ui`**: **`OperatorFirstRunWorkflowPanel`** on Home; improved empty states (**runs** list, **run detail** pre-manifest, **replay**); **new run** callout; **onboarding** links Home; Vitest **`OperatorFirstRunWorkflowPanel.test.tsx`**; **`README.md`**. Phase 7 unchanged. |
| 2026-04-08 | (docs) | **`docs/DEPLOYMENT_RUNBOOK.md`** — practical failed-deploy / rollback for Container Apps + GitHub CD; link from **`DEPLOYMENT.md`**. Phase 7 unchanged. |
| 2026-04-08 | (CD post-deploy) | **`scripts/ci/cd-post-deploy-verify.sh`**: structured checks + logs + `::error::`; **`/health/ready`** must report **`Healthy`**; **`/version`** + OpenAPI; optional **`CD_POST_DEPLOY_*`** retries. **`cd.yml`** / **`cd-staging-on-merge.yml`**; test contract in **`HealthEndpointSecurityIntegrationTests`**. Docs: **`DEPLOYMENT_CD_PIPELINE.md`**, **`RELEASE_SMOKE.md`**, **`ARCHITECTURE_ON_A_PAGE.md`**. Phase 7 unchanged. |
| 2026-04-08 | (CD / docs) | **`.github/workflows/cd.yml`** + **`cd-staging-on-merge.yml`**: deploy **worker** Container App with same **`archlucid-api:<tag>`** image as API; optional rollback deactivates worker revision too; staging smoke aligned with manual CD (**`/health/ready`**, **`/openapi/v1.json`**). Docs: **`DEPLOYMENT_CD_PIPELINE.md`**, **`DEPLOYMENT.md`**, **`CONTAINERIZATION.md`**, **`DEPLOYMENT_TERRAFORM.md`**, **`RELEASE_SMOKE.md`**, **`ARCHITECTURE_ON_A_PAGE.md`**, **`infra/terraform-container-apps/README.md`**. Phase 7 unchanged. |
| 2026-04-08 | (docs / V1 scope) | **`docs/V1_DEFERRED.md`** — doc-sourced inventory of **V1.1+ / deferred / exploratory** items (59R planning bridge, **AUDIT_COVERAGE_MATRIX** known gaps, Phase 7 rename reference, **CONTAINERIZATION** org gaps, **NEXT_REFACTORINGS** as maintainer backlog). Cross-links from **`V1_SCOPE.md`**, **`ARCHITECTURE_INDEX.md`**. Phase 7 unchanged. |
| 2026-04-08 | (CI guard) | C# rename guard: allowlist **`RlsArchLucidScopeIntegrationTests.cs`** for Phase 7 **`rls.ArchiforgeTenantScope`** policy name in SQL/doc/class id; **`SqlConnectionHealthCheck`** primary ctor param **`archiForgeOptions` → `archLucidOptions`** (avoids case-insensitive `archiforge` hit). |
| 2026-04-08 | (OTel + metrics + CLI + UI env + CI guards) | **String literal cleanup:** OTel activity tags `archlucid.*`, metrics `archlucid_*` (`ArchLucidInstrumentation`, Prometheus/Grafana/Azure monitor TF, docs). CLI usage `archlucid`; exports/ProblemDetails/CloudEvents/queue defaults/webhook defaults/temp paths → ArchLucid. **Support bundle** JSON `hasArchlucidJson` / `archlucidAndDotnetEnvironment` + legacy read-only props; **CLI/API env** `ARCHLUCID_*` first with legacy fallbacks; **Backfill** `ARCHLUCID_SQL` bridge. **UI:** `legacy-arch-env.ts`, `fetchArchLucidJson` / `askArchLucid`, `ArchLucidRole`, mock server rename, proxy/config/auth env bridges, `SupportBundleRedactor` includes `ARCHLUCID_*` keys. **CI:** case-insensitive C# grep + TS/TSX guard + `archiforge-rename-allowlist-ts.txt`. **Contracts:** `archlucid-asyncapi-2.6.yaml`, schema descriptions. Phase 7 config/OIDC/SQL filenames unchanged. |
| 2026-04-08 | (schema) | **`043_ArtifactBundles_LegacyJsonNullable.sql`** + **`ArchiForge.sql`**: `ArtifactsJson` / `TraceJson` on **`dbo.ArtifactBundles`** nullable (dual-write / relational-primary rows). **`PersistenceContractSupplement.sql`**: align **`TraceJson`** with existing **`ArtifactsJson`** nullability for integration tests. Phase 7 unchanged. |
| 2026-04-08 | (LLM resilience) | **Per-call Polly retry inside CB decorators** (`LlmCallResilienceDefaults`, `AgentExecution:Resilience` LLM keys); OTel `archlucid_llm_call_retries_total`; Simmy tests + `LlmCallChaosEndToEndTests`; scheduled **`.github/workflows/simmy-chaos-scheduled.yml`**; **`docs/LLM_RETRY_AND_CIRCUIT_BREAKER.md`**. Docker sample `appsettings` Resilience keys aligned. Phase 7 unchanged. |
| 2026-04-08 | (auditability) | **`docs/AUDIT_COVERAGE_MATRIX.md`** + CI guard; **`IAuditRepository.GetFilteredAsync`** + `GET /v1/audit/search` + `GET /v1/audit/event-types`; **`CircuitBreakerAuditEntry`** / optional **`CircuitBreakerGate`** callback + **`CircuitBreakerAuditBridge`** DI; operator UI **`/audit`** + **`ShellNav`**. OpenAPI snapshot regen. Phase 7 unchanged. |
| 2026-04-08 | (explainability) | **`ExplainabilityTraceCompletenessAnalyzer`** + advisory `ResultJson.traceCompleteness` + OTel histogram `archlucid_explainability_trace_completeness_ratio`; engine trace fills (policy/requirement/topology coverage, rules/notes on baseline/cost/requirement/compliance). UI: `ProvenanceGraphDiagram` on coordinator provenance page. **`docs/EXPLAINABILITY_TRACE_COVERAGE.md`**. Phase 7 unchanged. |
| 2026-04-08 | (integration events) | **Single publish entry point:** `AuthorityRunOrchestrator` uses `OutboxAwareIntegrationEventPublishing.TryPublishOrEnqueueAsync` for `com.archlucid.authority.run.completed` (transactional enqueue when enabled + `SupportsExternalTransaction`). CI grep + `DependencyConstraintTests` guard direct `IIntegrationEventPublisher.PublishAsync` except `IntegrationEventPublishing` / `IntegrationEventOutboxProcessor`. **JSON Schema** catalog under `schemas/integration-events/`; `IntegrationEventPayloadContractTests` assert payloads against committed schemas. Docs: `INTEGRATION_EVENTS_AND_WEBHOOKS.md`. Phase 7 unchanged. |
| 2026-04-08 | Phase 6.4 cleanup | **String literal rename sweep (~60 replacements):** API/Swagger/Worker startup messages, agent prompts, export/report headings, CLI operator output, decisioning manifest labels, artifact packaging, XML doc comments, validation + Swagger auth description text (new key first, legacy `ArchiForge*` named where still valid). Tests + temp prefixes aligned. **CI:** `Guard — unexpected ArchiForge string literals in C#` after `dotnet build` in `dotnet-fast-core`; allowlist = Phase 7 bridges, `ArchiForge.sql`, connection string / config key literals, test factories. Remaining `ArchiForge` in `.cs` = Phase 7 / compatibility only. |
| 2026-04-07 | (resilience) | **External resilience + CB metrics:** named `CircuitBreakerOptions` (`AzureOpenAI:CircuitBreaker:Completion` / `:Embedding` + shared fallback); `IOptionsFactory` snapshot at keyed `CircuitBreakerGate` construction; OTel counters `archiforge_circuit_breaker_*` on the gate; `Persistence:SqlOpenResilience` for SQL open retries; CLI `httpResilience` in `archiforge.json`. **`docs/RESILIENCE_CONFIGURATION.md`**. Tests: `CircuitBreakerGateMetricsTests`, `SqlOpenResilienceOptionsTests`, CLI handler/options. **`IntegrationEventOutboxProcessorCorrelationTests`** filter activities by `archiforge.outbox_id` so parallel `Suite=Core` runs do not flake. Phase 7 deferred. |
| 2026-04-08 | (CI) | **CodeQL JavaScript job:** `source-root` and npm paths use `ARCHLUCID_UI_DIR=archlucid-ui` (post–Phase 3 rename); verify step fails fast if the folder is missing. |
| 2026-04-08 | (integration events) | **Transactional outbox for all 6 event types** via `OutboxAwareIntegrationEventPublishing`; `IIntegrationEventOutboxRepository.EnqueueAsync` takes `Guid? runId`. **Canonical** `com.archlucid.*` type strings + legacy map (`IntegrationEventTypes`). **Worker** `AzureServiceBusIntegrationEventConsumer` + `LoggingIntegrationEventHandler`; `IntegrationEventsOptions` consumer settings. **Terraform** `infra/terraform-servicebus/`. Docs: `INTEGRATION_EVENTS_AND_WEBHOOKS.md`, AsyncAPI. Tests: outbox-aware, contract payloads, governance outbox path, `LoggingIntegrationEventHandler`. |
| 2026-04-07 | (observability) | **Background job correlation:** new `ActivitySource`s `RetrievalIndexingOutbox`, `IntegrationEventOutbox`, `DataArchival` + OTel registration; `RetrievalIndexingOutboxProcessor`, `IntegrationEventOutboxProcessor`, `DataArchivalCoordinator` set `correlation.id` + Serilog `CorrelationId`; `AdvisoryScanRunner` / `AuthorityRunOrchestrator` push `LogContext`. **`docs/BACKGROUND_JOB_CORRELATION.md`**. Persistence tests: `*CorrelationTests` (`Suite=Core`). Serilog 3.1.1 package for `LogContext` on coordination/integration/runtime/advisory persistence projects. |
| 2026-04-07 | (architecture tests) | **`ArchLucid.Architecture.Tests`**: NetArchTest.Rules 1.3.2, 15× `[Fact]` + `Suite=Core` in `DependencyConstraintTests`; Tier 2/CLI API boundary via `GetReferencedAssemblies()` where namespace prefix would false-positive (`ArchLucid.Api.Client`). **`docs/ARCHITECTURE_CONSTRAINTS.md`** + index link. Project added to **`ArchLucid.sln`**. Phase 7 unchanged (await explicit go-ahead). |
| 2026-04-07 | (bugfix) | **`FindingsJson` read path:** `SqlFindingsSnapshotRepository` calls `FindingPayloadJsonCodec.HydrateJsonElementPayloads` after `FindingsSnapshotMigrator.Apply` so JSON-deserialized findings get typed `Payload` (matches relational read). Tests: `FindingPayloadJsonCodecTests` + `SqlFindingsSnapshotRepositorySqlIntegrationTests.GetById_when_no_FindingRecords_falls_back_to_FindingsJson`. |
| 2026-04-07 | 2.3 | ArchLucid product config section merge + authority resolver uses `ResolveArchLucidOptions` |
| 2026-04-07 | 2.4–2.6, 3.x, 4.x, 5.11 | Appsettings ArchLucid keys + `IOptions`/`ResolveArchLucidOptions` legacy-base fix; OIDC storage bridge; **`archlucid-ui`**; CI images **`archlucid-api`**/**`archlucid-ui`**; Prometheus/Grafana **`archlucid-*`** files; template **`archlucid-finding-engine`**; Terraform display strings (Entra/APIM/monitoring). |
| 2026-04-04 | 1.1 | layout.tsx title + heading → ArchLucid |
| 2026-04-05 | 1.2 | archlucid-ui/README.md product-facing name → ArchLucid (env keys unchanged) |
| 2026-04-05 | 1.4 | GLOSSARY.md title + ArchLucid note; outbox / integration dead-letter terms |
| 2026-04-05 | 2.1–2.2, 2.5 | Config bridges + `ResolveArchiForgeOptions` at early DI sites; UI proxy API key fallback; ops docs (architecture on a page, code map, config sunset, capacity/cost) |
| 2026-04-05 | 1.5 | Architecture context/components/containers titles + ArchLucid product framing; production cost guardrails (`production.tfvars.example`, LA quota note, WAF version var, FinOps playbook §9) |
| 2026-04-05 | (Improvement 5) | Merged `ArchiForge.DecisionEngine` into `ArchLucid.Decisioning` (later flattened to `ArchLucid.Decisioning.Merge` / `ArchLucid.Decisioning.Validation` — see 2026-04-06); added `ArchLucid.Persistence.Runtime` (orchestration, UoW, hot-path cache impls, blob store impls, archival); coordinator repos renamed to `ICoordinatorGoldenManifestRepository` / `ICoordinatorDecisionTraceRepository` in `Persistence.Data.Repositories`. |
| 2026-04-06 | Namespace flatten | Removed nested `Decisioning.DecisionEngine.*` namespaces: code under `ArchLucid.Decisioning/Merge/` and `ArchLucid.Decisioning/Validation/`; tests under `ArchLucid.Decisioning.Tests/Merge/` and `Validation/`; OTel meter `ArchLucid.Decisioning.SchemaValidation` (replaces `ArchiForge.DecisionEngine.SchemaValidation`). |
| 2026-04-06 | Quality batch (six improvements) | Stryker `break:60` (all `stryker-config*.json`); merged line-coverage gate documented in `docs/coverage-exclusions.md` (not Coverlet `<Threshold>` per assembly — see comment in `coverage.runsettings`); CLI `ResolveApiErrorMessage` for typed ProblemDetails; rename `DecisionTrace` → `RunEventTrace` (coordinator) / `RuleAuditTrace` (authority); `GET /v1/authority/runs/{id}/provenance`; UI `/runs/[id]/provenance`; `docs/DUAL_PIPELINE_NAVIGATOR.md`, `docs/CHAOS_TESTING.md`, `CliRetryDelegatingHandlerTests`. Prometheus SLO/outbox alerts already in `infra/prometheus/`. Phase 5–6 bulk project/namespace rename **not** started (explicit scope). |
| 2026-04-06 | 1.7 + CI coverage gate | Phase **1.7** pilot/release docs → ArchLucid product naming (`PILOT_GUIDE`, `OPERATOR_QUICKSTART`, `RELEASE_SMOKE`, `RELEASE_LOCAL`); CI **70%** merged line gate via `scripts/ci/assert_merged_line_coverage_min.py`; `RetrievalIndexingOutboxProcessor` uses `RunDetailDto.DecisionTrace` (fixes stale `RuleAuditTrace` property). |
| 2026-04-06 | **5.1** | All main-solution leaf test projects renamed to **`ArchLucid.*.Tests`** (dirs + csproj); solution + Stryker + Dockerfile + `nswag.json` paths updated; `OpenApiContractSnapshotTests` resolves `ArchLucid.Api.Tests.csproj`; docs: `CI_MIGRATION_CHECKLIST`, `ArchLucid.Api.Client/README`, `Migrations/README`. |
| 2026-04-06 | 1.10 (partial) + cognitive-load | Expanded `docs/DUAL_PIPELINE_NAVIGATOR.md` (side-by-side flow, shared-artifact matrix, coordinator HTTP→commit walkthrough); introduced sealed `RunEventTrace` / `RuleAuditTrace` with abstract `DecisionTrace` + `DecisionTraceJsonConverter` (stable OpenAPI shape); `docs/ONBOARDING_HAPPY_PATH.md` link + corrected `POST /v1/architecture/request`; `docs/ARCHITECTURE_INDEX.md` navigator blurb; `ArchLucid.Contracts.Tests/DecisionTraceJsonRoundTripTests.cs`. |
| 2026-04-06 | **5.2–5.10, 5.12–5.13 + 6.x** | **Phases 5–6:** `git mv` all product projects `ArchiForge.*` → `ArchLucid.*`; **`ArchLucid.sln`**; identifier pass (`AddArchLucid*`, `IArchLucidUnitOfWork`, `ArchLucidConfigurationBridge`, `ResolveSqlConnectionString` ArchLucid→ArchiForge fallback); NSwag **`ArchLucidApiClient`**; **`Directory.Build.props`** `ArchLucidApiClientPackageVersion` + MSBuild bridge; renamed `ArchiForge*.cs` source files; **`X-ArchLucid-*`** headers; CORS policy **`ArchLucid`**; scripts `phase6-rename-identifiers.ps1` / `bulk-replace-archlucid-phase6.ps1`. **Deferred:** template `archlucid-finding-engine` (5.11). |
| 2026-04-06 | (decomposition) | **`ArchitectureRunService`** → thin facade over **`IArchitectureRunCreateOrchestrator`** / **`Execute`** / **`Commit`** (`ArchLucid.Application/Runs/Orchestration/`). **`DecisionEngineService`** delegates to **`DecisionMergeInputGate`**, **`AgentProposalManifestMerger`**, **`DecisionNodeManifestMerger`**, **`ManifestGovernanceMerger`**, **`DecisionMergeTraceRecorder`**, **`GoldenManifestFactory`**, **`DecisionTraceManifestAttachment`**; DI registers merge strategies in **`ServiceCollectionExtensions.CoordinatorAndArtifacts`**. Single-arg **`DecisionEngineService(ISchemaValidationService)`** retained for tests. |
| 2026-04-06 | 1.10 (slice) | **`docs/API_SLOS.md`** — SLO table (99.5% / 5xx budget, p95 guardrail, synthetic canary); **`.github/workflows/api-synthetic-probe.yml`** — scheduled external `GET /health/live` + `GET /version`; **`docs/runbooks/SLO_PROMETHEUS_GRAFANA.md`** §10 + component table row. |
| 2026-04-06 | 1.10 (slice) | **`ILlmProvider`** + **`LlmProviderDescriptor`** / **`LlmProviderAuthScheme`** in `ArchLucid.AgentRuntime`; **`IAgentCompletionClient`** inherits `ILlmProvider`; Azure/echo/fake/wrappers expose `Descriptor`; **`DelegatingLlmCompletionProvider`** overlays telemetry labels; **`ILlmProvider`** registered in host composition. |
| 2026-04-06 | 1.10 (slice) | **Prompt reproducibility:** `Prompts/*SystemPromptTemplate`, **`IAgentSystemPromptCatalog`** / **`CachedAgentSystemPromptCatalog`**, **`AgentPromptReproMetadata`**, trace fields + **`IAgentExecutionTraceRecorder`**; **`AgentPromptActivityTags`**; Simulator traces; NSwag client regen. |
| 2026-04-06 | 1.10 (slice) | **`docs/CONTAINERIZATION.md`** — table + notes: RID-scoped `dotnet restore` matches Alpine `publish -r linux-musl-x64 --no-restore`; aligns with `ArchLucid.Api/Dockerfile` (avoids NETSDK1047 in CI/buildx). |
| 2026-04-06 | Phase 4 slice (4.4 partial) | **`infra/terraform-container-apps/main.tf`** worker `command` → **`ArchLucid.Worker.dll`**; locals comment; **`variables.tf`**, **`terraform.tfvars.example`**, **`README.md`**; **`infra/terraform-entra/variables.tf`** + **`infra/terraform/variables.tf`** descriptions (ArchLucid.Api / API backend). **`terraform fmt`** on `terraform-container-apps`. |
| 2026-04-07 | **1.10 + 1.11** | **Docs + UI docs rename sweep:** `docs/**/*.md` product/DI/telemetry names aligned to **ArchLucid** (`DI_REGISTRATION_MAP` → `Host.Composition`, `AddArchLucid*`, `X-ArchLucid-*`, `SYSTEM_MAP`, ADRs 0010–0011, SQL/runbook prose); **kept** `ArchiForge.sql`, `ArchiForge:*` / `ArchiForgeAuth` / legacy connection string keys / Prometheus alert names / `archiforge_*` metric prefixes where operational. **`archlucid-ui/docs/*.md`** titles + diagrams + examples; **`archlucid-ui/src/app/global-error.tsx`** heading → **ArchLucid**. |
| 2026-04-07 | **2.4–2.6, 3.x, 4.x, 5.11** | **Appsettings** → `ArchLucid` / `ArchLucidAuth` / `ConnectionStrings:ArchLucid` (+ `ResolveArchLucidOptions` legacy-`ArchiForge` base + `IOptions` bridge). **OIDC** sessionStorage `archlucid_oidc_*` + legacy read/migrate. **`archlucid-ui`** dir + CI/CD image names **`archlucid-api`** / **`archlucid-ui`**. **Prometheus/Grafana** files `archlucid-*.yml` / `dashboard-archlucid-*.json`. **Template** `templates/archlucid-finding-engine`. Terraform **human-facing** strings (Entra consent, APIM display, monitoring descriptions); many **resource addresses** / defaults still `archiforge*` until Phase 7 `state mv`. |
| 2026-04-13 | (quality improvement 1) | **Pre-commit gate tests:** `ArchitectureRunServiceExecuteCommitTests` (block/allow/skip gate), `ArchitectureRunCommitPipelineIntegrationTests` (real gate + audit + no manifest on block), **`PreCommitGovernanceGateTests`** edge cases. **Stryker:** baselines **62→65**, workflow tolerance **0.15→0.10** pp; **`MUTATION_TESTING_STRYKER.md`** ratchet section; **`TEST_STRUCTURE`** / **`TEST_EXECUTION_MODEL`** Tier **4c**; **`CHANGELOG`**. |
| 2026-04-13 | (structured output evaluation harness) | **`AgentResultParserTests`** + **`AgentCompletionModelMetadataTests`**; handler + **`RealRuntimeMixedModeTests`** wired to **`IAuditService`** / **`IScopeContextProvider`**; **`ExplanationServiceRunTests`** / **`ExplanationServiceComparisonTests`** pass **`ISchemaValidationService`**; run explanation test for invalid **`schemaVersion:1`** + real **`SchemaValidationService`**. **`ExplanationService`**: `StartsWith("{", …)` (compile on current `string` API). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (live E2E operator happy path) | **`e2e/helpers/live-api-client.ts`**; **`live-api-journey.spec.ts`**: create → execute → poll **ReadyForCommit** → commit → manifest UI → **`GET /v1/artifacts/runs/{runId}/export`** → governance submit/approve (**peer reviewer** ≠ dev submitter) → audit type assertions (**`RunExported`** included) → **`/governance`** **Load** → **`/audit`**. Docs: **`LIVE_E2E_HAPPY_PATH.md`**, **`TEST_STRUCTURE`**, **`TEST_EXECUTION_MODEL`**, **`V1_READINESS_SUMMARY`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (k6 smoke vs rate limit) | **`tests/load/smoke.js`**: runs → **`GET /v1/architecture/runs`**; audit → **`take=50`**. **`.github/workflows/ci.yml`** **`k6-smoke-api`**: **`RateLimiting__FixedWindow__PermitLimit=200000`** for API process (default **100/min** caused mass **429** on **`[EnableRateLimiting("fixed")]`** endpoints). **`PERFORMANCE_TESTING.md`** — local rate-limit note. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (quality improvement 1 — gates + coverage advisory) | **`ui-e2e-live`** + **`k6-smoke-api`**: **`continue-on-error` removed** (merge-blocking). **`assert_merged_line_coverage_min.py`**: **`--warn-below-package-line-pct 70`**, **`--annotations-file`**; **`coverage-metrics`** artifact is a folder (**`coverage-metrics.txt`** + **`coverage-annotations-assert.txt`**); **`coverage-pr-comment`** appends assert warnings. **`Suite=Core`** on learning/product-learning/evolution/ask integration tests + **`EvolutionSimulationReportBuilderTests`**; extra **`Learning`/`ProductLearning`/`Evolution`** API tests. Live E2E: **5xx retry** on run poll, **`expect.soft`** governance negatives, **`GET /v1/architecture/runs`**. Docs: **`TEST_EXECUTION_MODEL`**, **`V1_READINESS_SUMMARY`**, **`LIVE_E2E_HAPPY_PATH`**, **`TEST_STRUCTURE`**, **`PERFORMANCE_TESTING`**, **`coverage-exclusions.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (live E2E — error/edge paths 1A–1C) | **`e2e/live-api-conflict-journey.spec.ts`** (409 double-commit + audit **`ManifestGenerated`** stability, 404 commit); **`e2e/live-api-governance-rejection.spec.ts`** (reject + audit + UI); **`e2e/live-api-error-states.spec.ts`** (fake run detail, runs list, audit no-results, governance dashboard). **`live-api-client.ts`**: **`commitRunRaw`**, **`waitForReadyForCommit`**, **`rejectGovernanceRequest`**, **`postGovernanceRejectRaw`**. **`playwright.live.config.ts`** **`testMatch`** array; **`playwright.config.ts`** **`testIgnore: "**/live-api-*.spec.ts"`**. Docs: **`LIVE_E2E_HAPPY_PATH.md`**, **`TEST_STRUCTURE.md`**, **`TEST_EXECUTION_MODEL.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (traceability improvement 1 — audit indexes + runbook) | Migration **`055_AuditEvents_CorrelationId_RunId_Indexes.sql`** + **`ArchLucid.sql`** filtered indexes; **`AUDIT_COVERAGE_MATRIX`** §Indexes; **`AuditServiceCorrelationEnrichmentTests`**; **`AuditControllerSearchTests`** (`correlationId` / `runId` query); **`docs/runbooks/TRACE_A_RUN.md`** + runbooks index; **`infra/grafana/dashboard-archlucid-run-lifecycle.json`**; **`OBSERVABILITY`**, **`SLO_PROMETHEUS_GRAFANA`**, **`LIVE_E2E_HAPPY_PATH`**; live E2E **`correlationId`** assertions + **`searchAudit`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-13 | (quality improvement 2 — trace blobs + structural eval) | **`PersistFullPrompts`** default **true**; **`appsettings`** **`AgentExecution:TraceStorage`**; **`IAgentOutputEvaluator`** / **`AgentOutputEvaluator`** / **`AgentOutputEvaluationRecorder`**; OTEL **`archlucid_agent_output_structural_completeness_ratio`** + **`archlucid_agent_output_parse_failures_total`**; **`GET /v1/architecture/run/{runId}/agent-evaluation`**; contracts **`AgentOutputEvaluationScore`** / **`AgentOutputEvaluationSummary`**; tests **`AgentOutputEvaluatorTests`**; docs **`AGENT_TRACE_FORENSICS`**, **`OBSERVABILITY`**, **`AGENT_OUTPUT_EVALUATION`**, **`ARCHITECTURE_INDEX`**. OpenAPI snapshot regen when CI requires. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (Compose full-stack + k6 baseline) | **`docker-compose.yml`**: **`sqlserver-init`** one-shot creates **`ArchLucid`** DB before **api**; **api** env **`RateLimiting__*`** high permit limits for k6 (parity with **`ci.yml`** k6 smoke). **`record_baseline.ps1`**: **`try`/`finally`** teardown; **`K6_SUMMARY_TREND_STATS`** quoted for PowerShell; bind-mount path uses forward slashes. **`record_baseline.sh`**: **`trap`** teardown; quoted **`K6_SUMMARY_TREND_STATS`**. **`print_k6_summary_metrics.py`**: read k6 summary metrics when trend stats are on the metric object (not only **`values`**). **`LOAD_TEST_BASELINE.md`** **Initial** row + **`hotpaths.js`** **`p(95)<2000`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (live E2E — full `live-api-*.spec.ts` in CI) | **`playwright.live.config.ts`**: **`testMatch: ["live-api-*.spec.ts"]`** (all 15 live files). **`live-api-client.ts`**: advisory/replay/analysis/DOCX/alert-rules/graph/ask + digest subscription helpers. New specs: **`live-api-digest-webhook.spec.ts`**, **`live-api-concurrency.spec.ts`**, **`live-api-archival.spec.ts`**. **`.github/workflows/ci.yml`** **`ui-e2e-live`**: **`timeout-minutes: 35`**. Docs: **`LIVE_E2E_HAPPY_PATH.md`**, **`V1_READINESS_SUMMARY.md`**, **`TEST_EXECUTION_MODEL.md`**, **`QUALITY_ASSESSMENT_2026_04_14.md`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (quality improvement 1 — forensic prompts + reference eval) | Removed **`PersistFullPrompts`**; **`PatchInlinePromptFallbackAsync`** + SQL **`062`** inline columns; **`AgentOutputReferenceCaseRunEvaluator`** + **`AgentExecution:ReferenceEvaluation`** + OTEL **`archlucid_agent_output_reference_case_*`**; **`IAgentOutputEvaluationResultRepository`** / **`063`**; docs **`AGENT_TRACE_FORENSICS`**, **`OBSERVABILITY`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-14 | (quality improvement 2 — API semantic + contract alignment) | **`AgentOutputSemanticScore`** moved to **`Contracts`**; **`GET …/agent-evaluation`** returns nested **`Semantic`**, **`blobUploadFailed`**, **`averageSemanticScore`**; **`AgentOutputEvaluationRecorder`** semantic log threshold **0.3**; **`AgentOutputEvaluationRecorderTests`**; OpenAPI snapshot + **`ArchLucid.Api.Client`** regen. Blob retries / **`BlobUploadFailed`** / semantic evaluator already landed earlier. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-15 | (operator UI usability pass) | **`AppShellClient`**: collapsible **`SidebarNav`** + **`MobileNavDrawer`** + **`Breadcrumbs`** + **`CommandPalette`** (**cmdk**, Ctrl+K, run UUID quick-open); **`nav-config.ts`**. **Runs:** **`RunsListClient`** filter/sort + pagination **`button disabled`** for Previous/Next. **Governance dashboard:** multi-select + batch approve/reject (**`Promise.allSettled`**). **Wizard:** **`showSuccess`/`showError`** (**Sonner**) instead of inline toast div. **Tailwind:** **`OperatorShellMessage`**, Home, **`OperatorFirstRunWorkflowPanel`**. **Axe E2E:** expanded **`accessibility.spec.ts`** routes. **`breadcrumb-map.test.ts`**. Removed duplicate **`id="main-content"`** from nested **`main`** (layout owns landmark). **`npm test` + `npm run build`** green in **`archlucid-ui`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | (usability improvements 1–6) | **UI:** **`WelcomeBanner`** + home copy; **`/onboarding`** nav tier **essential**; route **`loading.tsx`** skeletons (**`Skeleton`**, **`RunsListSkeleton`**, **`RunDetailSkeleton`**, **`AlertsSkeleton`**, **`GenericPageSkeleton`**); **`EmptyState`** + **`empty-state-presets`** on runs/alerts/graph/compare/planning/governance; **`useRouteChangeFocus`** + **`RouteAnnouncer`** + **`getRouteTitle`** in **`AppShellClient`**; **`e2e/accessibility-focus.spec.ts`**; Vitest coverage for new pieces; **`getting-started`** fragment anchors. **API:** **`Scalar.AspNetCore`** + **`DeveloperExperienceOptions`** + pipeline (**Scalar** replaces **Swagger UI**; **`UseSwagger`** retained for JSON); **`DocsController`** → **`/scalar/v1`**; **`docs/API_EXPLORER.md`**; demo seed **`try/catch`** in **`ArchLucidPersistenceStartup`**; **`appsettings`** **`Demo`** / **`DeveloperExperience`** defaults. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-16 | (weighted quality improvements 1–6 — verification + gaps) | **`scripts/ci/assert_v1_traceability.py`**: UTF-8 stdout where supported; ASCII-safe logging; count matching tests even when other assemblies print per-DLL “no test matches”. **`coverage_gap_analysis.py`** refresh **`COVERAGE_GAP_ANALYSIS.md`**. **Imp2:** **`ArchLucid.Host.Composition.Tests`** — **`AuthSafetyGuardTests`** + **`ArchLucidAuthorizationPoliciesRegistrationTests`** (17). **Imp3:** repo root **`ArchLucid.sln` only**; no stub **`ArchiForge/`** product **`.cs`** tree; **Release** build green. **Imp4:** V1 matrix **11** filter rows validated via script. **Imp5:** **`FindingExplainabilityNarrativeBuilderTests`** (7). **Persistence:** archival cascade **073** + **`SqlRunRepository`** + extended cascade tests + matrix/API doc touchpoints (session carry-over). Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Fail-closed security defaults | Production-like **Azure Content Safety** mandatory + **`FailClosedOnSdkError`**, dev **`AllowNullGuardInDevelopment`**; **RLS bypass** requires env + **`ArchLucid:Persistence:AllowRlsBypass`** + gauge/alert **`archlucid_rls_bypass_enabled_info`** / **`ArchLucidRlsBypassEnabledInProduction`**; **fixed** rate limit default **60**/min + **`RateLimitingFixedWindowDefaultTests`**; prompt-injection eval datasets + CI **`eval_agent_quality.py --prompt-injection-only`**; **`docs/SECURITY.md`**. Phase **7.5–7.8** unchanged. |
| 2026-04-17 | CI quality gates + UI contracts | **Coverage:** merged **79%** line / **63%** branch / **63%** per-package line (`ci.yml`, **`CODE_COVERAGE.md`**, **`coverage-exclusions.md`**). **Stryker:** **`stryker-baselines.json`** **67→70** (+3 pp) + **`MUTATION_TESTING_STRYKER.md`**. **UI:** **`ui-axe-components`** (Vitest + **jest-axe**); default **`playwright.config.ts`** = live; **`playwright.mock.config.ts`** for **`npm run test:e2e`**; axe Playwright → **`live-api-accessibility*.spec.ts`**; **`live-e2e-nightly.yml`** uses default config. **FsCheck:** **`FindingsOrchestratorPropertyTests`**, **`DefaultGoldenManifestBuilderManifestIdentityPropertyTests`**. Docs: **`TEST_STRUCTURE`**, **`TEST_EXECUTION_MODEL`**, **`LIVE_E2E_HAPPY_PATH`**, **`ACCESSIBILITY`**, **`V1_READINESS_SUMMARY`**, **`TESTING_AND_TROUBLESHOOTING`**, **`LIVE_E2E_JWT_SETUP`**. Phase **7.5–7.8** unchanged. |
| 2026-04-17 | Cost visibility + per-tenant burst FinOps | **`LlmCostEstimationOptions`** defaults **`Enabled`** + illustrative USD/M rates; **`archlucid_llm_cost_usd_total{tenant}`** via **`ArchLucidInstrumentation.RecordLlmCostUsd`** from **`AgentExecutionTraceRecorder`**; Grafana **`archlucid-cost`** dashboard (**`infra/terraform-monitoring/grafana_dashboards/archlucid-cost.json`**) + **`grafana_dashboard` `cost`**; consumption budgets **50/75/90%** actual + **100%** forecasted (**`terraform-container-apps`** / **`terraform-sql-failover`**); **`tests/load/per-tenant-burst.js`** + **`.github/workflows/k6-per-tenant-burst-scheduled.yml`** (**`--max-p95-ms 3000`**); docs **`CAPACITY_AND_COST_PLAYBOOK`**, **`PERFORMANCE`**, **`LOAD_TEST_BASELINE`**, **`SCALING_PATH`**. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Pricing freeze (locked list prices 2026) | **`docs/go-to-market/PRICING_PHILOSOPHY.md`** — replaced `$X–$Y` placeholders with locked list prices effective 2026-04-17: Team $199/workspace/mo + $79/seat/mo; Professional $899/workspace/mo + $179/seat/mo; Enterprise from $60,000/yr; guided pilot $15,000 (credited on conversion); design partner 50% off Pro (first 3 customers). Added §5 Locked list prices with fair-value derivation, discount stack (trust −25%, reference −15%, self-serve −10%), and re-rate gates. Added §7 Sensitivity playbook. Cross-links added to **`POSITIONING.md`**, **`TRIAL_AND_SIGNUP.md`**, **`ROI_MODEL.md`**, **`ORDER_FORM_TEMPLATE.md`**, **`CUSTOMER_ONBOARDING_PLAYBOOK.md`**. CI guard **`scripts/ci/check_pricing_single_source.py`** added + wired into **`ci.yml`** `doc-markdown-links` job. **`docs/CHANGELOG.md`** updated. Phase **7.5–7.8** unchanged (deferred). |
| 2026-04-17 | Order form + ROI model alignment | **`ORDER_FORM_TEMPLATE.md`**: replaced all placeholder prices with links to PRICING_PHILOSOPHY.md §5; added run overage section with 150%-allowance worked example; three concrete worked examples (Team 3-seat, Pro 8-seat, Enterprise 50-seat/3-workspace); Addendum A (annual prepay); Addendum B (design partner — 50% off Pro, 3-customer cap, case study + quarterly reference deliverables). **`ROI_MODEL.md`**: added §8 (subscription cost + payback at full list ≈ 2 months and design partner discount ≈ 1.5 months); added §9 (3-year TCO vs LeanIX/Ardoq using publicly observed ranges from COMPETITIVE_LANDSCAPE.md; sensitivity at 50% savings → payback < 4 months); updated §10 (was §8) "How to present" references. **`scripts/ci/check_pricing_single_source.py`**: added `ROI_MODEL.md` to allowlist (computed totals from locked prices). **`docs/CHANGELOG.md`** updated. Phase **7.5–7.8** unchanged (deferred). |
